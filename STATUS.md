# STATUS — Verstak

Обновлено: 2026-08-08.

> **КАК ЧИТАТЬ ЭТОТ ФАЙЛ — прочти до того, как считать что-то незакрытым.**
> Разделы ниже «Текущее состояние» — **ХРОНИКА, а не список задач.** Каждая
> запись верна на СВОЮ дату и с тех пор не переписывается. Пометка «ЧАСТИЧНО»
> означает «столько было сделано в тот день», а не «работа брошена»: по одному
> блоку таких записей несколько подряд, и читать их надо снизу вверх как дневник.
> 06.08 четыре такие пометки приняли за четыре брошенных куска — при проверке по
> коду две оказались закрытыми, одна отложенной с причиной, одна живой.
> **Единственный список того, что открыто СЕЙЧАС, — раздел «Открытый остаток»
> и врезка в его начале.** Всё остальное — история.

## Текущее состояние

- **Оркестратор (задача 10) — ФРОНТ собран, флаг `orchestrator_default` ВКЛЮЧЁН по
  умолчанию (07.08).** Это последний пункт «под ноль» десктопной линии. Модель по
  ходу решает, что задача самостоятельная, и зовёт `spawn_task_session` — фронт
  создаёт ВИДИМУЮ дочернюю сессию (kind=main + parentChatId, ⑂ в Sidebar), сеет туда
  задачу и кладёт карточку-СЛЕД в родительский чат. Возврат результата = обновление
  карточки (готово/ошибка), а НЕ инъекция ответа ребёнка в контекст родителя (это НЕ
  delegate_task — родитель не блокируется). Смерть ребёнка mid-stream ловит
  `run-finalized`: карточка говорит «прогон оборвался», не застревает в «выполняется».
  Килл-свитч — тумблер «Отдельная сессия под задачу» в Настройки → «Поведение агента»
  (`RuntimeFlagsTab`); осознанное выключение (`stored === 'false'`) уважается. Пины:
  spawn-child-session (8) + spawn-session-card (6), анти-дрейф полярности флага.
- **Версия: опубликована `2.4.2`; в исходниках `2.4.3` — собрана 06.08 ЛИЧНО
  ДЛЯ ПАВЛА и НЕ опубликована** (тега `v2.4.3` нет, `latest.yml` никуда не ушёл,
  автообновление людям её не раздаёт). Провенанс сборки сходится с HEAD
  `e49e573`. Независимая проверка артефакта вне гейта: 325 пакетов в asar,
  транзитивная `p-retry` на месте, все починки 2.4.3 найдены в СОБРАННОМ файле
  (контрольный маркер обязателен — без него проверка дважды дала ложное «не
  найдено» из-за формы пути). Прогон гейта на `e49e573`: 4779 тестов, 0 падений.
  Смысл 2.4.3 — прогон переживает переключение чата, оборвавшийся подбирается без
  перезапуска, отчёт DOCX ложится туда, где человек его найдёт, разделы Workflows
  и Решения открыты.
- Прежняя `2.4.0` опубликована 01.08, тег `v2.4.0` на `41fdac58` (MATCH).
- Прежняя версия `2.3.0` опубликована 31.07, тег `v2.3.0` на `f091e71`
  (исторически — MATCH).
- Релиз собирается только из запушенного коммита и публикуется через fail-closed
  release gate с публичным readback установщика и `latest.yml`. Гейт с 29.07
  сохраняет полный вывод упавших тестов на диск, ограничивает параллелизм
  замеренным значением и отдельной проверкой сверяет полноту набора с эталоном.
- **Два бага релиз-инструментария, найденные при выпуске 2.3.0 (31.07,
  `f091e71`) — дорого достались, поэтому здесь.**
  · `release-build.mjs` подключал `node_modules` в чистую копию через **junction**,
    и electron-builder, обходя его через reparse-point, брал зависимости только
    ГЛУБИНЫ 1 — 34 пакета в asar вместо 324. Транзитивные (`p-retry` и др.) не
    попадали, приложение падало на старте `ERR_MODULE_NOT_FOUND`. Починено: сборка
    копирует дерево РЕАЛЬНО (robocopy), не через junction.
  · **гейт был ЗЕЛЁНЫМ на ОБЕИХ битых сборках**, потому что проверял исходники
    (на диске `node_modules` полон), а ломался собранный артефакт. Дефект поймал
    только шаг update-path — то есть ЧЕЛОВЕК, ставящий версию руками. Добавлена
    проверка `[3.5]`: извлечь `app.asar` из готового установщика, посчитать пакеты
    (порог 300 от рабочих 324), убедиться в наличии транзитивной `p-retry` —
    маркера, на котором падало. Появилась после случая, когда всё было зелёным и
    всё было сломано.
- Основная ветка: `main`, синхронизирована с `origin/main`.
- **Генератор планов доведён до результата живой проверкой (29.07).** Пять живых
  заходов, семь дефектов, все закрыты; функция проверена человеком на четырёх
  провайдерах: подписочный `codex-cli` (через осознанный фолбэк на Gemini),
  DeepSeek (два прогона), свой OpenAI-совместимый эндпоинт, прямые API. Планы
  создаются, отказы называют настоящую причину, а постановка не про открытый
  проект распознаётся моделью за секунды и с перечнем того, что в папке лежит.
  Бюджет времени (3 мин) и замер пройденных шагов в отказе оставлены НЕТРОНУТЫМИ:
  живая проверка показала, что они работают как задумано. Разбор — в
  `docs/CODE-AUDIT-2026-07-25.md`.
- Волна 2.2 закрывает скорость: параллельная подготовка контекста, одна сборка
  CLI payload, покадровый batch стрима и lazy-loading вторичных экранов.
- 2.2.1 закрыт: durable-счётчики пула подписочных аккаунтов в Настройках,
  живое состояние чата в боковой панели (точка больше не врёт), удаление
  тройного хранения состояния чата и мёртвых символов.
- 2.2.2 закрыт: чистый старт нового чата без фиктивного каталога ролей,
  честное состояние коннекторов, однократная передача правил проекта в CLI,
  видимые runtime-флаги и стабилизация релизных тестов.
- 2.1.2 Durable Agent Control Plane закрыт, включая модульный runtime делегирования.
- 2.1.3 управление подписками, аккаунтами, fallback lineage и расходом находится
  в `main`.
- 2.1.4 Model Gym закрыт: 30 детерминированных fixture-задач, role policy,
  адаптеры Verstak/Codex/OpenCode, воспроизводимый Arena-отчёт и release gate.
- 2.1.5 Outcome Mode закрыт: единый вход «До результата», три уровня глубины,
  durable-прогон, экран плана/jobs/маршрута/проверок/Proof и локальные метрики.
- 2.1.6 закрыт: durable-история прогонов по проекту, фильтры состояния и
  открытие старого результата без повторного обращения к модели.
- 2.1.7 закрыт: в истории собраны требующие человека jobs с безопасными
  cancel/resume/apply/reject и обязательным readback состояния.
- 2.1.8 закрыт: Markdown-паспорт результата из main-owned данных, системный
  Save As, redaction секретов/путей и readback записанного файла.
- 2.1.13 Memory lifecycle закрыт: срезы `pre-compress` и `session-end` сохраняют
  решения, факты и долги вместо сырого transcript; сырой автозахват
  (`auto_capture_memory`) переведён в opt-in.
- 2.2.2 Supply-chain закрыт разбором по достижимости: production advisories
  17 → 4. Оставшиеся четыре — цепочка `@huggingface/transformers` без
  upstream-фикса и без достижимого пути в коде Verstak; это пин с триггером
  пересмотра, а не задача.
- PerChatState закрыт полностью: срез 4.4 влит коммитом `ef4af73`, `chats` —
  единственный SSOT состояния чата.
- 2.1.10 декомпозиция agent runtime — срезы A–G выполнены, пакет закрыт. Ядро
  agent-loop разнесено по `ai/runner-*.ts`; из `ai:send` вынесены
  Outcome-preflight, выбор провайдера и модели, pre-flight подписочного аккаунта,
  fallback-маршрут, бухгалтерия прогона с таймаутом, IPC управления прогоном
  (`ipc/ai-resolve.ts`) и — срезом G — вся сборка контекста и промпта
  (`ipc/ai-send/{memory-context,system-assembly,provider-options,run-input}.ts`).
  `electron/ipc/ai.ts` 1569 → 1104 строк, тело `ai:send` 842 → 694. В хендлере
  осталась оркестрация прогона, а не сборка.
- 2.1.11 декомпозиция renderer **закрыта 2026-07-27**. Выполнены срезы A–D:
  `PolicyTab`, весь композер (`chat/ComposerSkillBar`, `chat/ComposerBudgetBar`,
  `chat/system-slash-commands`, `chat/ComposerInputRow`, `chat/ComposerMetaRow`),
  рендер потока (`chat/ChatStreamMessages` + `chat/message-parts`) и панели
  управления прогоном (`chat/ChatRunControls`). `Chat.tsx` 4122 → **3205** (−22%),
  поведение закреплено 46 пинами characterization, снятыми ДО каждого переноса и
  оставшимися зелёными без единой правки.

  **Пакет закрыт сознательно, с недостигнутой исходной целью «≤1500».** Вся
  разметка вынесена: в `Chat.tsx` осталось 393 строки JSX против 2588 строк
  логики — резать по разметке больше нечего. Дальнейшее сокращение требует
  декомпозиции логики, а это другой класс риска: подписка `ai.onEvent` намеренно
  ставится один раз за жизнь экрана, и её пересоздание теряет события молча —
  без падений и красных тестов. Цена (регрессия, невидимая для сетки) выше
  выгоды (читаемость). Не брать без отдельной постановки со своей сеткой.
  Вкладки `Settings.tsx` (4649), деление `layout.css` (21994) и UI smoke —
  тоже не берём: это техдолг без пользовательского эффекта.

- **Рантайм-флаги выведены в UI (27.07).** Вкладка Настройки → AI → «Поведение
  агента» (`src/components/RuntimeFlagsTab.tsx`) показывает все пять флагов разом:
  память проекта, знание о проекте в промпте, умный выбор модели, автоподмена при
  сбое, сырой автозахват. У каждого человеческая подпись и строка «если выключить».
  Дефолты НЕ изменены: четыре флага opt-out, `auto_capture_memory` остаётся opt-in.
  Полярность объявлена один раз в `src/lib/runtime-flags.ts`; расхождение с main
  ловит анти-дрейф страж, читающий исходники `electron/`. 30 пинов в трёх файлах,
  включая сквозной прогон «переключил → перезапуск БД → настоящие потребители main
  видят новое поведение».

- **Скиллы чужих CLI и канона работают в Verstak (28.07).** `loader.ts` читает
  каждый корень в ДВУХ формах — одиночные `*.md` и папки со `SKILL.md` внутри
  (формат Claude Code / Codex / канона Sistems); добавлен корень `~/.codex/skills`.
  Порядок от слабого к сильному: built-in → `~/.claude` → `~/.codex` → `~/.grok` →
  `~/.verstak` → `extraDirs`; при совпадении id старший побеждает молча.
  В Настройках → «Сервер скиллов» появилось поле «Дополнительная папка скиллов»
  (`skills_extra_dir`) — канон читается прямо из источника, без копирования.
  Security-инвариант server-скиллов не тронут.
  **Известное ограничение:** относительные ссылки внутри тела скилла
  (`references/*.md`) агент НЕ разрешает — они трактуются от корня проекта, а не от
  папки скилла. По абсолютному пути файл читается. Разбор — в аудите.

- **Хвост §10 закрыт (28.07): пять дефектов согласования плана.** Что изменилось
  для человека. (1) «Доработать» перестало вести в тупик: замечания правят ТОТ ЖЕ
  план — ревизия растёт, дубликат не создаётся, доработанный план снова уходит на
  согласование. Раньше на чат-пути `replan_plan` требовал Outcome-контекста,
  которого у продолжения нет, и ошибался ВСЕГДА. (2) «Одобрить» больше не меняет
  режим чата: права даются ОДНОМУ прогону (режим едет параметром `ai:send`), после
  него чат остаётся в своём режиме — раньше один клик переводил чат в «Принимать
  правки» навсегда. (3) Клик мимо окна согласования больше не считается отказом —
  отказ только кнопкой (раньше промах мышью отменял план и сносил чекпойнт, то
  есть убивал продолжение безвозвратно). (4) Карточка принадлежит своему чату:
  две карточки в разных чатах сосуществуют, модалка показывает карточку активного
  чата, и продолжение уходит туда же, а не в случайный открытый чат. Хранится в
  bundle чата — как всё остальное состояние чата (PerChatState 4.4). (5) У
  удержанного чекпойнта появился путь освобождения: Stop, Shift+Esc, закрытие
  проекта и удаление плана освобождают его через `plans:release-approval`; раньше
  это умела только кнопка в модалке, и снапшот полной истории прогона оставался в
  БД навсегда. Статус плана при этом не выдумывается — решения не было, план
  остаётся в «Планах».
  30 пинов в семи файлах, каждый краснел на коде ДО своего фикса.
  **Доработка после адверсариального ревью (28.07)** закрыла то, что первая
  редакция объявила шире сделанного: (а) approve ВТОРОЙ веткой — пайплайновой —
  всё ещё писал режим в настройку чата, и это была живая половина дефекта 2, ведь
  при выключенном тумблере карточки рождает именно Outcome-пайплайн; (б) доработка
  плана пайплайна переписывала его МОЛЧА, в обход quality gate и запрета на
  расширение writeScope — теперь отказ `REPLAN_PIPELINE_OWNED`; (в) переезд якоря
  плана на новый прогон оставлял осиротевший снапшот истории навсегда — прежний
  прогон отпускается явно; (г) два текста продолжения называли разные инструменты,
  а «дубликата нет» держалось на послушании модели — теперь на рантайме.
  **Тумблер «Ждать одобрения плана» ВКЛЮЧЁН по умолчанию с A3 §2.1 (30.07).**
  Прежняя запись «остаётся выключенным по умолчанию» устарела в тот же день и
  снята: дефолт `plan_approval_gate` сменён false→true, полярность переехала в
  `shared/contracts/runtime-flag-policy.ts` (main и renderer читают одно), никого
  не перещёлкивает молча. (Код-комментарий `plan-gate-modes.ts:24` про «выключен
  по умолчанию» тоже устарел — отмечено для отдельной правки.) **Известные
  ограничения (честно):** карточка чата чужого проекта теряется. Прежнее
  «карточка после перезапуска теряется БЕЗВОЗВРАТНО» ЗАКРЫТО A3 §2.3 (30.07):
  карточка согласования переживает перезапуск. Главный урок («любую правку
  план-гейта проверять на ОБЕИХ осях — чат-контекст с тумблером и Outcome-пайплайн
  без него») — в аудите.
- **Блок C закрыт (28.07): план и чек-лист связаны, ручной ввод шагов убран.**
  (1) В разделе «Планы» поле «Шаги, по одному на строку» заменено свободным «Что
  нужно сделать», кнопка — «Сгенерировать план»: она НЕ зовёт `plans.create`, а
  запускает обычный прогон, который сохраняет план через `create_plan` — с
  контекстом проекта, порогом согласования и проверкой качества ТЗ. Прямой ввод
  шагов клал в БД ровно напечатанное, минуя и планирование, и гейт.
  (2) Появились инструменты чек-листа `checklist_add` / `checklist_complete` /
  `checklist_list`. Главное правило: системный пункт закрывается ТОЛЬКО по
  доказательству — пустое evidence закрытием не является, а источник пункта не
  берётся из аргументов модели (всё, что заводит агент, — системное).
  `todo_*` для этого не используются: session todos живут один прогон, чек-лист —
  сущность проекта. (3) Во вкладке «Чеклист» у пункта видно происхождение
  (вы / Verstak), дату, необязательную связь с планом и доказательство закрытия.
- **§7.2 закрыт (28.07): план в чате — карточка, а не строка активности.**
  Событие `plan-created` кладёт в поток карточку (название, число шагов, статус
  «сохранён» / «ждёт вашего решения», кнопка «Открыть план»); карточка живёт в
  bundle СВОЕГО чата и чистится на новом прогоне, как preflight'ы. Подписка
  `ai.onEvent` не тронута — правился только обработчик события внутри неё.
- **Блок B-2 (§5 матрица режимов) — ЧАСТИЧНО (28.07): перевёрнуто решение о
  карточке, пауза на ответственном действии НЕ сделана.**
  > **ПОПРАВКА 06.08: вторая половина заголовка УСТАРЕЛА — пауза ПОСТРОЕНА
  > 29.07** (`electron/ai/plan-await.ts`, отдельная запись «Позиция 2: пауза перед
  > ответственным действием — ПОСТРОЕНА» в `docs/CODE-AUDIT-2026-07-25.md`).
  > Запись прожила лишнюю неделю и дважды направляла постановку задач: её читали
  > как незакрытую позицию. Заголовок оставлен дословно — он часть хроники, — но
  > читать его как текущее состояние НЕЛЬЗЯ. Это тот самый случай из §3.1
  > CLAUDE.md: запись, пережившая свою правду, опаснее отсутствия записи.
  Что изменилось: карточка
  согласования появляется в режимах «Запрос разрешений» и «Принимать правки», а в
  «Режиме планирования» её больше нет — согласовывать там нечего, выполнение
  запрещено всегда. Раньше было строго наоборот. Решение живёт чистой функцией
  `electron/ai/plan-gate-modes.ts`; ось Outcome-пайплайна проверяется первой,
  иначе матрица убила бы пайплайн (в его фазе плана режим форсится в `plan`, а
  approve — единственное, что двигает пайплайн дальше). Тумблер «Ждать одобрения
  плана» оставлен рубильником: выключен — поведение прежнее байт в байт.
  21 пин на чистую матрицу, мутации дают 5 и 1 красный; снимок «как есть» из
  `d1925d0` переписан в «как стало» — шесть кейсов перешли в противоположные,
  остальные остались дословно теми же.
  **Почему матрицу нельзя было включить по умолчанию:** у §10 нашёлся хвост из
  пяти дефектов. ОН ЗАКРЫТ 28.07 (см. пункт выше) — но тумблер по решению
  постановщика остаётся выключенным: включение по умолчанию проходит своей
  приёмкой, а не как побочный эффект починки.
  **Живой порог §4.2 закрыт (28.07).** Порог требовал полного structured spec у
  ВСЕХ шагов, а `create_plan` прямо разрешает его не передавать — fail-safe «нет
  объявления → карточка» вырождался: при включённом тумблере карточка вылезала на
  КАЖДЫЙ многошаговый план, включая читающий, и ломала правило «вопрос
  отвечается без единого клика». Вторая причина, найденная при разборе: даже
  присланный моделью spec на чат-пути ВЫБРАСЫВАЛСЯ (`specs` заполняется только
  под `ctx.outcome`). Теперь у шага три источника суждения по убыванию доверия:
  полный spec → частично разобранный сырой spec (только поля порога; в
  outcome/quality партиальность не течёт) → консервативный вывод по тексту, где
  признак записи перевешивает признак чтения, а отсутствие обоих оставляет шаг
  неопределимым. Читающий план без spec автоутверждается, пишущий даёт карточку,
  неопределимый даёт карточку. Инвариант безопасности не тронут: автоутверждение
  снимает карточку и НЕ выдаёт прав на запись.
  **Пауза на ответственном действии ПОСТРОЕНА 29.07 — рантайм-гейтом, не
  текстом.** Живёт в `resolveDecision` (`electron/ai/permission-rules.ts`):
  `classifyResponsibleAction` (публикация / отправка / удаление / права /
  платёж) стоит ВЫШЕ режима `auto`, тумблера авто-подтверждения и allow-правила
  permissions; внутри `resolveDecision` исключение одно — `bypass`. 30.07 закрыт
  ЧЕТВЁРТЫЙ обход — `bash_allowlist`: хендлеры перебивали confirm доверенной
  командой, не различая его причин, и `git` в списке ради `git status` молча
  пропускал `git push`. Теперь `resolveDecision` возвращает явную причину
  (`confirmCause: mode / responsible-action / ask-rule`), и allowlist гасит
  только режимную рутину — в обоих местах, `command.ts` и `process.ts`.
  Сетка: `tests/ipc/responsible-pause-gate.test.ts` (19 пинов, включая сквозные
  на настоящих хендлерах и контроль полезного сценария allowlist).
  Запись 28.07 «пауза НЕ сделана» была верна на свой момент и снята 30.07.

  **ГРАНИЦА ЗАЩИТЫ, названная честно (адверсариальный аудит 30.07).** Гейт
  накрывает ТОЛЬКО тех, кто зовёт `resolveDecision`: `command.ts`, `process.ts`,
  `file-ops.ts`, `connectors.ts`, `files.ts`, `execute-code.ts`. Каналы, которые
  до него не доходят и потому паузы не дают ни в одном режиме, включая `plan`:
  · ~~`attest_verification`~~ — **ЗАКРЫТО 30.07** (SEC-CMD-04): проверки идут
    через настоящий `runCommandHandler`, то есть судятся под именем
    `run_command` и наследуют весь гейт разом. Гейт под собственным именем
    инструмента был бы ПУСТЫШКОЙ: `decide` и `classifyResponsibleAction`
    разбирают аргументы только для run_command/connector_query/execute_code.
    Список разрешённых verify-команд отвергнут осознанно — оба существующих
    списка знают лишь JS-стек (+pytest/ruff/mypy) и сделали бы DoD недостижимым
    для Go/Rust/.NET/Java, которые описание `run_command` само предлагает
    модели; трение в `ask` снимает пользовательский `bash_allowlist`;
  · `browser_click` / `browser_navigate` (`ipc/tool-handlers/browser.ts`) —
    клик по «Отправить»/«Оплатить»/«Удалить» в залогиненном webview.
    **Частично закрыто 30.07:** в режиме `plan` клик больше не исполняется
    (SEC-CMD-06) — категория `MUTATING_BROWSER_TOOLS` заведена СПИСКОМ в
    `mode-policy`, поэтому будущий мутирующий браузерный инструмент попадает
    под режим по умолчанию. Чтение (`read_page`, `screenshot`) и `navigate` в
    `plan` работают как раньше. **Остаётся:** порог для `ask`/`auto` — его
    выбирает Павел по фактическим цифрам, которые копит наблюдаемость клика
    (`b13e9e1`), а не по нашим оценкам: три независимые проработки разошлись в
    полтора-два раза, потому что считать было нечего. Рамка доверия по origin
    отклонена постановщиком: в SPA один origin — это весь кабинет, человек
    одобрил бы по одному селектору всё внутри.
    **Шаг 1b закрыт НЕ ТЕМ, чем задумывался (SEC-CMD-07).** Разведка показала,
    что спорить о словаре опасных URL рано: не работал сам механизм, поверх
    которого любой словарь должен стоять. `extractArgText` не знал браузерных
    инструментов и отдавал пустую строку — правило с паттерном, включая
    АБСОЛЮТНЫЙ `deny`, не матчилось никогда; а хендлер знал единственный вердикт
    `block` и пропускал `confirm` в исполнение, из-за чего любой будущий
    классификатор URL оказался бы ложно-зелёным. Обе половины починены, и теперь
    у человека есть РЕГУЛЯТОР: он пишет свои правила по URL и селектору сам, не
    дожидаясь нашего словаря. Объявленная граница: в правилах `*` не переходит
    через `/`, для URL нужен `**` — семантика общая с правилами по путям и
    намеренно не менялась, пин это фиксирует.
    **Остаётся нашего словаря опасных URL (`?action=delete`, `/logout`,
    `unsubscribe?token=`) — НЕ СДЕЛАН, и это осознанно.** Критика показала три
    тривиальных обхода лексического подхода (`%6C` в пути не декодируется —
    измерено запуском node; регистр; пробел в значении) и главное: тот же
    `/logout` достижим КЛИКОМ, а клик вне `plan` не гейтован, то есть словарь
    закрыл бы половину двери. Решать — на живых цифрах.
    **Вторая дверь закрыта 30.07 (SEC-CMD-08):** `setWindowOpenHandler` в
    `main.ts` грузил в webview любой http(s)-попап, проверив одну лишь схему.
    Теперь редирект попапа судит тот же `applyPermissionRules` и под тем же
    именем `browser_navigate`, поэтому одно правило пользователя закрывает обе
    двери. Объявленная граница: обработчик СИНХРОНЕН, поэтому режим агента к
    попапу неприменим (это событие страницы, а не вызов инструмента), а
    `ask`-правило означает отказ, а не вопрос — спросить неоткуда, а тихо
    грузить нельзя.
  **Дыры покрытия классификатора закрыты 30.07 (SEC-CMD-09).** Главным было не
  число паттернов, а НОРМАЛИЗАЦИЯ: классификатор матчил сырую строку, тогда как
  денилист-близнец матчит ещё и деобфусцированную, — подход переняли, предпосылку
  нет, и `"git" push` с `g^it push` проходили мимо паузы. Теперь
  `deobfuscateShellWord` переиспользуется (не копируется), поэтому новый паттерн
  пишется один раз. Покрыты: глобальные флаги git (`-C`, `--git-dir=`, `-c`,
  `subtree push`), повседневная выкатка (`npm/yarn/pnpm run deploy`, `make deploy`,
  `docker push`, `helm upgrade`, `kubectl apply`, `gh pr/repo/gist create`,
  `aws s3 cp|sync`, `terraform apply`, `wrangler/firebase/flyctl deploy`),
  выгрузка мимо `-X`/`-d` (`curl -T|--upload-file|-F|--data-binary`,
  `wget --post-data`), `ssh/scp/rsync` по алиасу из `~/.ssh/config`,
  `git clean -fd|--force`. Универсальные коннекторы судятся по ОПЕРАЦИИ:
  `ssh(run_remote)`, `http(POST|PUT|PATCH|DELETE)`, `gsheets(append_row|update_cell)`;
  чтение через них молчит.
  **Объявленные пробелы (пины, не недосмотр):** самодельные обёртки
  (`./scripts/release.js`, `python upload.py`), индирекция без слов команды в
  строке (`X=push; git $X`), нелатинские формы. Плюс измеренная цена: матчинг
  идёт по подстроке, поэтому упоминание команды в тексте стоит лишнего вопроса —
  fail-safe. Ответ для непокрытого — правило пользователя, работающее с SEC-CMD-07.
  **Закрыто 30.07 (SEC-CMD-05):** подделка вердикта лишним ключом `connector`
  при исполнении по `id`, обход правила через алиас коннектора (канонизация
  стояла ПОСЛЕ вердикта) и мёртвый ключ `yandex-disk` при реальном id
  `yandex_disk`. Имя берут из одного места (`ai/connector-id.ts`) и гейт, и
  исполнитель; аргументы, называющие два разных коннектора, отклоняются целиком.
  **Остаток B-2 ЗАКРЫТ 30.07 (A3 §2.4).** Формулировка «статус не ставит ни одна
  строка кода» была неточной, и настоящий дефект оказался хуже: на пайплайн-оси
  статус ставился с 29.07 (`outcome.ts`, `blocked → 'skipped'`), а на чат-оси
  ставился ЛОЖНЫЙ — диспетчер знал только `done → 'done'` и `error → 'failed'`,
  тогда как отказ человека завершает прогон ШТАТНО. Шаг, который человек
  ЗАПРЕТИЛ, помечался ВЫПОЛНЕННЫМ: продукт утверждал неправду о своей работе
  ровно там, куда человек идёт проверять результат.
  Теперь признак отказа собирается в итоге хода (`runner-tool-outcome.ts`,
  по единому тексту `'User rejected'` всех семи спрашивающих хендлеров), а
  чат-ось пишет `'skipped'` — ТО ЖЕ значение, что и пайплайн-ось: разные статусы
  для одного исхода стали бы заготовкой дрейфа. «Почему» не потеряно вместе со
  схлопыванием: в `result` кладётся явная причина отказа, а не обрывок реплики
  модели. Слово «заблокирован», если понадобится, — подпись в UI, не третье
  значение в данных.
- **§10, недоделка закрыта (28.07): карточка больше не исчезает вместе с
  прогоном.** Дефект делал кнопку «Одобрить» недостижимой: прогон завершается
  штатно сразу после показа карточки, а renderer снимал карточку по `done` того же
  прогона — обоснование в комментарии описывало мир до §10. Убрано в двух местах
  диспетчера. Найдено разведкой под §5; 6 пинов, три краснели до фикса.
- **VSK-TASK-FLOW-A1, блок B — ЧАСТИЧНО (28.07), сделана позиция §10 «ожидание
  согласования снаружи прогона».** Что изменилось для человека: план, оставленный
  на согласовании, больше не умирает от таймаута прогона. Раньше `create_plan`
  ждал решения ВНУТРИ прогона, а рядом тикал сторож `agent_run_timeout_ms` —
  ушёл от карточки надолго, вернулся к «Прогон остановлен по таймауту», и
  утверждать было нечего. Теперь прогон завершается штатно и сторож снимается
  вместе с ним; ожидание держит БД (план в `draft` + `plans.agent_run_id` +
  чекпойнт прогона). После «Одобрить» работа продолжается по чекпойнту
  (`resumeFromRunId`), историю заново не пересобираем. Отклонение = ноль
  действий, чекпойнт освобождается; доработка ведёт к `replan_plan` того же
  плана, а не к новому. На время ожидания режим прогона ПОНИЖАЕТСЯ до `plan` —
  рантаймом, а не просьбой в тексте: запись в этом режиме блокирует
  `mode-policy.decide`. Реализация: `electron/ai/plan-await.ts` (чистая логика
  решения) + `plans:resolve-approval` + продолжение из `PlanConfirm`.
  9 новых пинов; главный написан на РЕАЛЬНОМ стороже с подкрученным временем, и
  рядом стоит контрольный кейс, где тот же сторож валит открытый прогон.
  **Остаток блока B:** матрица режимов §5; карточка `plan-created` в чате; снос
  мёртвого канала `pendingPlans` / `ai:resolve-plan`.
- **Блок B, §6: headless и scheduled план-гейта не получают — ПОДТВЕРЖДЕНО (28.07).**
  Проверка написана после §10 не зря: раньше ошибка стоила бы одного зависшего
  прогона, а теперь ожидание живёт в БД и переживает прогон — unattended-путь с
  гейтом копил бы висящие `draft`-планы и удержанные чекпойнты. Оба пути
  оказались не затронуты, причины РАЗНЫЕ: у scheduled нет `create_plan` в наборе
  инструментов, а условие гейта на его контексте (`agentMode: 'auto'`, без
  outcome) ложно даже при включённой глобально настройке; `scripts/verstak-cli.mjs`
  — отдельная программа, которая не импортирует код приложения вообще.
  6 пинов, из них два поведенческих; мутация (пустить гейт в режим `auto`) даёт
  2 красных.
- **VSK-TASK-FLOW-A1, блок B — ЧАСТИЧНО (28.07), сделаны позиции 1–2 из шести
  оставшихся.** Порог показа карточки §4.2: план, объявивший одно чтение,
  автоутверждается — карточки нет, план в БД остаётся; запись, ответственное
  действие или высокий риск дают карточку. Порог живёт чистым модулем
  `electron/ai/plan-threshold.ts`, fail-safe: нет объявления — карточка нужна.
  **Инвариант безопасности:** автоутверждение снимает только КАРТОЧКУ и НЕ
  повышает режим прогона; повышение осталось единственным путём — approve по
  кнопке. Значит план, объявивший себя read-only и попытавшийся писать, приходит
  в `mode-policy.decide` с прежним режимом и блокируется. Закреплено пином на
  исходник ветки автоутверждения.
  **Остаток блока B:** ожидание approve снаружи прогона и продолжение по
  чекпойнту; матрица режимов §5; тест headless без план-гейта; карточка
  `plan-created` в чате.
- **VSK-TASK-FLOW-A1, блок B — ЧАСТИЧНО (28.07).** Сделана только идемпотентность
  `create_plan` (§9): один прогон — один план. Реестр `sendId → planId` в
  `runner-shared`, чистится вместе с прогоном; повторный вызов возвращает
  существующий `planId` и подсказывает `replan_plan`. Решено РАНТАЙМОМ, не
  промптом. 6 пинов, мутация даёт 2 красных.
  **Остаток блока B (не начат):** порог показа карточки §4.2; инвариант «read-only
  план всё равно упирается в `mode-policy`»; ожидание approve снаружи прогона и
  продолжение по чекпойнту через `plans.agent_run_id`; карточка `plan-created` в
  чате; матрица режимов §5; тест headless без план-гейта.
  Решение по §10 принято постановщиком — вариант «б»; поле
  `agent_runs.awaiting_human_since` остаётся в схеме неиспользуемым (удалять
  миграцию задним числом нельзя, стоимость пустой колонки нулевая).
- **VSK-TASK-FLOW-A1, блок A закрыт (28.07) — инфраструктурный, видимых изменений
  нет.** Миграция **60** (append-only, старые не тронуты): `plans` получил
  происхождение (`chat_id`, `source_message_id`, `agent_run_id`), `tasks` — источник
  и НЕОБЯЗАТЕЛЬНУЮ связь с планом (`source` со значением `manual` по умолчанию,
  `plan_id`, `plan_step_id`, `evidence`), `agent_runs` — `awaiting_human_since` под
  сторож времени прогона из §10 ТЗ. Удаление плана **не каскадит** в чек-лист:
  `plans.remove` обнуляет связь, пункты остаются. Внешние записи в `plans` мимо
  `create_plan` (регламент проекта) читаются и не ломают список — под пином.
  33 пина: 11 characterization ДО правок остались зелёными без единой правки,
  22 новых на миграцию, связи и обратную совместимость.
  **Дальше по пакету:** блоки B (фаза плана в runtime, карточка, идемпотентность
  `create_plan`), C (инструменты `checklist_*`, снятие ручного Plan composer),
  D (сквозное выполнение, evidence, restart). В этой сессии они намеренно не
  тронуты.

## Открытый остаток

> **СВЕРКА 06.08 — что открыто НА САМОМ ДЕЛЕ.** Поводом стал вопрос Павла: в
> сводке значилось «позиции 1, 3, блок B, спринт 2.3 — все ЧАСТИЧНО, каждая
> сессия сделала кусок и остановилась». Проверено по коду, а не по документам:
>
> | Пометка | Что оказалось на деле |
> |---|---|
> | Блок B, «пауза не сделана» | **ЗАКРЫТО 29.07** — `plan-await.ts` существует. Запись устарела |
> | Позиция 3 (блок D), 3 подпункта «не сделано» | подпункт 1 (итог плана в чат) **ЗАКРЫТ 29.07** там же; подпункт 2 не строился **с названной причиной** (на чат-пути шагов как границ нет); подпункт 3 проверен ранее. Заголовок устарел |
> | Блок B, четыре пометки «ЧАСТИЧНО» подряд | это **ЧЕТЫРЕ ОТЧЁТА РАЗНЫХ СЕССИЙ** об одном блоке, читаются снизу вверх как дневник: идемпотентность → позиции 1–2 → ожидание согласования → матрица режимов. Не четыре брошенных дела, а один почти доделанный блок |
> | Спринт 2.3, позиция 2 (дубль регламента в CLI) | **ЗАКРЫТО 27.07**, замер приложен |
> | Гипотеза «пустой прогон `7f3f1862` отчитался успехом» (08.08, штаб) | **НЕ дефект.** По `agent_run_events`: реальный непустой ответ на «привет» (status completed), 0 инструментов — приветствию они не нужны, успех честный. «3 прогона за 41с» = ТРИ РАЗНЫХ ЧАТА (27/33/38), не повторная отправка. Гипотеза по цифрам 0 ходов/0 инструментов событиями НЕ подтвердилась |
>
> **Реально открытым осталось три пункта, все с причинами** (подробности ниже по
> разделу): мёртвый канал `pendingPlans` — снос отложен СОЗНАТЕЛЬНО, задевает
> пути abort/suspend; 5 эндпоинтов коннекторов из 31 не проверены — окружение
> исполнителя их не резолвит; `execute_code` — периметр доказан, включение ждёт
> живого прогона на авторизованном провайдере.
>
> **Корень проблемы не в работе, а в записях.** Сессии закрывали позиции и не
> возвращались снимать пометки в хронике; файл при этом отстал на два релиза
> (шапка говорила «2.4.0», когда опубликована 2.4.2). Отсюда правило, которое
> здесь и применено: **устаревшая запись снимается ТЕМ ЖЕ движением, что
> закрывает позицию** — иначе через неделю она направляет постановку задач,
> и это уже происходило дважды.

- **Галочка «Больше не спрашивать» обещает то, чего не делает — ЖИВОЕ, но
  fail-safe (30.07). ЧИНИТЬ СНЯТИЕМ ОБЕЩАНИЯ, А НЕ ИСПОЛНЕНИЕМ.**
  К модалке `CommandConfirm` ходят ШЕСТЬ инструментов (`pending-command`):
  `run_command`, `spawn_process`, `connector_query`, `execute_code`,
  `edit_spreadsheet` и MCP-тулзы. Галочка захардкожена на
  `rememberApproval('run_command', …)` (`src/components/CommandConfirm.tsx:15-16`).
  Мусорных правил она НЕ пишет: `derivePrefixRule` возвращает null, потому что
  первым словом сводки идут «Коннектор…», «Правка таблицы…», «MCP…»,
  «execute_code:…» — ни одно не проходит SAFE_COMMANDS/SAFE_SUBCOMMANDS. Значит
  для четырёх инструментов из шести галочка видна, нажимается и не делает
  НИЧЕГО, хотя её `title` обещает правило в `~/.verstak/permissions.json`.
  Ошибается она в сторону лишнего вопроса, поэтому последствия безобидны — но
  дефект уже случившийся, а не отложенный.
  **ОСТОРОЖНО С НАПРАВЛЕНИЕМ.** Очевидная починка — «научить её писать правила и
  для остальных четырёх» — сделает ХУЖЕ: `telegram` шлёт сообщение живому
  человеку, `yookassa` — про деньги, `social-publish` публикует. Запомненное
  «больше не спрашивать» для `connector_query` есть молчаливое одобрение будущих
  исходящих действий — ровно того класса, ради которого строилась пауза
  (SEC-CMD-04/05 и линия 29–30.07). Честная починка — УБРАТЬ ОБЕЩАНИЕ: не
  показывать галочку там, где она не работает и работать не должна.
  Снятие галочки — изменение интерфейса, зона Павла и Ильи; без его слова не трогаем.
- **Инструменты браузера работают БЕЗ открытой вкладки (закрыто a0f7a22 + b008403,
  02.08).** Прежняя запись «браузер живёт, только пока открыта вкладка Browser» СНЯТА
  как пережившая свою правду (§3.1): она читалась моделью и заставляла её просить
  человека открыть вкладку при рабочем инструменте. Теперь `BrowserView` смонтирован
  ВСЕГДА (`PersistentBrowser`, скрыт `display:none` вне своей вкладки), `window.verstakBrowser`
  живёт с запуска; стартовая гонка (вызов раньше монтирования) устранена ожиданием
  готовности API. Вкладка — окно наблюдения, не условие работы; откроют — видят ту же
  страницу, которой управляет агент.
- **`browser_read_page` отдаёт ТОЛЬКО `innerText`** — ни тега, ни `href`, ни
  разметки. Отсюда два следствия: модель не может построить достоверный
  CSS-селектор и в реальности опирается на текстовый фолбэк по четырём
  семействам (`a`, `button`, `[role=button]`, `input[type=submit]`), а значит
  оценка цены «решать по свойствам элемента» (вариант 2 гейта клика) сегодня
  посчитана неверно и требует пересчёта на живых данных.

- ~~**«Первый файл правил выигрывает» происходит МОЛЧА**~~ — **ЗАКРЫТО задачей B
  (07.08): предупреждение построено, текст утверждён Павлом.**
  `loadUserLayer` (`electron/ai/user-layer.ts`) по-прежнему берёт первый файл из
  `AGENTS.md → CLAUDE.md → GEMINI.md → .verstak/RULES.md` — приоритет НЕ менялся
  (решение штаба: слияние молча склеивает два набора инструкций, опаснее). Что
  изменилось: loadUserLayer возвращает `ignored` — существующие-но-взятые файлы,
  а прогон эмитит в ленту agent-progress предупреждение поимённо (что читается, что
  игнорируется) через `buildRuleConflictWarning` → `assembleSendSystem.ruleConflictWarning`
  → `emitAgentProgress` в `ipc/ai.ts`. Один файл → тишина (контрольный пин). 11 пинов.
  Текст утверждён Павлом (заголовок «Правила читаются из одного файла»); менять
  формулировку — только по его слову (регистр специально выверен: без капса/эмодзи,
  одно мягкое действие, ни слова о самом продукте).
  **Область:** только API-путь (там Verstak сам собирает user-layer). CLI-провайдеры
  читают `CLAUDE.md`/`AGENTS.md` своим механизмом мимо `user-layer` — предупреждать
  нам там не о чем.

- ~~**2.1.12 остаточный долг (P2)**~~ — **закрыт 27.07 целиком.** Рантайм-флаги
  выведены в UI (см. «Текущее состояние»); TODO незакрытой review-сессии снят как
  отменённое требование (нужен lifecycle ревью, которого в планах нет, а доступный
  сигнал означает другое и заставил бы статус врать); по lazy-импортам `App.tsx`
  премиса не подтвердилась — все 26 дают собственные чанки, статически не
  импортируется ни один, убирать нечего. Вывод закреплён стражом
  `tests/lib/app-lazy-imports.test.ts`. Разбор — в аудите.
- **Mode C (CLI session-continuity) — разведка проведена 27.07, решение ждёт
  цифр.** Установленный `claude 2.1.207` нативную сессию **умеет**: `--session-id`
  и `--resume` живьём удержали один `session_id` на три хода, транскрипт одного
  файла накопил все реплики, тогда как одноразовое плечо породило три независимых
  сессии. Значит допущение в шапке `electron/ai/cli-prompt.ts` («CLI одноразовые»)
  для `claude-cli` устарело. НЕ снято: input-токены по ходам — дочерний CLI в
  окружении исполнителя не авторизован. Пока неизвестно, экономит ли сессия
  отправку истории или лишь попадает в prompt cache, размер выигрыша неизвестен и
  реализацию начинать рано. Отчёт, таблицы и готовая команда для досъёма —
  `docs/cli-session-recon-2026-07-27.md`.
- **Дубль регламента в CLI закрыт 27.07 (спринт 2.3, позиция 2).**
  `cliReadsLayerNatively` больше не захардкожена в `false`: пропуск включается для
  одного проверенного сочетания — `claude-cli` и слой РОВНО `CLAUDE.md`. Замер на
  реальном размере регламента: payload **28 904 → 6 164 символа, экономия ≈5 685
  токенов на каждом ходе**.
  **Премиса при этом подтвердилась не полностью, и это важно:** в самом репозитории
  Verstak дубля НЕТ. Там есть и `AGENTS.md`, и `CLAUDE.md`, а `loadUserLayer` берёт
  первый найденный — инжектится `AGENTS.md`, тогда как CLI подхватывает `CLAUDE.md`.
  Уходят два РАЗНЫХ файла; экономия здесь ноль, и это измерено. Выигрыш получают
  проекты, где `CLAUDE.md` — первый найденный файл правил.
  Остальные CLI-провайдеры не тронуты: документированного нативного чтения у них не
  подтверждено, поведение прежнее. 11 пинов, мутация даёт 3 красных.

- **`execute_code` (PTC) — периметр доказан, включение ждёт одного живого прогона
  (27.07).** Инструмент остаётся за флагом `ptc_enabled` (по умолчанию выключен), но
  это больше не «висит на комментарии»: живая петля собрана на реальных read-only
  тулзах и реальных файлах, итог в контексте более чем в 10 раз короче суммы сырых
  tool-результатов; периметр закреплён пинами — в песочнице ровно пять read-only
  тулзов, права совпадают с `run_command` во всех пяти режимах (в `plan` блок, в
  `ask` подтверждение), гейт по умолчанию тоже под пином. Не хватает единственного:
  прогона, где МОДЕЛЬ сама вызывает инструмент, — нужен авторизованный провайдер.
  Условие включения и точная инструкция — в аудите.

- **Коннекторы: аудит 27.07 + позиция 6.** 31 зарегистрирован (цифра верна).
  Эндпоинты живьём: **20 отвечают**, 6 с динамическим адресом, 5 не проверены из-за
  резолвера окружения исполнителя. Сломанных нет.
  **Реально настроены 2 из 30** — Я.Метрика и Я.Вебмастер. Прежний вывод «все 31
  настроены» был ОШИБКОЙ метода: заполненность считали по длине ЗАШИФРОВАННОГО
  значения, а шифрование пустой строки тоже даёт блоб ~44 символа; строки в
  настройках появляются от одного сохранения пустого поля. Поправка — §0 отчёта.
  **Авторизованный вызов сделан** (позиция 6): Я.Метрика, `HTTP 200`, сервис вернул
  98 счётчиков — ключ не просто лежит, он подходит.
  `list_connectors` больше **не врёт**: `info()` объявляет `requires`/`requiresAnyOf`,
  реестр сверяет их с хранилищем и отдаёт `needs-config` с именами недостающих
  ключей (значения не читаются). Тесты коннекторов: 31 файл, 313 тестов; в сеть
  по-прежнему не ходит ни один — это осознанная граница.
  Отчёт — `docs/connectors-audit-2026-07-27.md`.

Подробности и история срезов — `docs/CODE-AUDIT-2026-07-25.md`.

## Отдельная линия

Browser Employee / Connected Eyes развивается отдельно в worktree
`verstak-extension` на ветке `codex/browser-extension-readonly-mvp`. Не смешивать
с `main` до живой приёмки расширения.

## Источники истины

- Возможности и установка: `README.md`.
- История изменений: `CHANGELOG.md`.
- Текущая документация: `docs/README.md`.
- Активный код и тесты важнее этого снимка.

## Решение по техдолгу (2026-07-27)

Линия декомпозиции кода закрыта. Оба монолита, которые реально мешали, разобраны:
`ipc/ai.ts` (1569 → 1104) и разметка `Chat.tsx` (4122 → 3205). Проверка гипотезы
«много мусорного кода» её опровергла — структурного мусора нет. Дальнейший рефакторинг
даёт читаемость, но не даёт пользователю ничего, а риск тихих регрессий растёт.
Следующая работа по Verstak — продуктовая, не уборочная.

## Следующий продуктовый шаг

Продуктовая волна 2.1.x закрыта целиком, memory lifecycle выполнен. Открытый
остаток (см. выше) — техдолг, а не продукт: он не выбирает следующую волну и не
должен её подменять. Следующий продуктовый шаг ждёт решения Павла.
