# План правок Verstak — 2026-07-24 (dev-hq, VSK-AUDIT-2.0.11)

**Статус на 24.07:** ✅ Фаза 0 (`cd99dd0d`) · ✅ Фаза 1 (`73620258`, `f3e886a9`) · ✅ Фаза 2 (`d7e24c65` — 2.1 оказалась уже проведена в 2.0.8-E/F, верифицировано по коду; 2.2 — найдено единственное недостающее звено: диспетчер не пробрасывал cacheWriteTokens в sessionUsage, red-first 3 теста) · ✅ Фаза 3 (`3138fe7f` NUL→`\u001F` + страж очищен; `6e289f0a` 3 eslint-деферрала сняты реальными фиксами; suppression ledger 2.0.10-G закрыт целиком) · ⬜ Фазы 4–5 впереди. Бонус-фиксы: package-lock sync (`b0f546f2`), дедуп recent ChatHome.

База: обкатка HEAD (поверх v2.0.11) — type 0 err · test:fast 3584/0 · build OK · lint 0 err/36 warn · mojibake OK.
Правила проекта соблюдаем: red-first тест → фикс · перед коммитом `npm run type && npm run test:fast` · allowlist файлов на срез · денежный код (Фаза 2) → адверс-ревью до merge · MIGRATIONS только append.

---

## Фаза 0 — Починка установки (блокеры, ~1 сессия)

**Зачем первой:** сейчас чистый клон не собирается — любой новый агент/машина/CI упирается в это до любой другой работы.

| # | Правка | Файлы | Приёмка |
|---|---|---|---|
| 0.1 | Решение по vite/electron-vite: предпочтительно bump `electron-vite` до версии с peer-поддержкой vite 8; если такой нет — откат `vite` на ^7. Фолбэк (быстрый): `.npmrc` с `legacy-peer-deps=true` | `package.json`, `package-lock.json`, возможно `.npmrc` | `npm ci` на чистом дереве проходит БЕЗ флагов; `npm run build` зелёный |
| 0.2 | Postinstall-скрипты (electron/esbuild/sharp/onnxruntime-node блокируются allow-scripts): задокументировать `npm approve-scripts` в README/AGENTS.md или зафиксировать разрешения в конфиге | `README.md` / `AGENTS.md`, возможно `.npmrc` | Повторная чистая установка даёт рабочий electron.exe + better-sqlite3 без ручных шагов сверх документированных |
| 0.3 | Smoke после фазы: чистый worktree → `npm ci && npm run type && npm run test:fast` | — | Гейт зелёный из чистого дерева |

---

## Фаза 1 — Закрепить незакоммиченное (~0.5 сессии)

**Зачем:** 250+ строк работы висят вне git — риск потери; pure-фикс без теста нарушает регламент.

| # | Правка | Файлы | Приёмка |
|---|---|---|---|
| 1.1 | Тест на `loader.ts` (`typeof raw !== 'string'` → null): юнит на parseSkillFile с не-строковым входом + BOM-кейс рядом | `electron/ai/skills/loader.ts`, `tests/ai/skills-loader.test.ts` (или существующий файл тестов скиллов) | Red (без фикса падает) → green; коммит |
| 1.2 | ChatHome отдельным срезом: characterization-тест рендера (HOME_AGENTS отдают карточки, клик по агенту → prompt в композер, aside пуст/выбран), i18n-parity уже стражем покрыт | `src/components/ChatHome.tsx`, `src/components/Chat.tsx`, `src/i18n/*`, `src/styles/layout.css`, `tests/` +1 файл | Тест зелёный, `type` 0, коммит отдельным срезом |

---

## Фаза 2 — Деньги (P0, денежный код → адверс-ревью, ~1–2 сессии)

| # | Правка | Файлы | Приёмка |
|---|---|---|---|
| 2.1 | Тарификация cacheWrite: `ModelPrice.cacheWrite` + учёт в `billableInputTokens`/cost-guard и в persistence (`agent_run_usage`) — ОДИНАКОВО в обоих местах, чтобы CAP и история не разошлись | `shared/contracts/usage.ts`, `src/lib/pricing.ts` (или где price-слой), `electron/ai/cost-guard.ts`, runner'ы | Red-first: Claude cache-creation учитывается в стоимости; inclusive-провайдеры не изменились (характеризация) |
| 2.2 | Per-chat cost pill в UI: перейти на тот же billable-слой (сейчас занижен для Claude) | `src/components/Chat.tsx` (точечно), store | Pill для Claude == значению из usage-слоя на фикстуре |

⚠️ Павлу предупреждение: после 2.1 дневной CAP для Claude будет срабатывать раньше — это прекращение занижения, не регрессия.

---

## Фаза 3 — Механический техдолг (~1 сессия)

| # | Правка | Файлы | Приёмка |
|---|---|---|---|
| 3.1 | NUL-байты → `\x1F` в составных ключах | `electron/ai/memory-hooks.ts`, `electron/ai/yandex-gpt.ts` | `git diff` по файлам снова текстовый; страж `no-binary-sources` baseline сокращён до 0; тесты провайдеров зелёные |
| 3.2 | Снять 3 eslint-disable деферрала из ledger: floating-promise в bootstrap main.ts (обработка ошибок, не void), await-thenable в claude.ts, switch-exhaustiveness в claude-cli/codex-cli (недостающий case) | `electron/main.ts`, `electron/ai/claude.ts`, `electron/ai/claude-cli.ts`, `electron/ai/codex-cli.ts` | `lint:changed` 0 errors без disable-комментариев; ledger очищен |

---

## Фаза 4 — Продуктовые решения (нужно слово Павла, потом ~1 сессия каждая)

| # | Вопрос | Варианты |
|---|---|---|
| 4.1 | **Exact Rewind**: ручной Windows-smoke Павла по чек-листу → включить флаг или держать выключенным в 2.0.12 | Smoke-чеклист: откат после серии правок, откат после crash-resume, read-fail сценарий |
| 4.2 | **Мёртвый UI**: 16/18 тумблеров уведомлений + smart-approve — довести до рабочих или честно удалить | Рекомендация: удалить мёртвое (честность UI = фишка продукта), smart-approve — отдельной карточкой, если нужен |
| 4.3 | **D2-хвост подписок**: спец-текст «аккаунт удалён, выберите другой» вместо общего «провайдер недоступен» | Маленький срез: строка i18n + рендер по sendId≤0 |

---

## Фаза 5 — Декомпозиция Chat.tsx (большая, 3–5 сессий, после 2.0.12)

План уже в AGENTS.md п.5 и в комментариях `projectStore.ts`:
1. `PerChatState` (map chatId → ChatStateBundle) вместо top-level полей + chatSnapshots.
2. `ChatSessionLifecycle` (enterChat/leaveChat) вместо setProject + switchChatSession + newChatSession.
3. Разрез `send()` (сложность 131) на этапы: resolve route → policy → stream → finalize — каждый чистой функцией с тестом.

Порядок: сначала characterization-харнес на текущее поведение send (как делали для Settings 2.0.8-A), потом вынос по одному классу за срез.

---

## График (сессии, последовательно)

```
Фаза 0 (1) → Фаза 1 (0.5) → Фаза 2 (1–2, ревью) → Фаза 3 (1) → [релиз 2.0.12] → Фаза 4 (по решениям) → Фаза 5 (3–5)
```

Итого до релиза 2.0.12: ~4–5 сессий. Фазы 0–1 можно сжать в одну сессию.
