# Живые проверки — список для Павла

**Статус: ОТДАН 05.08.2026.** Код-часть задач 1–3 закрыта и влита в `main`
(`609bc5a` обрыв прогона, `25fbe5f` папка→DOCX, `5b762c6` разбор кеширования,
эталон 4764, ноль падений). Всё ниже — то, что тестами доказать нельзя
по построению: §3.1 «зелёные тесты ≠ работающая функция».

Порядок неважен, время — минут двадцать. Если пойдёт не так на первом же
пункте — остальное можно не делать, отчёт об одном отказе полезнее трёх
галочек.

---

## 1. Обрыв прогона — второй тайминг (задача 1)

**Зачем:** харнес доказал механизм, но §3.1 — «зелёные тесты ≠ работающая
функция». Со стороны пользователя ни один сценарий пока не проверен.

1. Длинная задача на **«Свой (OpenAI-compatible)»** (claude-opus-4-8) — та же,
   на которой рвалось 01.08. Дождаться середины ответа, переключить чат, уйти в
   другой проект, вернуться. **Ожидание:** ответ доезжает; если оборвался —
   появляется предложение восстановить, а не «Ответ не сохранён» без выхода.
2. То же самое на **DeepSeek**, на заведомо ДЛИННОЙ задаче. **Это контрольный
   случай:** он должен показать, что дело не в провайдере, а в длине ответа.
   Если DeepSeek не рвётся даже на длинной — объяснение неполно, вернуть в работу.
3. Проверить формулировку карточки при реальном обрыве: она НЕ должна говорить
   «приложение было закрыто» (исправлено в `611dd83`, `609bc5a`).

## 2. Сценарий A1 — папка → отчёт → DOCX (задача 2)

1. Настоящая клиентская папка с документами вперемешку (docx/xlsx/pdf/txt),
   среди них **заведомо битый или огромный** файл.
2. Формулировка своими словами, как написал бы не-разработчик: «собери короткий
   отчёт с таблицей выводов и сохрани DOCX рядом».
3. **Проверить три вещи:** файл лёг ТУДА, куда просили (и ответ назвал полный
   путь); агент перечислил, что прочитал и чего НЕ смог; расход показан суммой.
4. Повторить на **Kimi** — второй дешёвой модели.

## 3. Снятие сырого `usage` (задача 3, остаток §4 отчёта)

Если пункт 2 и так идёт на DeepSeek/Kimi — снять заодно. Вопрос один: под каким
именем провайдер отдаёт попадание в кэш (`prompt_tokens_details.cached_tokens`
или своё). Закрывает несделанный остаток `docs/cache-audit-2026-08-05.md`.
**Временный дамп убрать сразу** — сырой ответ мимо чистильщика уже уезжал в лог
(`f7f7150`).

---

## Что НЕ входит и почему

- **Три не-разработчика для приёмки A1** — Павел их пока не назвал. Это
  отдельный, более дорогой заход; сначала должен пройти сценарий выше своими
  руками.
- **Изоляция браузера Verstak** (`partition`) — разлогинит его везде в
  встроенном браузере. Решение за ним, к этому списку не привязано; список
  доменов показать не могу, читать его куки — не моя зона.
