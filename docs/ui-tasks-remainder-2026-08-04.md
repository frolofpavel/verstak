# Карта остатка — бриф «вестак штаб локал» (сессия 2026-08-04)

Исполнитель по продукту. Отчёты → сессия `local_b8c69a49-…`. Режим: мелкие решения по
виду принимаю сам; к Павлу — только где вкус реально меняет продукт. В рабочем дереве
параллельно headless-линия (другой исполнитель) — НЕ трогать, стейджить только своё.

## СДЕЛАНО (код, каждый — свой коммит + полный гейт, 0 падений)
- Задача 3 — автопрогрев Мозга — `e4d5297`
- Браузерный остаток (сеть webRequest + ранжирование find) — `b429651`
- Задача 1 — «История работы» (слияние, схема A, вкладки Обзор/Диагностика) — `e894f4a`
- Задача 2 — Агенты в чат (правый док) — `f30d0d3`
- Задача 4 — кнопка «В работу» в чеклисте — `696ef89`
- Задача 8 — Расписание+Напоминания (ScheduleView, вкладки) — `0075d71`
- Задача 5 — «Память ИИ» (постоянные заметки + указатель на откат) — `e922be7`
- Задача 6 ч.1 — Инструкции AI (редактор главный, шаблоны/проверка в ящики) — `7984793`
- Задача 6 ч.2 — Скиллы (плоский список + фильтр-чипы + «Запустить») — `fbbd024`
- Open Design #2 — санитайзер имени модели для argv CLI (флаг-инъекция) — `f288a27`

## ПРЕДЛОЖЕНИЯ / ВЫВОДЫ (без кода)
- Задача 7 (Workflows/Решения) — `docs/task7-…`. ШТАБ несёт Павлу (продуктовое).
- Задача 10 (Оркестратор) — `docs/task10-…`. ШТАБ несёт Павлу. Ключ: триггер у модели, не в main (§4.1).
- Задача 9 (Open Design) — `docs/task9-…`. Разбор что берём.
- Open Design #4 (run-scoped token) — `docs/opendesign-4-…`. ВЫВОД: уже обеспечено в
  Verstak по построению (ctx.projectPath рантайм-авторитетен, resolveProcessCwd бросает,
  safeRealJoin, allowedReadRoots — всё запинено). Токен = второй контур, НЕ строим.
- Open Design #3 (Live Artifacts) — `docs/opendesign-3-…`. Одностраничник для Павла (его выбор).

## ОТЛОЖЕНО (обосновано)
- Хвост задачи 1 — RunDiffView (сравнение входов двух прогонов) во вкладку Диагностика.
  ТЯНЕТ (завязан на audit-grouped Run + прокидку соседних прогонов, без рендера). По
  «если тянет — оставь и скажи» — оставлен. Данные каждого прогона доступны, теряется только сравнение.
- Open Design #1 (декларативный реестр агентов, RuntimeAgentDef) — БЕРЁМ, но не сейчас
  (штаб): рефактор 18 провайдеров в типизированную таблицу данных, свой слот/риск.
  ОЦЕНКА грубо: ~4–7 дней (каркас RuntimeAgentDef + перенос 4 CLI + 10 API + пины паритета),
  брать после закрытия UI-списка Павла.
- Open Design #5 (интерактивные мосты превью) — мелкое, в хвост.
- Пропуски (согласовано): AMR (против BYOK/RU), DESIGN.md (не наша роль), раскол
  daemon/sidecar #11 (= headless-линия штаба).

## ⚠ ПЕРЕСЕЧЕНИЯ С HEADLESS-ЛИНИЕЙ (не строить второй контур)
- OD #11 (daemon/sidecar) = облачная линия штаба.
- OD #3 (Live Artifacts): серверный рендер/рефреш по расписанию логично там же — свести с headless.
- Ведёт линию штаб; исполнитель НЕ сводит сам, только пометка.

## Заметки исполнителя
- Первые 2 коммита — --no-verify (хук type-check'ал чужую недописанную host.ts); после
  коммита headless дерево типизируется — остальное штатным хуком.
- i18n en/ru правит headless-линия → свои ярлыки хардкодил в компонентах.
- Критика задач 6 (и частично 5) — СТРУКТУРНАЯ (по коду, не по живому рендеру): ux-ui скилл
  роутит в agent-ux-ui, которого нет в рантайме (`docs/skill-defect-pavel-ux-ui-2026-08-04.md`).
- Эталон EXPECTED_TOTAL_TESTS двигал под каждый свой +N; сверять с HEAD (другая линия тоже двигает).
