/**
 * Версия, на которой профиль РОДИЛСЯ.
 *
 * Ставится один раз, в main, на пустом хранилище настроек — до того, как
 * онбординг или что-либо ещё запишет первую строку. Отличает первую в жизни
 * установку от обновления, которое пришло в живой профиль (§5 ревью 2.6.4).
 *
 * Ключ живёт в shared, чтобы main (кто ставит) и renderer (кто читает) не
 * разъехались в написании строки — урок plan_approval_gate, CLAUDE.md §5.
 */
export const FIRST_SEEN_VERSION_KEY = 'first_seen_version'
