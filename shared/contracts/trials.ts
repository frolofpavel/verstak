/**
 * P1: состязание исполнителей — контракт, общий для main и renderer.
 *
 * Допущение объёма оценки живёт ЗДЕСЬ, а не в electron/ai/trial-estimate.ts,
 * потому что у цифры два читателя: main считает по ней центы, а панель обязана
 * показать её рядом с центами (правило постановки: допущение объявлено явно).
 * Две копии однажды разъехались бы — и подпись в UI врала бы расчёту.
 */

export interface TrialTokenAssumption {
  inputTokens: number
  outputTokens: number
}

/**
 * Объём «типичной агентной задачи» для дефолтной оценки. Ориентир — живой разбор
 * трёх агентов 08.08 (задачи по $3–9 на моделях среднего/верхнего ценника).
 * Это ДОПУЩЕНИЕ для сравнения исполнителей между собой, не обещание чека.
 */
export const TRIAL_ESTIMATE_TOKENS: TrialTokenAssumption = { inputTokens: 400_000, outputTokens: 30_000 }
