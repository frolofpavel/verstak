# Задача Илье — живой тест ядра v3 + доказательство «Opus на DeepSeek»

**Кому:** Илья (@filatoff_me). **Когда:** дома (завтра).
**Что:** прокликать в запущенном Verstak харнес v3 (A–D собран мной ночью, type+build+тесты зелёные) и снять доказательство шага E. Логика готова — нужна живая проверка на реальной модели.

## Контекст
- План: `Downloads/verstak-product-plan-v3-2026-06-19.md` + план #5 в Verstak.
- Собрано (в `main`): A. ядро надёжности (verify-gate+авто-починка), B. task-spec контракт, C. профиль проекта, D. beast+max-steps.
- Тезис, который проверяем: **результат уровня Opus на дешёвой модели (DeepSeek) через харнес.**

## Что прокликать (по шагам)

### A — verify-gate / авто-починка (Pipeline)
1. Запусти Pipeline (бриф→Plan→Execute→Verify) на задаче, где проверка СНАЧАЛА упадёт (напр. DoD = `npm run type`, а правка ломает типы).
2. Убедись: на Verify провал НЕ ведёт в Proof — кнопка стала «Починить», прогон возвращается на Execute (счётчик попытки).
3. После лимита (2 попытки) — статус `blocked` («Заблокировано — нужно вмешательство»), НЕ «completed».

### B — task-spec контракт
4. Прогони Pipeline-план: проверь, что шаги плана детальные (файлы, критерий готовности), а не «улучшить X». Это рычаг для дешёвой модели.

### C — профиль проекта
5. Создай `.verstak/profile.json` в проекте (или попроси агента собрать). Открой новый чат — убедись, что агент «знает» суть/стек проекта без ручного ввода (профиль в контексте).

### D — beast + max-steps
6. Переключи режим в `auto`/`bypass` — агент должен доводить до конца, не спрашивая очевидное, и проверять результат.
7. (опц.) Длинный зацикленный прогон → на лимите шагов агент отдаёт отчёт «сделано/не доделано/дальше», а не молчит.

### E — ГЛАВНОЕ: доказательство «Opus на DeepSeek»
8. Возьми ОДНУ реальную бизнес-задачу (напр. простой лендинг/правка/скрипт).
9. Прогони её через Pipeline на **дешёвой модели (DeepSeek)** с включённым харнесом.
10. Повтори ~10 раз (или на нескольких задачах). **Цель: 9 из 10 — рабочий результат.**
11. Сравни с тем же на Opus — близко ли по качеству? Это и есть проверка тезиса.

## DoD
- [ ] A: провал verify → авто-починка → blocked (не completed);
- [ ] B: шаги плана — детальные ТЗ;
- [ ] C: профиль виден агенту в новой сессии;
- [ ] D: auto-режим доводит до конца + max-steps отчёт;
- [ ] **E: DeepSeek через харнес даёт рабочий результат 9/10** ← гейт тезиса.

После — отпиши Павлу: что зелёное, где харнес течёт. Если E не 9/10 — **чинить харнес, не добавлять фичи.**

---
*Зоны: правки UI/стилей — твои (`src/`); если харнес-логика (`electron/`) течёт — отметь, поправлю я. Перед коммитом: `npm run type && npm run test:fast`.*
