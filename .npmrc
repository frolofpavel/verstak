# vite 8 ставится поверх electron-vite@5 (peer допускает только vite 5-7) —
# зависимости проверены и собираются; флаг зафиксирован здесь, чтобы
# `npm install` / `npm ci` работали без ручных флагов на чистой машине.
legacy-peer-deps=true
