/**
 * V2-3 (agent-runtime-v2.md §4) — completion gate: «написал ≠ сделал».
 *
 * Механизм «не выпускать финал без доказательства» в продукте был, но включался
 * только под recipe (`recipe.reviewer.required`, см. review-gate.ts). На обычном
 * пути агент мог записать файлы и объявить работу сделанной, ни разу её не
 * проверив. Baseline Arena 09.08 показал цену: слабые классы — правка связанных
 * файлов (1/3) и регрессия от собственной правки (2/3).
 *
 * Правило: были ПРИНЯТЫЕ записи в файлы и не было ни одной проверки за прогон —
 * финал не выпускаем, возвращаем модели ход с требованием доказательства.
 * Bounded по построению: после COMPLETION_GATE_MAX_NUDGES попыток прогон
 * закрывается честной формулировкой «сделано, не проверено» — видимой человеку,
 * а не выдачей работы за проверенную.
 *
 * ЕДИНСТВЕННЫЙ ИСТОЧНИК ПРАВИЛА — `scripts/agent-completion-gate.mjs`, и этот
 * файл его ре-экспортирует. Так сделано потому, что тот же гейт обязан работать
 * на CLI-пути (`scripts/verstak-cli.mjs`), который исполняется без сборки и без
 * Electron и потому не может импортировать из `electron/`. Обратное направление
 * возможно: TypeScript берёт .mjs через соседний `.d.ts` (allowJs в проекте
 * выключен и включать его ради одного модуля нельзя). Дубля логики нет — есть
 * один файл и две точки входа; именно расхождения этой пары боялся штаб.
 *
 * Промпт не трогаем (§4.1 system-layer — только по решению Павла): гейт живёт в
 * рантайме, потому что просьба в промпте — это то, что уже не сработало.
 */
export {
  COMPLETION_GATE_MAX_NUDGES,
  isVerificationToolCall,
  decideCompletionGate,
  buildCompletionGateNudge,
  unverifiedWorkNote,
} from '../../scripts/agent-completion-gate.mjs'
export type { CompletionGateDecision } from '../../scripts/agent-completion-gate.mjs'
